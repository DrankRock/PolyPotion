// Auto-generated by merge-and-categorize.py
// Contains merged, deduplicated, and recategorized shader presets
// Load after shader-lib.js (which defines AXIS, FRESNEL_FACTOR, buildShaderMaterial, etc.)

const PRESETS_BY_CATEGORY = {
    "Realistic": [{
            id: "frosted",
            name: "Frosted glass",
            category: "Realistic",
            kind: "physical",
            swatch: ['#eaf4f7', '#b9cdd4'],
            params: [{
                    key: 'color',
                    label: 'Tint',
                    type: 'color',
                    default: '#eef6f8',
                    apply: (m, v) => m.color.set(v)
                },
                {
                    key: 'roughness',
                    label: 'Frost',
                    type: 'range',
                    min: 0.1,
                    max: 1,
                    step: 0.01,
                    default: 0.55,
                    apply: (m, v) => {
                        m.roughness = v;
                    }
                },
            ],
            make: "() => new THREE.MeshPhysicalMaterial({ transmission: 1, thickness: 0.9, roughness: 0.55, metalness: 0, ior: 1.4, transparent: true, side: THREE.DoubleSide, envMapIntensity: 1 })"
        },
        {
            id: "chrome",
            name: "Chrome",
            category: "Realistic",
            kind: "physical",
            swatch: ['#f4f6fa', '#9aa2ad'],
            params: [{
                    key: 'color',
                    label: 'Tint',
                    type: 'color',
                    default: '#f4f6fa',
                    apply: (m, v) => m.color.set(v)
                },
                {
                    key: 'roughness',
                    label: 'Polish',
                    type: 'range',
                    min: 0,
                    max: 0.5,
                    step: 0.01,
                    default: 0.03,
                    apply: (m, v) => {
                        m.roughness = v;
                    }
                },
            ],
            make: "() => new THREE.MeshPhysicalMaterial({ metalness: 1, roughness: 0.03, color: 0xf4f6fa, envMapIntensity: 1.2, side: THREE.DoubleSide })"
        },
        {
            id: "brushed",
            name: "Brushed steel",
            category: "Realistic",
            kind: "physical",
            swatch: ['#c6cad0', '#7f858d'],
            params: [{
                    key: 'color',
                    label: 'Tint',
                    type: 'color',
                    default: '#b8bcc4',
                    apply: (m, v) => m.color.set(v)
                },
                {
                    key: 'roughness',
                    label: 'Grain',
                    type: 'range',
                    min: 0.1,
                    max: 0.8,
                    step: 0.01,
                    default: 0.38,
                    apply: (m, v) => {
                        m.roughness = v;
                    }
                },
            ],
            make: "() => new THREE.MeshPhysicalMaterial({ metalness: 1, roughness: 0.38, color: 0xb8bcc4, envMapIntensity: 1, side: THREE.DoubleSide })"
        },
        {
            id: "gold",
            name: "Gold",
            category: "Realistic",
            kind: "physical",
            swatch: ['#ffe08a', '#c8912f'],
            params: [{
                    key: 'color',
                    label: 'Metal',
                    type: 'color',
                    default: '#ffce5a',
                    apply: (m, v) => m.color.set(v)
                },
                {
                    key: 'roughness',
                    label: 'Polish',
                    type: 'range',
                    min: 0,
                    max: 0.7,
                    step: 0.01,
                    default: 0.18,
                    apply: (m, v) => {
                        m.roughness = v;
                    }
                },
            ],
            make: "() => new THREE.MeshPhysicalMaterial({ metalness: 1, roughness: 0.18, color: 0xffce5a, envMapIntensity: 1.2, side: THREE.DoubleSide })"
        },
        {
            id: "copper",
            name: "Copper",
            category: "Realistic",
            kind: "physical",
            swatch: ['#f0a878', '#a8582e'],
            params: [{
                    key: 'color',
                    label: 'Metal',
                    type: 'color',
                    default: '#d98b5a',
                    apply: (m, v) => m.color.set(v)
                },
                {
                    key: 'roughness',
                    label: 'Polish',
                    type: 'range',
                    min: 0,
                    max: 0.7,
                    step: 0.01,
                    default: 0.28,
                    apply: (m, v) => {
                        m.roughness = v;
                    }
                },
            ],
            make: "() => new THREE.MeshPhysicalMaterial({ metalness: 1, roughness: 0.28, color: 0xd98b5a, envMapIntensity: 1.1, side: THREE.DoubleSide })"
        },
        {
            id: "ceramic",
            name: "Glazed ceramic",
            category: "Realistic",
            kind: "physical",
            swatch: ['#f2f4f7', '#c3ccd4'],
            params: [{
                    key: 'color',
                    label: 'Glaze',
                    type: 'color',
                    default: '#eef1f4',
                    apply: (m, v) => m.color.set(v)
                },
                {
                    key: 'roughness',
                    label: 'Matte',
                    type: 'range',
                    min: 0.05,
                    max: 0.9,
                    step: 0.01,
                    default: 0.28,
                    apply: (m, v) => {
                        m.roughness = v;
                    }
                },
            ],
            make: "() => new THREE.MeshPhysicalMaterial({ metalness: 0, roughness: 0.28, clearcoat: 1, clearcoatRoughness: 0.08, color: 0xeef1f4, envMapIntensity: 1, side: THREE.DoubleSide })"
        },
        {
            id: "pearl",
            name: "Pearl / car paint",
            category: "Realistic",
            kind: "physical",
            swatch: ['#f7d7ee', '#8fb6e0'],
            params: [{
                    key: 'color',
                    label: 'Base',
                    type: 'color',
                    default: '#c05a9a',
                    apply: (m, v) => m.color.set(v)
                },
                {
                    key: 'iridescence',
                    label: 'Shift',
                    type: 'range',
                    min: 0,
                    max: 1,
                    step: 0.01,
                    default: 1,
                    apply: (m, v) => {
                        m.iridescence = v;
                    }
                },
            ],
            make: "() => new THREE.MeshPhysicalMaterial({ metalness: 0.6, roughness: 0.25, clearcoat: 1, clearcoatRoughness: 0.05, iridescence: 1, iridescenceIOR: 1.6, color: 0xc05a9a, envMapIntensity: 1.1, side: THREE.DoubleSide })"
        },
        {
            id: "velvet",
            name: "Velvet",
            category: "Realistic",
            kind: "physical",
            swatch: ['#7a2740', '#3a1020'],
            params: [{
                    key: 'color',
                    label: 'Cloth',
                    type: 'color',
                    default: '#5a1f2a',
                    apply: (m, v) => m.color.set(v)
                },
                {
                    key: 'sheen',
                    label: 'Sheen',
                    type: 'color',
                    default: '#d98a9a',
                    apply: (m, v) => m.sheenColor.set(v)
                },
            ],
            make: "() => new THREE.MeshPhysicalMaterial({ metalness: 0, roughness: 0.9, sheen: 1, sheenRoughness: 0.5, sheenColor: new THREE.Color(0xd98a9a), color: 0x5a1f2a, envMapIntensity: 0.6, side: THREE.DoubleSide })"
        },
        {
            id: "clay",
            name: "Matte clay",
            category: "Realistic",
            kind: "physical",
            swatch: ['#d1a184', '#8a5a3e'],
            params: [{
                key: 'color',
                label: 'Clay',
                type: 'color',
                default: '#c88f6a',
                apply: (m, v) => m.color.set(v)
            }, ],
            make: "() => new THREE.MeshPhysicalMaterial({ metalness: 0, roughness: 1, color: 0xc88f6a, envMapIntensity: 0.5, side: THREE.DoubleSide })"
        }
    ],
    "Liquids & potions": [{
        id: "gelatinouscube",
        name: "Gelatinous Cube",
        category: "Liquids & potions",
        kind: "shader",
        swatch: ['#8affb0', '#0a4a2a'],
        gravity: true,
        transparent: true,
        additive: true,
        depthWrite: false,
        fragBody: `\`
        float t = u_time * u_speed; float wobble = sin(rad * 4.0 + t * 2.0) * 0.2; float gel = fbm(vObjPos * 3.0 + t * 0.3 + wobble); float opacity = smoothstep(0.3, 0.7, gel) * 0.9; col = hsv2rgb(vec3(0.3 + gel * 0.2, 0.8, 1.0)) * opacity * 1.5; alpha = opacity;
      \``,
        params: [{
            key: 'speed',
            label: 'Wiggle',
            type: 'range',
            min: 0,
            max: 3,
            step: 0.05,
            default: 1.2
        }]
    }],
    "Organic & fleshy": [{
        id: "heartbeat",
        name: "Heartbeat",
        category: "Organic & fleshy",
        kind: "shader",
        swatch: ['#ff4a5e', '#3a0510'],
        params: [{
                key: 'base',
                label: 'Flesh',
                type: 'color',
                default: '#40141c'
            },
            {
                key: 'color',
                label: 'Pulse',
                type: 'color',
                default: '#ff3a52'
            },
            {
                key: 'scale',
                label: 'Veins',
                type: 'range',
                min: 1,
                max: 10,
                step: 0.1,
                default: 4
            },
            {
                key: 'speed',
                label: 'BPM',
                type: 'range',
                min: 0.2,
                max: 3,
                step: 0.05,
                default: 1
            },
        ],
        fragUniforms: "uniform vec3 u_base; uniform vec3 u_color; uniform float u_scale;",
        fragBody: `
      float tb = fract(u_time * u_speed * 0.85);
      float beat = exp(-pow((tb - 0.10) * 14.0, 2.0)) + 0.55 * exp(-pow((tb - 0.32) * 14.0, 2.0));
      float vein = pow(1.0 - abs(snoise(vObjPos * u_scale)), 7.0);
      col = u_base * (0.25 + 0.4 * diff);
      col += u_color * vein * (0.4 + beat * 2.2);
      col += u_color * pow(fres, 2.2) * (0.3 + beat * 1.4);
    `
    }],
    "Stylized & toon": [{
            id: "matcap",
            name: "Sculpt matcap",
            category: "Stylized & toon",
            kind: "shader",
            swatch: ['#b7bcc4', '#5c626b'],
            params: [{
                    key: 'color',
                    label: 'Body',
                    type: 'color',
                    default: '#8a8f98'
                },
                {
                    key: 'spec',
                    label: 'Highlight',
                    type: 'color',
                    default: '#ffffff'
                },
            ],
            fragUniforms: "uniform vec3 u_color; uniform vec3 u_spec;",
            fragBody: `
      vec3 vn = normalize(vViewNormal);
      vec2 muv = vn.xy*0.5 + 0.5;
      float d = distance(muv, vec2(0.68, 0.72));
      float spec = smoothstep(0.45, 0.0, d);
      float amb = 0.25 + 0.7*muv.y;
      col = u_color * amb + spec * u_spec * 1.2;
      col *= (0.6 + 0.4*(1.0 - pow(fres, 3.0)));
    `
        },
        {
            id: "iridescent",
            name: "Iridescent",
            category: "Stylized & toon",
            kind: "shader",
            swatch: ['#7affd0', '#b06be6'],
            params: [{
                    key: 'color',
                    label: 'Base',
                    type: 'color',
                    default: '#20232a'
                },
                {
                    key: 'bands',
                    label: 'Bands',
                    type: 'range',
                    min: 0.5,
                    max: 4,
                    step: 0.05,
                    default: 1.6
                },
                {
                    key: 'intensity',
                    label: 'Sheen',
                    type: 'range',
                    min: 0,
                    max: 1.5,
                    step: 0.02,
                    default: 1
                },
                {
                    key: 'speed',
                    label: 'Drift',
                    type: 'range',
                    min: 0,
                    max: 3,
                    step: 0.05,
                    default: 1
                },
            ],
            fragUniforms: "uniform vec3 u_color; uniform float u_bands; uniform float u_intensity;",
            fragBody: `
      float fp = pow(fres, 1.4);
      float hue = fract(u_time*0.03*u_speed + fp*u_bands + N.y*0.25 + vWorldPos.y*0.1);
      vec3 rain = hsv2rgb(vec3(hue, 0.85, 1.0));
      col = mix(u_color*diff, rain, clamp(fp*u_intensity, 0.0, 1.0));
      col += pow(fp, 4.0)*0.5;
    `
        },
        {
            id: "normals",
            name: "Normals",
            category: "Stylized & toon",
            kind: "shader",
            swatch: ['#8a8fff', '#5affa0'],
            params: [],
            fragUniforms: "",
            fragBody: `
      col = normalize(vWorldNormal)*0.5 + 0.5;
    `
        },
        {
            id: "topo",
            name: "Topographic",
            category: "Stylized & toon",
            kind: "shader",
            swatch: ['#e8dcc4', '#5a4a2a'],
            params: [{
                    key: 'base',
                    label: 'Paper',
                    type: 'color',
                    default: '#efe6d2'
                },
                {
                    key: 'color',
                    label: 'Ink',
                    type: 'color',
                    default: '#4a3a20'
                },
                {
                    key: 'scale',
                    label: 'Terrain',
                    type: 'range',
                    min: 0.5,
                    max: 8,
                    step: 0.1,
                    default: 2.5
                },
                {
                    key: 'bands',
                    label: 'Contours',
                    type: 'range',
                    min: 3,
                    max: 30,
                    step: 1,
                    default: 12
                },
                {
                    key: 'speed',
                    label: 'Shift',
                    type: 'range',
                    min: 0,
                    max: 3,
                    step: 0.05,
                    default: 0.5
                },
            ],
            fragUniforms: "uniform vec3 u_base; uniform vec3 u_color; uniform float u_scale; uniform float u_bands;",
            fragBody: `
      float f = fbm(vObjPos * u_scale + vec3(0.0, u_time * u_speed * 0.06, 0.0));
      float c = fract(f * u_bands);
      float line = smoothstep(0.10, 0.02, min(c, 1.0 - c));
      col = mix(u_base * (0.55 + 0.45 * diff), u_color, line);
      col += u_color * pow(fres, 3.0) * 0.15;
    `
        },
        {
            id: "voxelize",
            name: "Voxelizer",
            category: "Stylized & toon",
            kind: "shader",
            swatch: ['#ff5500', '#0000ff'],
            gravity: true,
            fragBody: `\`
        float voxelSize = 0.1;
        vec3 voxel = floor(vObjPos / voxelSize) * voxelSize;
        float noise = snoise(voxel * 3.0 + u_time * u_speed);
        float edge = step(0.5, fbm(voxel * 10.0));
        vec3 c1 = hsv2rgb(vec3(noise * 0.5 + 0.2, 1.0, 1.0));
        vec3 c2 = hsv2rgb(vec3(noise * 0.5 + 0.7, 1.0, 1.0));
        col = mix(c1, c2, edge) * (0.5 + diff * 0.5);
        col += vec3(1.0) * pow(fres, 5.0) * 1.5;
      \``
        }
    ],
    "Sci-fi & cyberpunk": [{
            id: "nanoswarm",
            name: "Nano-swarm",
            category: "Sci-fi & cyberpunk",
            kind: "shader",
            swatch: ['#ff5500', '#111111'],
            params: [{
                key: 'speed',
                label: 'Move',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.05,
                default: 2
            }, ],
            fragBody: `
      float t = u_time * u_speed;
      float s1 = step(0.85, snoise(vObjPos * 10.0 + vec3(t, 0.0, 0.0)));
      float s2 = step(0.85, snoise(vObjPos * 12.0 - vec3(0.0, t, 0.0)));
      float s3 = step(0.85, snoise(vObjPos * 14.0 + vec3(0.0, 0.0, t)));
      col = vec3(0.1) * diff;
      col += vec3(1.0, 0.3, 0.0) * (s1 + s2 + s3);
      col += vec3(1.0, 0.5, 0.0) * pow(fres, 3.0) * 1.5;
    `
        },
        {
            id: "fiberoptic",
            name: "Fiber Optic",
            category: "Sci-fi & cyberpunk",
            kind: "shader",
            swatch: ['#00ffcc', '#ff00ff'],
            gravity: true,
            transparent: true,
            additive: true,
            depthWrite: false,
            fragBody: `\`
        float t = u_time * u_speed;
        float hNorm = (h - u_levMin) / (u_levMax - u_levMin);
        float s = sin(h * 30.0 + t * 5.0 + rad * 10.0) * 0.5 + 0.5;
        float bright = smoothstep(0.85, 1.0, s);
        vec3 c1 = hsv2rgb(vec3(hNorm * 0.8, 1.0, 1.0));
        vec3 c2 = hsv2rgb(vec3(hNorm * 0.8 + 0.5, 1.0, 1.0));
        col = mix(c1, c2, bright) * bright * 3.0;
        col += vec3(1.0) * pow(fres, 3.0) * 1.5;
        alpha = bright * 0.9;
      \``
        },
        {
            id: "echolocation",
            name: "Echolocation",
            category: "Sci-fi & cyberpunk",
            kind: "shader",
            swatch: ['#00ff00', '#000000'],
            gravity: true,
            transparent: true,
            additive: true,
            depthWrite: false,
            fragBody: `\`
        float t = u_time * u_speed;
        float dist = rad;
        float wave = sin(dist * 30.0 - t * 8.0) * 0.5 + 0.5;
        float ring = smoothstep(0.45, 0.6, wave) * smoothstep(0.9, 0.8, dist);
        col = vec3(0.2, 1.0, 0.2) * ring * 2.0;
        col += vec3(0.8) * pow(fres, 4.0) * 0.5;
        alpha = ring * 0.8;
      \``
        },
        {
            id: "liquidcrystal",
            name: "Liquid Crystal",
            category: "Sci-fi & cyberpunk",
            kind: "shader",
            swatch: ['#ff80c0', '#80ff80'],
            gravity: true,
            transparent: true,
            depthWrite: false,
            fragBody: `\`
        float t = u_time * u_speed;
        float hNorm = (h - u_levMin) / (u_levMax - u_levMin);
        float n = fbm(vObjPos * 3.0 + t * 0.5);
        float swirl = sin(rad * 5.0 + h * 10.0 + t * 2.0 + n * 5.0) * 0.5 + 0.5;
        vec3 c1 = hsv2rgb(vec3(hNorm * 0.7 + n * 0.2, 1.0, 1.0));
        vec3 c2 = hsv2rgb(vec3(hNorm * 0.7 + 0.5 + n * 0.2, 1.0, 1.0));
        col = mix(c1, c2, swirl) * (0.4 + diff * 0.6);
        col += vec3(1.0) * pow(fres, 3.0) * 1.2;
        alpha = 0.7 + swirl * 0.3;
      \``
        },
        {
            id: "neon_circuits",
            name: "Neon Circuits",
            category: "Sci-fi & cyberpunk",
            kind: "shader",
            swatch: ["#0d0221", "#00ffff"],
            transparent: false,
            additive: false,
            depthWrite: true,
            params: [{
                    key: "circuitColor",
                    label: "Circuit Color",
                    type: "color",
                    default: "#00ffff"
                },
                {
                    key: "baseDark",
                    label: "Dark Base",
                    type: "color",
                    default: "#050510"
                },
                {
                    key: "density",
                    label: "Density",
                    type: "float",
                    default: 0.5,
                    min: 0.1,
                    max: 2.0
                },
                {
                    key: "pulseSpeed",
                    label: "Pulse Speed",
                    type: "float",
                    default: 1.0,
                    min: 0.1,
                    max: 5.0
                },
                {
                    key: "glowFalloff",
                    label: "Glow Falloff",
                    type: "float",
                    default: 1.0,
                    min: 0.5,
                    max: 3.0
                },
                {
                    key: "sparkle",
                    label: "Sparkle",
                    type: "float",
                    default: 0.5,
                    min: 0.0,
                    max: 1.0
                }
            ],
            fragUniforms: `
      uniform vec3 u_circuitColor;
      uniform vec3 u_baseDark;
      uniform float u_density;
      uniform float u_pulseSpeed;
      uniform float u_glowFalloff;
      uniform float u_sparkle;
    `,
            fragBody: `
      vec3 pos = vWorldPos * 0.5;
      vec2 uv = pos.xy * u_density;

      float trace = 0.0;
      float node = 0.0;
      float t = u_time * u_pulseSpeed;

      for (int i = 0; i < 4; i++) {
        float fi = float(i) + 1.0;
        vec2 offset = vec2(snoise(vec3(fi * 13.7, 0.0, 0.0)), snoise(vec3(fi * 17.3, 0.0, 0.0))) * 2.0;
        vec2 p = uv + offset + vec2(snoise(vec3(uv, fi * 23.1 + t * 0.1)), snoise(vec3(uv, fi * 29.7 + t * 0.13)));
        float d = abs(sin(p.x * 3.14159) * cos(p.y * 3.14159));
        d = 1.0 - smoothstep(0.0, 0.05, d) * smoothstep(0.15, 0.12, d);
        trace += d * 0.25;
        float nd = length(fract(p * 2.0) - 0.5);
        node += (1.0 - smoothstep(0.05, 0.15, nd)) * (0.5 + 0.5 * sin(t * 5.0 + fi));
      }
      trace = clamp(trace, 0.0, 1.0);
      node = clamp(node, 0.0, 1.0) * u_sparkle;

      vec3 glow = u_circuitColor * (trace * 1.5 + node * 2.0) * u_glowFalloff;
      float diffuseLit = diff * 0.2 + 0.8;
      col = mix(u_baseDark, glow, trace + node) * diffuseLit;
      col += glow * 0.2;
      alpha = 1.0;
    `
        },
        {
            id: "hologram",
            name: "Hologram",
            category: "Sci-fi & cyberpunk",
            kind: "shader",
            swatch: ["#00ffff", "#ff00ff"],
            transparent: true,
            additive: true,
            depthWrite: false,
            params: [{
                    key: "baseTint",
                    label: "Base Tint",
                    type: "color",
                    default: "#88ccff"
                },
                {
                    key: "shimmerColor",
                    label: "Shimmer Color",
                    type: "color",
                    default: "#ffffff"
                },
                {
                    key: "scanlineCnt",
                    label: "Scanline Count",
                    type: "float",
                    default: 30.0,
                    min: 5.0,
                    max: 80.0
                },
                {
                    key: "interferenceScale",
                    label: "Interference Scale",
                    type: "float",
                    default: 10.0,
                    min: 1.0,
                    max: 30.0
                },
                {
                    key: "opacity",
                    label: "Opacity",
                    type: "float",
                    default: 0.6,
                    min: 0.1,
                    max: 1.0
                },
                {
                    key: "flickerSpd",
                    label: "Flicker Speed",
                    type: "float",
                    default: 1.0,
                    min: 0.1,
                    max: 5.0
                }
            ],
            fragUniforms: `
      uniform vec3 u_baseTint;
      uniform vec3 u_shimmerColor;
      uniform float u_scanlineCnt;
      uniform float u_interferenceScale;
      uniform float u_opacity;
      uniform float u_flickerSpd;
    `,
            fragBody: `
      vec2 uv = vWorldPos.xy * u_interferenceScale;
      float t = u_time * u_flickerSpd;
      float scanline = sin(vWorldPos.y * u_scanlineCnt * 3.14159) * 0.5 + 0.5;
      scanline = scanline * 0.7 + 0.3;
      float interference = sin(uv.x * 8.0 + t) * cos(uv.y * 6.0 - t * 0.7);
      vec3 holoCol = u_baseTint * scanline;
      holoCol += u_shimmerColor * interference * 0.3;
      float rOffset = snoise(vec3(vWorldPos.xy, t)) * 0.02;
      col = holoCol * 0.8;
      col += holoCol * 0.2 * snoise(vec3(vWorldPos.xy * 4.0, t * 0.5));
      alpha = u_opacity * (0.6 + 0.4 * scanline) * clamp(interference + 0.5, 0.0, 1.0);
    `
        }
    ],
    "Glitch & digital": [{
            id: "glitch",
            name: "Glitch / corrupt",
            category: "Glitch & digital",
            kind: "shader",
            swatch: ['#ff2a6a', '#0a2a3a'],
            gravity: true,
            params: [{
                    key: 'color',
                    label: 'Base',
                    type: 'color',
                    default: '#1a7a8a'
                },
                {
                    key: 'rows',
                    label: 'Rows',
                    type: 'range',
                    min: 8,
                    max: 80,
                    step: 1,
                    default: 32
                },
                {
                    key: 'speed',
                    label: 'Corrupt',
                    type: 'range',
                    min: 0,
                    max: 3,
                    step: 0.05,
                    default: 1
                },
            ],
            fragUniforms: "uniform vec3 u_color; uniform float u_rows;\\nfloat hashf(float x){ return fract(sin(x * 127.1) * 43758.5453); }",
            fragBody: `\`
      float tq = floor(u_time * u_speed * 9.0);
      float row = floor(hn * u_rows);
      float rr = hashf(row * 1.37 + tq);
      vec3 base = u_color * (0.35 + 0.6 * diff);
      float rim = pow(fres, 2.0);
      base += vec3(rim * 0.8, 0.0, 0.0) * hashf(tq + 3.0) + vec3(0.0, 0.0, rim * 0.8) * hashf(tq + 7.0);
      if (rr > 0.86) base = vec3(1.0) - base;
      if (hashf(row + tq + 11.0) > 0.93) base *= 0.15;
      col = base * (0.85 + 0.15 * sin(hn * 500.0 + u_time * 20.0));
    \``
        },
        {
            id: "coderain",
            name: "Code rain",
            category: "Glitch & digital",
            kind: "shader",
            swatch: ['#3aff6a', '#020a04'],
            gravity: true,
            params: [{
                    key: 'color',
                    label: 'Glyphs',
                    type: 'color',
                    default: '#2aff5e'
                },
                {
                    key: 'cols',
                    label: 'Columns',
                    type: 'range',
                    min: 8,
                    max: 80,
                    step: 1,
                    default: 36
                },
                {
                    key: 'speed',
                    label: 'Rain',
                    type: 'range',
                    min: 0,
                    max: 3,
                    step: 0.05,
                    default: 1
                },
            ],
            fragUniforms: "uniform vec3 u_color; uniform float u_cols;\\nfloat hashf(float x){ return fract(sin(x * 127.1) * 43758.5453); }",
            fragBody: `\`
      float colId = floor((ang / 6.2831 + 0.5) * u_cols);
      float sp = 0.35 + hashf(colId) * 1.1;
      float v = fract(hn * (0.8 + hashf(colId + 5.0) * 0.6) + u_time * u_speed * sp * 0.35);
      float trail = pow(v, 5.0);
      float cell = floor(hn * 30.0);
      float glyph = 0.35 + 0.65 * step(0.3, hashf(colId * 7.3 + cell * 1.7 + floor(u_time * u_speed * 6.0) * step(0.93, hashf(cell + colId))));
      col = vec3(0.01, 0.03, 0.015);
      col += u_color * trail * glyph * 1.8 + vec3(0.85, 1.0, 0.9) * smoothstep(0.97, 1.0, v) * glyph;
      col += u_color * pow(fres, 2.5) * 0.25;
    \``
        },
        {
            id: "voidrift",
            name: "Void rift",
            category: "Glitch & digital",
            kind: "shader",
            swatch: ['#ff00ff', '#000000'],
            params: [{
                key: 'speed',
                label: 'Tear',
                type: 'range',
                min: 0,
                max: 3,
                step: 0.05,
                default: 1
            }, ],
            fragBody: `\`
      float t = u_time * u_speed;
      float rift = abs(sin(ang * 2.0 + hn * 5.0 + t * 2.0));
      float edge = smoothstep(0.0, 0.05, rift);
      vec3 worldCol = normalize(vWorldPos) * 0.5 + 0.5;
      vec3 voidCol = vec3(0.0);
      col = mix(worldCol * diff, voidCol, edge);
      col += vec3(1.0, 0.0, 1.0) * smoothstep(0.05, 0.0, rift) * 6.0; // Blinding neon rift edges
      col += vec3(0.0, 1.0, 1.0) * smoothstep(0.02, 0.0, rift) * 4.0;
    \``
        },
        {
            id: "missingtex",
            name: "Error 404",
            category: "Glitch & digital",
            kind: "shader",
            swatch: ['#ff00ff', '#000000'],
            params: [{
                key: 'speed',
                label: 'Glitch',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.05,
                default: 1.5
            }, ],
            fragBody: `\`
      float t = u_time * u_speed;
      vec3 p = vObjPos * 5.0;
      p += vec3(fbm(p + t), fbm(p - t), fbm(p + t * 0.5)) * 2.0;
      float checker = mod(floor(p.x) + floor(p.y) + floor(p.z), 2.0);
      vec3 c1 = vec3(0.0, 0.0, 0.0);
      vec3 c2 = vec3(1.0, 0.0, 1.0);
      col = mix(c1, c2, checker) * (0.5 + diff * 0.5);
      col += vec3(1.0) * pow(fres, 4.0) * 2.0;
    \``
        },
        {
            id: "timeslicer",
            name: "Time Slicer",
            category: "Glitch & digital",
            kind: "shader",
            swatch: ['#ff0000', '#00ff00'],
            gravity: true,
            fragBody: `\`
        float t = u_time * u_speed;
        float slice = sin(h * 15.0 - t * 5.0) * 0.5 + 0.5;
        float thickness = abs(fract(h * 15.0 - t * 5.0) - 0.5) * 2.0;
        float sliceEdge = smoothstep(0.8, 0.9, thickness) * smoothstep(0.95, 0.9, thickness);
        float glow = exp(-thickness * 10.0);
        vec3 c1 = vec3(1.0, 0.2, 0.1) * glow;
        vec3 c2 = vec3(0.1, 1.0, 0.2) * sliceEdge;
        col = (c1 + c2) * 1.5 * (0.5 + diff * 0.5);
        col += vec3(1.0) * pow(fres, 3.0) * 1.2;
      \``
        }
    ],
    "Cosmic & radiant": [{
            id: "sonar",
            name: "Sonar ping",
            category: "Cosmic & radiant",
            kind: "shader",
            swatch: ['#3aff8a', '#03251a'],
            transparent: true,
            additive: true,
            depthWrite: false,
            gravity: true,
            params: [{
                    key: 'color',
                    label: 'Ping',
                    type: 'color',
                    default: '#3aff8a'
                },
                {
                    key: 'speed',
                    label: 'Sweep',
                    type: 'range',
                    min: 0,
                    max: 3,
                    step: 0.05,
                    default: 1
                },
            ],
            fragUniforms: "uniform vec3 u_color;",
            fragBody: `\`
      float ping = fract(length(vObjPos) / E * 0.85 - u_time * u_speed * 0.45);
      float ring = pow(1.0 - ping, 9.0);
      float grid = pow(0.5 + 0.5 * sin(hn * 60.0), 12.0) * 0.15 + pow(0.5 + 0.5 * sin(ang * 24.0), 12.0) * 0.12;
      col = u_color * (0.04 + ring * 1.8) + u_color * grid + u_color * pow(fres, 2.0) * 0.35;
      alpha = clamp(0.22 + ring * 0.78, 0.0, 1.0);
    \``
        },
        {
            id: "radioactive",
            name: "Radioactive",
            category: "Cosmic & radiant",
            kind: "shader",
            swatch: ['#b6ff2a', '#1a2a05'],
            params: [{
                    key: 'base',
                    label: 'Sludge',
                    type: 'color',
                    default: '#1e2a10'
                },
                {
                    key: 'color',
                    label: 'Isotope',
                    type: 'color',
                    default: '#a8ff1e'
                },
                {
                    key: 'scale',
                    label: 'Cracks',
                    type: 'range',
                    min: 1,
                    max: 12,
                    step: 0.1,
                    default: 5
                },
                {
                    key: 'speed',
                    label: 'Decay',
                    type: 'range',
                    min: 0,
                    max: 3,
                    step: 0.05,
                    default: 1
                },
            ],
            fragUniforms: "uniform vec3 u_base; uniform vec3 u_color; uniform float u_scale;",
            fragBody: `
      float crack = pow(1.0 - abs(snoise(vObjPos * u_scale)), 9.0);
      float flick = 0.75 + 0.25 * sin(u_time * 13.0 * u_speed + snoise(vObjPos * 2.0 + u_time * 0.5) * 4.0);
      col = u_base * (0.3 + 0.5 * diff);
      col += u_color * crack * flick * 2.4;
      col += u_color * pow(fres, 2.5) * flick * 0.9;
    `
        },
        {
            id: "portal",
            name: "Portal vortex",
            category: "Cosmic & radiant",
            kind: "shader",
            swatch: ['#8a3aff', '#ff8a3a'],
            gravity: true,
            params: [{
                    key: 'color',
                    label: 'Void',
                    type: 'color',
                    default: '#180830'
                },
                {
                    key: 'hue',
                    label: 'Hue',
                    type: 'range',
                    min: 0,
                    max: 1,
                    step: 0.01,
                    default: 0.75
                },
                {
                    key: 'arms',
                    label: 'Arms',
                    type: 'range',
                    min: 1,
                    max: 8,
                    step: 1,
                    default: 3
                },
                {
                    key: 'speed',
                    label: 'Spin',
                    type: 'range',
                    min: 0,
                    max: 3,
                    step: 0.05,
                    default: 1
                },
            ],
            fragUniforms: "uniform vec3 u_color; uniform float u_hue; uniform float u_arms;",
            fragBody: `\`
      float sw = sin(ang * u_arms + rad * 16.0 - u_time * u_speed * 3.2 + fbm(vObjPos * 2.0) * 3.0);
      float sp = pow(0.5 + 0.5 * sw, 3.0);
      vec3 rain = hsv2rgb(vec3(fract(u_hue + rad * 0.35 - u_time * u_speed * 0.05), 0.85, 1.0));
      col = mix(u_color, rain * 2.0, sp);
      col += rain * pow(fres, 2.0) * 0.8;
    \``
        },
        {
            id: "soulfire",
            name: "Soul fire",
            category: "Cosmic & radiant",
            kind: "shader",
            swatch: ['#5ad2ff', '#0a1a4a'],
            transparent: true,
            additive: true,
            depthWrite: false,
            gravity: true,
            params: [{
                    key: 'color',
                    label: 'Flame',
                    type: 'color',
                    default: '#3ab8ff'
                },
                {
                    key: 'scale',
                    label: 'Licks',
                    type: 'range',
                    min: 1,
                    max: 10,
                    step: 0.1,
                    default: 4
                },
                {
                    key: 'speed',
                    label: 'Burn',
                    type: 'range',
                    min: 0,
                    max: 3,
                    step: 0.05,
                    default: 1
                },
            ],
            fragUniforms: "uniform vec3 u_color; uniform float u_scale;",
            fragBody: `\`
      float n = fbm(vObjPos * u_scale - upA * (u_time * u_speed * 1.8));
      float flame = pow(clamp(n * 0.5 + 0.5, 0.0, 1.0), 2.2) * (0.35 + 0.85 * hn);
      vec3 fc = mix(u_color * 0.4, mix(u_color, vec3(1.0), 0.75), flame);
      col = fc * (flame * 2.4 + pow(fres, 1.6) * 1.2);
      alpha = clamp(flame * 1.3 + pow(fres, 1.8) * 0.7, 0.0, 1.0);
    \``
        },
        {
            id: "star",
            name: "Living star",
            category: "Cosmic & radiant",
            kind: "shader",
            swatch: ['#ffdf6a', '#c83205'],
            displaceParam: true,
            params: [{
                    key: 'heat',
                    label: 'Heat',
                    type: 'range',
                    min: 0.5,
                    max: 2.5,
                    step: 0.05,
                    default: 1.2
                },
                {
                    key: 'scale',
                    label: 'Granules',
                    type: 'range',
                    min: 1,
                    max: 8,
                    step: 0.1,
                    default: 3
                },
                {
                    key: 'displace',
                    label: 'Boil',
                    type: 'range',
                    min: 0,
                    max: 0.2,
                    step: 0.005,
                    default: 0.04
                },
                {
                    key: 'speed',
                    label: 'Churn',
                    type: 'range',
                    min: 0,
                    max: 3,
                    step: 0.05,
                    default: 1
                },
            ],
            fragUniforms: "uniform float u_heat; uniform float u_scale;",
            fragBody: `
      float g1 = fbm(vObjPos * u_scale + vec3(u_time * u_speed * 0.10));
      float g2 = fbm(vObjPos * u_scale * 2.7 - vec3(u_time * u_speed * 0.07));
      float gr = clamp(0.5 + 0.5 * (g1 * 0.65 + g2 * 0.35), 0.0, 1.0);
      col = mix(vec3(0.45, 0.05, 0.0), vec3(0.98, 0.45, 0.05), smoothstep(0.15, 0.55, gr));
      col = mix(col, vec3(1.0, 0.96, 0.82) * u_heat, smoothstep(0.55, 0.9, gr));
      col += vec3(1.0, 0.55, 0.1) * pow(fres, 2.0) * 1.6;
    `
        },
        {
            id: "blackhole",
            name: "Black hole",
            category: "Cosmic & radiant",
            kind: "shader",
            swatch: ['#ff8a3a', '#000008'],
            gravity: true,
            params: [{
                    key: 'core',
                    label: 'Horizon',
                    type: 'range',
                    min: 0,
                    max: 0.8,
                    step: 0.01,
                    default: 0.35
                },
                {
                    key: 'heat',
                    label: 'Accretion',
                    type: 'range',
                    min: 0.3,
                    max: 2.5,
                    step: 0.05,
                    default: 1.2
                },
                {
                    key: 'speed',
                    label: 'Spin',
                    type: 'range',
                    min: 0,
                    max: 3,
                    step: 0.05,
                    default: 1
                },
            ],
            fragUniforms: "uniform float u_core; uniform float u_heat;",
            fragBody: `\`
      float disc = smoothstep(u_core, u_core + 0.18, fres);
      float swirl = sin(ang * 3.0 - pow(fres, 2.0) * 14.0 - u_time * u_speed * 2.6 + fbm(vObjPos * 3.0) * 2.0);
      vec3 hot = mix(vec3(1.0, 0.55, 0.1), vec3(0.4, 0.6, 1.0), 0.5 + 0.5 * swirl);
      col = hot * pow(fres, 3.5) * (1.6 + swirl * 0.4) * u_heat * disc;
    \``
        },
        {
            id: "aurora",
            name: "Aurora",
            category: "Cosmic & radiant",
            kind: "shader",
            swatch: ["#0b0b2b", "#00ff88"],
            transparent: false,
            additive: false,
            depthWrite: true,
            params: [{
                    key: "auroraColor1",
                    label: "Aurora Color 1",
                    type: "color",
                    default: "#00ff88"
                },
                {
                    key: "auroraColor2",
                    label: "Aurora Color 2",
                    type: "color",
                    default: "#0044ff"
                },
                {
                    key: "waveScale",
                    label: "Wave Scale",
                    type: "float",
                    default: 0.8,
                    min: 0.1,
                    max: 2.0
                },
                {
                    key: "speed",
                    label: "Speed",
                    type: "float",
                    default: 1.0,
                    min: 0.1,
                    max: 3.0
                },
                {
                    key: "altitude",
                    label: "Altitude",
                    type: "float",
                    default: 0.5,
                    min: 0.0,
                    max: 1.0
                },
                {
                    key: "rayCount",
                    label: "Ray Count",
                    type: "float",
                    default: 10.0,
                    min: 2.0,
                    max: 30.0
                }
            ],
            fragUniforms: `
      uniform vec3 u_auroraColor1;
      uniform vec3 u_auroraColor2;
      uniform float u_waveScale;
      uniform float u_speed;
      uniform float u_altitude;
      uniform float u_rayCount;
    `,
            fragBody: `
      vec2 uv = vWorldPos.xy * u_waveScale;
      float t = u_time * u_speed;
      float y = vWorldPos.y;
      float altMask = smoothstep(u_altitude - 0.3, u_altitude, y) * (1.0 - smoothstep(u_altitude + 0.3, u_altitude + 0.5, y));
      float wave = sin(uv.x * 2.0 + t * 0.5 + snoise(vec3(uv * 0.3, t * 0.2)) * 5.0) * 0.5 + 0.5;
      wave += sin(uv.x * 3.5 - t * 0.7 + snoise(vec3(uv * 0.25, t * 0.7)) * 3.0) * 0.3;
      float rays = 0.0;
      for (int i = 0; i < int(u_rayCount); i++) {
        float fi = float(i);
        float phase = fi * 0.73 + t * 0.13;
        float rx = sin(phase + y * 8.0) * 0.5 + 0.5;
        rays += 1.0 - smoothstep(0.0, 0.03, abs(uv.x - rx));
      }
      rays = clamp(rays / u_rayCount * 5.0, 0.0, 1.0);
      float intensity = wave * rays * altMask;
      vec3 auroraCol = mix(u_auroraColor1, u_auroraColor2, wave) * intensity;
      vec3 base = vec3(0.01, 0.01, 0.08);
      col = base + auroraCol * 1.5;
      alpha = 1.0;
    `
        },
        {
            id: "magnetar",
            name: "Magnetar core",
            category: "Cosmic & radiant",
            kind: "shader",
            swatch: ['#0055ff', '#000022'],
            transparent: true,
            additive: true,
            depthWrite: false,
            displaceParam: true,
            params: [{
                    key: 'speed',
                    label: 'Pulse',
                    type: 'range',
                    min: 0,
                    max: 4,
                    step: 0.05,
                    default: 1.5
                },
                {
                    key: 'displace',
                    label: 'Distort',
                    type: 'range',
                    min: 0,
                    max: 0.2,
                    step: 0.005,
                    default: 0.08
                },
            ],
            fragBody: `
      float t = u_time * u_speed;
      float field = sin(vObjPos.x * 6.0 + t) + cos(vObjPos.y * 6.0 - t) + sin(vObjPos.z * 6.0 + t * 0.5);
      field = abs(field);
      float core = smoothstep(1.2, 2.5, field);
      vec3 c1 = vec3(0.0, 0.1, 0.5);
      vec3 c2 = vec3(0.5, 0.8, 1.0);
      col = mix(c1, c2, pow(field, 3.0)) * diff;
      col += vec3(1.0) * core * 4.0; // Blinding magnetic arcs
      col += vec3(0.2, 0.6, 1.0) * pow(fres, 2.0) * 2.5;
      alpha = clamp(0.4 + core, 0.0, 1.0);
    `
        },
        {
            id: "galaxy",
            name: "Galaxy",
            category: "Cosmic & radiant",
            kind: "shader",
            swatch: ["#110022", "#ff88cc"],
            transparent: false,
            additive: false,
            depthWrite: true,
            params: [{
                    key: "coreColor",
                    label: "Core Color",
                    type: "color",
                    default: "#ffffff"
                },
                {
                    key: "dustColor",
                    label: "Dust Color",
                    type: "color",
                    default: "#2a0a4a"
                },
                {
                    key: "armCount",
                    label: "Arm Count",
                    type: "float",
                    default: 3.0,
                    min: 1.0,
                    max: 6.0
                },
                {
                    key: "tightness",
                    label: "Spiral Tightness",
                    type: "float",
                    default: 1.5,
                    min: 0.5,
                    max: 3.0
                },
                {
                    key: "starDensity",
                    label: "Star Density",
                    type: "float",
                    default: 0.6,
                    min: 0.1,
                    max: 1.0
                },
                {
                    key: "rotSpeed",
                    label: "Rotation Speed",
                    type: "float",
                    default: 0.2,
                    min: 0.0,
                    max: 1.0
                }
            ],
            fragUniforms: `
      uniform vec3 u_coreColor;
      uniform vec3 u_dustColor;
      uniform float u_armCount;
      uniform float u_tightness;
      uniform float u_starDensity;
      uniform float u_rotSpeed;
    `,
            fragBody: `
      vec2 uv = vWorldPos.xy * 0.8;
      float r = length(uv);
      float a = atan(uv.y, uv.x);
      float t = u_time * u_rotSpeed;
      float spiral = sin(a * u_armCount - r * u_tightness * 6.0 + t);
      spiral = smoothstep(-0.1, 0.3, spiral);
      float arms = spiral * (1.0 - r * 0.8);
      float dust = fbm(vec3(uv * 2.0, t * 0.1)) * 0.7;
      dust *= arms;
      float core = 1.0 - smoothstep(0.0, 0.25, r);
      core = pow(core, 1.5);
      vec3 colArm = mix(u_dustColor, u_coreColor, arms);
      col = colArm * (diff * 0.5 + 0.5);
      col += u_coreColor * core * 0.9;
      float stars = snoise(vec3(uv * 20.0, 1.0)) * 0.5 + 0.5;
      stars = smoothstep(0.97, 1.0, stars) * u_starDensity;
      col += stars * u_coreColor * 0.8;
      alpha = 1.0;
    `
        },
        {
            id: "vortex",
            name: "Vortex",
            category: "Cosmic & radiant",
            kind: "shader",
            swatch: ["#0b0b2b", "#ff5500"],
            transparent: false,
            additive: false,
            depthWrite: true,
            params: [{
                    key: "vortexColor",
                    label: "Vortex Color",
                    type: "color",
                    default: "#ff5500"
                },
                {
                    key: "coreGlow",
                    label: "Core Glow",
                    type: "float",
                    default: 0.8,
                    min: 0.1,
                    max: 2.0
                },
                {
                    key: "armCount",
                    label: "Arm Count",
                    type: "float",
                    default: 4.0,
                    min: 1.0,
                    max: 8.0
                },
                {
                    key: "twistSpd",
                    label: "Twist Speed",
                    type: "float",
                    default: 1.0,
                    min: 0.1,
                    max: 3.0
                },
                {
                    key: "streakLen",
                    label: "Streak Length",
                    type: "float",
                    default: 0.5,
                    min: 0.1,
                    max: 1.0
                },
                {
                    key: "backgroundDark",
                    label: "Background Darkness",
                    type: "float",
                    default: 0.9,
                    min: 0.5,
                    max: 1.0
                }
            ],
            fragUniforms: `
      uniform vec3 u_vortexColor;
      uniform float u_coreGlow;
      uniform float u_armCount;
      uniform float u_twistSpd;
      uniform float u_streakLen;
      uniform float u_backgroundDark;
    `,
            fragBody: `
      vec2 uv = vWorldPos.xy * 1.2;
      float r = length(uv);
      float a = atan(uv.y, uv.x);
      float t = u_time * u_twistSpd;
      float angleShift = a + log(r + 0.01) * 3.0 * u_armCount + t;
      float arms = sin(angleShift) * 0.5 + 0.5;
      arms *= smoothstep(0.8, 0.0, r);
      float streaks = 0.0;
      for (int i = 0; i < 4; i++) {
        float seed = float(i) * 1.7;
        vec2 dir = vec2(cos(seed), sin(seed));
        float proj = dot(uv, dir) * 3.0 + t;
        streaks += 1.0 - smoothstep(0.0, u_streakLen, abs(fract(proj) - 0.5));
      }
      streaks = clamp(streaks * 0.5, 0.0, 1.0);
      float core = exp(-r * 5.0) * u_coreGlow;
      vec3 colVortex = u_vortexColor * (arms * 0.7 + streaks * 0.3 + core);
      vec3 bg = vec3(0.0, 0.0, 0.02) * u_backgroundDark;
      col = bg + colVortex;
      alpha = 1.0;
    `
        }
    ],
    "Plasma & energy": [{
            id: "neonpulse",
            name: "Neon Pulse",
            category: "Plasma & energy",
            kind: "shader",
            swatch: ['#ff00ff', '#00ffff'],
            gravity: true,
            fragBody: `\`
        float t = u_time * u_speed;
        float pulse = sin(rad * 8.0 - t * 4.0) * 0.5 + 0.5;
        float ring = smoothstep(0.3, 0.4, pulse) * smoothstep(0.6, 0.5, pulse);
        vec3 c1 = hsv2rgb(vec3(fract(h * 1.5 + t * 0.1), 1.0, 1.0));
        vec3 c2 = hsv2rgb(vec3(fract(h * 1.5 + 0.5 + t * 0.1), 1.0, 1.0));
        col = mix(c1, c2, ring) * (0.5 + diff * 0.5);
        col += vec3(1.0) * pow(fres, 3.0) * 2.0;
      \``
        },
        {
            id: "plasma_globe",
            name: "Plasma Globe",
            category: "Plasma & energy",
            kind: "shader",
            swatch: "#9400D3",
            flags: {
                isAdditive: true
            },
            params: [{
                    name: 'Speed',
                    default: 1.0,
                    min: 0.0,
                    max: 3.0
                },
                {
                    name: 'Intensity',
                    default: 0.7,
                    min: 0.0,
                    max: 1.0
                },
                {
                    name: 'Ray Count',
                    default: 3,
                    min: 1,
                    max: 8,
                    step: 1
                },
                {
                    name: 'Color Scheme',
                    default: 0.5,
                    min: 0.0,
                    max: 1.0
                },
                {
                    name: 'Distortion',
                    default: 0.4,
                    min: 0.0,
                    max: 1.0
                },
            ],
            fragUniforms: `
      uniform float u_speed;
      uniform float u_intensity;
      uniform int u_rayCount;
      uniform float u_colorScheme;
      uniform float u_distortion;
    `,
            fragBody: `
      vec2 uv = vUv - 0.5;
      float r = length(uv);
      float a = atan(uv.y, uv.x);
      float angleShift = a + uTime * u_speed * 0.2;
      float rayCount = float(u_rayCount);
      float ray = sin(angleShift * rayCount) * 0.5 + 0.5;
      ray = smoothstep(0.2, 0.8, ray);

      float dist = 1.0 - r;
      float arc = sin((r * 10.0 - uTime * u_speed) * 2.0) * 0.5 + 0.5;
      arc = smoothstep(0.4, 0.6, arc);

      float centerGlow = exp(-r * 5.0) * 0.8;
      float plasma = (ray * arc * u_intensity + centerGlow) * step(0.0, dist);

      vec3 col = mix(
        hsv2rgb(vec3(fract(a / 6.28318 + u_colorScheme * 0.7), 0.9, 1.0)),
        vec3(0.8, 0.6, 1.0),
        0.5
      );
      col *= plasma;
      gl_FragColor = vec4(col, plasma);
    `
        },
        {
            id: "electric_arc",
            name: "Electric Arc",
            category: "Plasma & energy",
            kind: "shader",
            swatch: "#00FFFF",
            flags: {
                isAdditive: true
            },
            params: [{
                    name: 'Arc Density',
                    default: 0.7,
                    min: 0.0,
                    max: 1.0
                },
                {
                    name: 'Branching',
                    default: 0.5,
                    min: 0.0,
                    max: 1.0
                },
                {
                    name: 'Glow',
                    default: 0.8,
                    min: 0.0,
                    max: 1.0
                },
                {
                    name: 'Color',
                    default: 0.5,
                    min: 0.0,
                    max: 1.0,
                    isColor: true
                },
                {
                    name: 'Flicker',
                    default: 0.3,
                    min: 0.0,
                    max: 1.0
                },
            ],
            fragUniforms: `
      uniform float u_arcDensity;
      uniform float u_branching;
      uniform float u_glow;
      uniform float u_color;
      uniform float u_flicker;
    `,
            fragBody: `
      vec2 uv = vUv;
      float t = uTime * (1.0 + sin(uTime * 10.0) * u_flicker);
      float arc = 0.0;
      vec2 p = uv * 2.0 - 1.0;
      for (int i = 0; i < 5; i++) {
        float offset = float(i) * 0.3;
        float x = p.x;
        float y = p.y + sin(x * 8.0 + t + offset) * 0.2;
        float d = abs(y) - 0.02 * u_branching;
        d = 1.0 - smoothstep(0.0, 0.02, d);
        arc += d * u_arcDensity * 0.2;

        y = p.y + cos(x * 15.0 + t * 1.3) * 0.15;
        d = abs(y) - 0.01;
        arc += (1.0 - smoothstep(0.0, 0.02, d)) * 0.1 * u_branching;
      }
      arc = clamp(arc, 0.0, 1.0);

      float glow = exp(-length(p) * 3.0) * u_glow;
      vec3 col = hsv2rgb(vec3(u_color, 0.9, 1.0)) * (arc + glow);
      gl_FragColor = vec4(col, arc + glow > 0.0 ? 1.0 : 0.0);
    `
        }
    ],
    "Elemental & heat": [{
            id: "magmacore",
            name: "Magma core",
            category: "Elemental & heat",
            kind: "shader",
            swatch: ['#ff6a1e', '#141012'],
            params: [{
                    key: 'scale',
                    label: 'Cracks',
                    type: 'range',
                    min: 1,
                    max: 12,
                    step: 0.1,
                    default: 4
                },
                {
                    key: 'speed',
                    label: 'Pulse',
                    type: 'range',
                    min: 0,
                    max: 3,
                    step: 0.05,
                    default: 1
                },
            ],
            fragUniforms: "uniform float u_scale;",
            fragBody: `
      float crack = pow(1.0 - abs(snoise(vObjPos * u_scale)), 12.0);
      float pulse = 0.55 + 0.45 * sin(u_time * u_speed * 2.2 + snoise(vObjPos) * 3.0);
      col = vec3(0.06, 0.05, 0.055) * (0.6 + 0.8 * diff);
      col += mix(vec3(1.0, 0.25, 0.02), vec3(1.0, 0.8, 0.3), crack) * crack * pulse * 2.6;
    `
        },
        {
            id: "sandstorm",
            name: "Sandstorm",
            category: "Elemental & heat",
            kind: "shader",
            swatch: ['#d4a036', '#3a2a0a'],
            gravity: true,
            transparent: true,
            additive: true,
            depthWrite: false,
            fragBody: `\n float t = u_time * u_speed; float grains = 0.0; for(int i=0; i<4; i++){ float fi = float(i); grains += step(0.82, snoise(vObjPos * (8.0+fi*3.0) + vec3(fi*t*1.7, -fi*t*0.9, fi))); } col = vec3(0.8,0.5,0.2) * grains * 2.0; alpha = grains * 0.85;\n`,
            params: [{
                key: 'speed',
                label: 'Wind',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.05,
                default: 2.2
            }, ]
        },
        {
            id: "molten",
            name: "Molten",
            category: "Elemental & heat",
            kind: "shader",
            swatch: ["#ff4500", "#ffd700"],
            transparent: false,
            additive: false,
            depthWrite: true,
            params: [{
                    key: "hotColor",
                    label: "Hot Color",
                    type: "color",
                    default: "#ff6600"
                },
                {
                    key: "coolColor",
                    label: "Cool Color",
                    type: "color",
                    default: "#330000"
                },
                {
                    key: "temperature",
                    label: "Temperature",
                    type: "float",
                    default: 0.8,
                    min: 0.0,
                    max: 1.5
                },
                {
                    key: "flowSpeed",
                    label: "Flow Speed",
                    type: "float",
                    default: 1.0,
                    min: 0.1,
                    max: 5.0
                },
                {
                    key: "bumpStrength",
                    label: "Bump Strength",
                    type: "float",
                    default: 0.5,
                    min: 0.0,
                    max: 1.0
                },
                {
                    key: "glowFalloff",
                    label: "Glow Falloff",
                    type: "float",
                    default: 0.6,
                    min: 0.1,
                    max: 2.0
                }
            ],
            fragUniforms: `
      uniform vec3 u_hotColor;
      uniform vec3 u_coolColor;
      uniform float u_temperature;
      uniform float u_flowSpeed;
      uniform float u_bumpStrength;
      uniform float u_glowFalloff;
    `,
            fragBody: `
      vec2 uv = vWorldPos.xy * 2.5;
      float t = u_time * u_flowSpeed;
      float displ = fbm(vec3(uv + vec2(0.0, t * 0.7), 0.5)) * u_bumpStrength;
      displ += snoise(vec3(uv * 3.0 + vec2(0.0, t * 1.2), 0.9)) * u_bumpStrength * 0.4;
      float tempMap = u_temperature * (0.6 + 0.4 * displ);
      tempMap = clamp(tempMap, 0.0, 1.0);
      vec3 blackbody = mix(vec3(0.6, 0.2, 0.05), vec3(1.0, 0.85, 0.1), tempMap);
      if (tempMap > 0.7) blackbody = mix(blackbody, vec3(1.0, 0.95, 0.6), (tempMap - 0.7) * 5.0);
      vec3 lava = mix(u_coolColor, u_hotColor * blackbody, tempMap);
      float spec = pow(max(0.0, dot(N, normalize(V + KL))), 30.0);
      spec *= smoothstep(0.3, 0.7, tempMap);
      vec3 glow = u_hotColor * displ * u_glowFalloff;
      col = lava * (diff * 0.6 + 0.5) + spec * 0.8 + glow * 0.3;
      alpha = 1.0;
    `
        },
        {
            id: "flame",
            name: "Flame",
            category: "Elemental & heat",
            kind: "shader",
            swatch: ["#ff4500", "#ffd700"],
            transparent: false,
            additive: false,
            depthWrite: true,
            params: [{
                    key: "baseColor",
                    label: "Base Color",
                    type: "color",
                    default: "#ff8c00"
                },
                {
                    key: "tipColor",
                    label: "Tip Color",
                    type: "color",
                    default: "#ffff00"
                },
                {
                    key: "turbulence",
                    label: "Turbulence",
                    type: "float",
                    default: 1.0,
                    min: 0.2,
                    max: 2.0
                },
                {
                    key: "speed",
                    label: "Speed",
                    type: "float",
                    default: 1.0,
                    min: 0.1,
                    max: 4.0
                },
                {
                    key: "soot",
                    label: "Soot",
                    type: "float",
                    default: 0.15,
                    min: 0.0,
                    max: 0.5
                },
                {
                    key: "brightness",
                    label: "Brightness",
                    type: "float",
                    default: 1.2,
                    min: 0.5,
                    max: 2.0
                }
            ],
            fragUniforms: `
      uniform vec3 u_baseColor;
      uniform vec3 u_tipColor;
      uniform float u_turbulence;
      uniform float u_speed;
      uniform float u_soot;
      uniform float u_brightness;
    `,
            fragBody: `
      vec2 uv = vWorldPos.xy * 2.0;
      float t = u_time * u_speed;
      float y = vWorldPos.y * 0.8 + 0.5;
      float noise = fbm(vec3(uv * 1.5 + vec2(0.0, t * 0.8), 0.5)) * u_turbulence;
      noise += snoise(vec3(uv * 3.0 + vec2(0.0, t * 1.3), 0.9)) * u_turbulence * 0.5;
      float shape = (1.0 - y) * (1.0 + noise * 0.5);
      shape = clamp(shape, 0.0, 1.0);
      vec3 flameCol = mix(u_baseColor, u_tipColor, y * shape);
      float sootLayer = smoothstep(0.1, 0.3, noise) * u_soot * (1.0 - y);
      flameCol = mix(flameCol, vec3(0.02, 0.02, 0.02), sootLayer);
      float intensity = shape * u_brightness;
      col = flameCol * intensity * (diff * 0.5 + 0.6);
      alpha = 1.0;
    `
        }
    ],
    "Structural color": [{
            id: "pearlescent",
            name: "Nacre / Pearl",
            category: "Structural color",
            kind: "shader",
            swatch: ['#ffe8f0', '#c8a0d0'],
            gravity: true,
            params: [{
                    key: 'baseTone',
                    label: 'Base Tone',
                    type: 'color',
                    default: '#fae8f0'
                },
                {
                    key: 'iridStr',
                    label: 'Iridescence',
                    type: 'range',
                    min: 0,
                    max: 1.5,
                    step: 0.02,
                    default: 1.0
                },
                {
                    key: 'layers',
                    label: 'Layer Count',
                    type: 'range',
                    min: 2,
                    max: 10,
                    step: 1,
                    default: 5
                },
                {
                    key: 'gloss',
                    label: 'Gloss',
                    type: 'range',
                    min: 0,
                    max: 1,
                    step: 0.01,
                    default: 0.7
                },
                {
                    key: 'speed',
                    label: 'Shimmer',
                    type: 'range',
                    min: 0,
                    max: 3,
                    step: 0.05,
                    default: 0.5
                },
            ],
            fragUniforms: "uniform vec3 u_baseTone; uniform float u_iridStr; uniform float u_layers; uniform float u_gloss;",
            fragBody: `\`
      // Nacre: layered aragonite plates cause thin-film interference
      // Layer count controls how many "virtual layers" contribute
      float t = u_time * u_speed;
      vec3 iriAccum = vec3(0.0);
      float weightSum = 0.0;
      for (int i = 0; i < 10; i++) {
        if (float(i) >= u_layers) break;
        float fi = float(i);
        float depth = fi * 0.12;
        float phase = fres * 12.0 + depth * 8.0 + fi * 1.8;
        float w = exp(-fi * 0.35);
        vec3 layerCol = hsv2rgb(vec3(
          fract(0.08 + fres * 0.22 + fi * 0.03 + t * 0.02),
          0.45,
          0.55 + 0.45 * (0.5 + 0.5 * sin(phase))
        ));
        iriAccum += layerCol * w;
        weightSum += w;
      }
      vec3 iriCol = iriAccum / weightSum;
      // Soft diffuse base with pearlescent sheen
      vec3 base = u_baseTone * (0.4 + diff * 0.55);
      col = mix(base, iriCol, u_iridStr * 0.7);
      // High-gloss specular
      float spec = pow(max(dot(reflect(-V, N), KL), 0.0), 60.0);
      col += vec3(1.0) * spec * u_gloss * 1.8;
      col += iriCol * pow(fres, 2.5) * u_iridStr * 0.7;
    \``
        },
        {
            id: "oil_spill",
            name: "Oil Spill",
            category: "Structural color",
            kind: "shader",
            swatch: ["#0a0a0a", "#ff00cc"],
            transparent: false,
            additive: false,
            depthWrite: true,
            params: [{
                    key: "hueShift",
                    label: "Hue Shift",
                    type: "float",
                    default: 0.0,
                    min: 0.0,
                    max: 1.0
                },
                {
                    key: "iridescence",
                    label: "Iridescence",
                    type: "float",
                    default: 1.0,
                    min: 0.0,
                    max: 2.0
                },
                {
                    key: "noiseScale",
                    label: "Noise Scale",
                    type: "float",
                    default: 3.0,
                    min: 1.0,
                    max: 10.0
                },
                {
                    key: "flowSpeed",
                    label: "Flow Speed",
                    type: "float",
                    default: 1.0,
                    min: 0.1,
                    max: 5.0
                },
                {
                    key: "gloss",
                    label: "Gloss",
                    type: "float",
                    default: 0.8,
                    min: 0.0,
                    max: 2.0
                }
            ],
            fragUniforms: `
      uniform float u_hueShift;
      uniform float u_iridescence;
      uniform float u_noiseScale;
      uniform float u_flowSpeed;
      uniform float u_gloss;
    `,
            fragBody: `
      vec2 uv = vWorldPos.xy * u_noiseScale;
      uv.y += u_time * u_flowSpeed;
      float n = fbm(vec3(uv, 0.5));
      n = n * 0.5 + 0.5;
      float thickness = pow(n, 0.8) * u_iridescence;
      float hue = fract(thickness + u_hueShift + length(vObjPos) * 0.2);
      vec3 oilCol = hsv2rgb(vec3(hue, 0.9, 0.7));
      float spec = pow(max(0.0, dot(N, normalize(V + KL))), 60.0);
      spec *= u_gloss;
      vec3 base = vec3(0.02, 0.01, 0.04);
      col = mix(base, oilCol, thickness * 0.8 + 0.2) + spec * oilCol * 0.7;
      alpha = 1.0;
    `
        }
    ],
    "Aged & weathered": [{
            id: "corrosion",
            name: "Rusted Iron",
            category: "Aged & weathered",
            kind: "shader",
            swatch: ['#8a3a1a', '#3a3a3a'],
            gravity: true,
            params: [{
                    key: 'rust',
                    label: 'Rust Color',
                    type: 'color',
                    default: '#b8451e'
                },
                {
                    key: 'iron',
                    label: 'Bare Iron',
                    type: 'color',
                    default: '#4a4a52'
                },
                {
                    key: 'decay',
                    label: 'Decay',
                    type: 'range',
                    min: 0,
                    max: 1,
                    step: 0.01,
                    default: 0.65
                },
                {
                    key: 'flakeSz',
                    label: 'Flake Size',
                    type: 'range',
                    min: 1,
                    max: 10,
                    step: 0.5,
                    default: 4
                },
                {
                    key: 'rough',
                    label: 'Roughness',
                    type: 'range',
                    min: 0,
                    max: 1,
                    step: 0.01,
                    default: 0.6
                },
                {
                    key: 'speed',
                    label: 'Creep',
                    type: 'range',
                    min: 0,
                    max: 2,
                    step: 0.05,
                    default: 0.2
                },
            ],
            fragUniforms: "uniform vec3 u_rust; uniform vec3 u_iron; uniform float u_decay; uniform float u_flakeSz; uniform float u_rough;",
            fragBody: `\`
      float t = u_time * u_speed;
      // Multi-octave rust map: large patches + fine granularity
      float rustLarge = fbm(vObjPos * u_flakeSz * 0.7 + vec3(t * 0.1, 0.0, 0.0));
      float rustFine  = fbm(vObjPos * u_flakeSz * 2.5 + vec3(0.0, t * 0.15, 0.0));
      float edgeRust  = pow(fres, 1.8); // rust starts at edges
      float rustMap   = clamp(rustLarge * 0.6 + rustFine * 0.3 + edgeRust * 0.2, 0.0, 1.0);
      float rustMask  = smoothstep(0.35, 0.55 + (1.0 - u_decay) * 0.3, rustMap);
      // Flaking: sharp cracks where rust meets iron
      float flakeCrack = pow(abs(snoise(vObjPos * u_flakeSz * 3.0)), 12.0);
      float flake = flakeCrack * rustMask * u_decay;
      // Iron has specular highlights; rust is matte
      float ironSpec = pow(max(dot(reflect(-V, N), KL), 0.0), 30.0) * (1.0 - u_rough);
      vec3 ironCol = u_iron * (0.3 + diff * 0.6) + vec3(1.0) * ironSpec * 0.7;
      vec3 rustCol = u_rust * (0.35 + diff * 0.45);
      // Rust gets darker / crustier in crevices
      rustCol *= 0.7 + 0.3 * (1.0 - rustFine);
      col = mix(ironCol, rustCol, rustMask);
      // Bright orange edge where fresh rust flakes are exposing
      col += u_rust * 1.3 * flake * 0.6;
      col += u_rust * pow(fres, 3.5) * 0.2;
    \``
        },
        {
            id: "cracked_earth",
            name: "Cracked Earth",
            category: "Aged & weathered",
            kind: "shader",
            swatch: ["#8b5a2b", "#3e2723"],
            transparent: false,
            additive: false,
            depthWrite: true,
            params: [{
                    key: "dryColor",
                    label: "Dry Color",
                    type: "color",
                    default: "#c8a96e"
                },
                {
                    key: "crackColor",
                    label: "Crack Color",
                    type: "color",
                    default: "#2d1c15"
                },
                {
                    key: "crackWidth",
                    label: "Crack Width",
                    type: "float",
                    default: 0.3,
                    min: 0.1,
                    max: 1.0
                },
                {
                    key: "crackDensity",
                    label: "Crack Density",
                    type: "float",
                    default: 2.0,
                    min: 0.5,
                    max: 5.0
                },
                {
                    key: "dustAmount",
                    label: "Dust Amount",
                    type: "float",
                    default: 0.3,
                    min: 0.0,
                    max: 1.0
                },
                {
                    key: "age",
                    label: "Age",
                    type: "float",
                    default: 0.7,
                    min: 0.0,
                    max: 1.0
                }
            ],
            fragUniforms: `
      uniform vec3 u_dryColor;
      uniform vec3 u_crackColor;
      uniform float u_crackWidth;
      uniform float u_crackDensity;
      uniform float u_dustAmount;
      uniform float u_age;
    `,
            fragBody: `
      vec3 pos = vWorldPos * u_crackDensity;
      float n = fbm(vec3(pos.xy * 1.8, 0.3));
      float ridge = fbm(vec3(pos.xy * 3.2, 1.7));
      float crackMask = smoothstep(u_crackWidth, u_crackWidth + 0.05, abs(ridge - 0.5) * 2.0);
      crackMask *= 1.0 - smoothstep(0.4, 0.6, n);
      float dustNoise = snoise(vec3(pos.xy * 8.0, u_time * 0.05)) * 0.5 + 0.5;
      float dust = dustNoise * u_dustAmount * (1.0 - crackMask);
      vec3 soil = mix(u_dryColor, u_crackColor, crackMask);
      soil = mix(soil, u_dryColor * 1.1, dust * u_age);
      vec3 finalCol = soil * (diff * 0.7 + 0.4);
      col = finalCol;
      alpha = 1.0;
    `
        }
    ],
    "Psychedelic & mind-bending": [{
            id: "overload",
            name: "Sensory overload",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#ff00ff', '#00ffff'],
            params: [{
                key: 'speed',
                label: 'Trip',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.05,
                default: 1.5
            }, ],
            fragBody: `
      float t = u_time * u_speed;
      float n1 = fbm(vObjPos * 4.0 + t * 0.5);
      float n2 = fbm(vObjPos * 6.0 - t * 0.8);
      float n3 = fbm(vObjPos * 3.0 + vec3(t * 1.2, -t * 0.5, 0.0));
      vec3 c1 = hsv2rgb(vec3(n1 * 0.5 + t * 0.1, 1.0, 1.0));
      vec3 c2 = hsv2rgb(vec3(n2 * 0.5 + 0.5, 1.0, 1.0));
      col = mix(c1, c2, n3) * (0.5 + diff * 0.8);
      col += vec3(1.0) * pow(fres, 4.0) * 3.0;
    `
        },
        {
            id: "moireheadache",
            name: "Moiré Madness",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#000000', '#ffffff'],
            params: [{
                key: 'speed',
                label: 'Spin',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.05,
                default: 1.5
            }],
            fragBody: `
        float t = u_time * u_speed;
        float x = vObjPos.x * 20.0;
        float y = vObjPos.y * 20.0;
        float m1 = sin(x * 2.0 + t) * sin(y * 2.0 - t);
        float m2 = sin(x * 1.5 - t * 1.3) * sin(y * 1.5 + t * 1.3);
        float m = abs(m1 - m2);
        col = vec3(m) * diff * 2.0;
        col += vec3(1.0) * pow(fres, 5.0) * 1.0;
      `
        },
        {
            id: "brainmelting",
            name: "Brain Melting",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#ffaa00', '#aa00ff'],
            params: [{
                key: 'speed',
                label: 'Trip',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.05,
                default: 2.0
            }],
            fragBody: `
        float t = u_time * u_speed;
        float n1 = fbm(vObjPos * 4.0 + t * 0.7);
        float n2 = fbm(vObjPos * 5.0 - t * 0.9);
        float n3 = fbm(vObjPos * 3.0 + vec3(t * 1.1, -t * 0.6, 0.0));
        float melt = sin(vObjPos.y * 10.0 + n1 * 8.0 + t) * 0.5 + 0.5;
        vec3 hot = vec3(1.0, 0.6, 0.1) * (1.0 - melt) * 2.0;
        vec3 cold = vec3(0.2, 0.1, 1.0) * melt * 2.0;
        col = mix(hot, cold, n3) * diff;
        col += vec3(1.0) * pow(fres, 4.0) * 1.8;
      `
        },
        {
            id: "cellularnoise",
            name: "Cellular Chaos",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#ff0088', '#00ff88'],
            gravity: true,
            fragBody: `\`
        float t = u_time * u_speed;
        vec3 p = vObjPos * 4.0;
        vec3 cell = floor(p) + 0.5;
        vec3 subs = p - cell;
        float dist = length(subs) * 0.8;
        float n = snoise(cell + t * 0.2);
        float brightness = 1.0 - smoothstep(0.0, 0.5, dist);
        vec3 c1 = hsv2rgb(vec3(n * 0.8, 1.0, 1.0));
        vec3 c2 = vec3(1.0);
        col = mix(vec3(0.05), c1, brightness * 0.8) * diff;
        col += c2 * pow(fres, 4.0) * 1.5;
      \``
        },
        {
            id: "neonwireframe",
            name: "Neon Wireframe",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ["#000000", "#ff0055"],
            gravity: false,
            fragBody: "float t = u_time * u_speed; float edge = abs(1.0 - 2.0 * fbm(vObjPos * 20.0 + t * 0.5)); edge = smoothstep(0.7,0.9,edge); vec3 wire = vec3(1.0, 0.0, 0.5) * edge * 3.0; col = wire * diff; col += vec3(1.0) * pow(fres,5.0)*0.5;",
            params: [{
                "key": "speed",
                "label": "Wire",
                "type": "range",
                "min": 0,
                "max": 4,
                "step": 0.05,
                "default": 1.0
            }]
        },
        {
            id: "psychedelicfractal",
            name: "Psychedelic Fractal",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ["#ff00ff", "#ffff00"],
            gravity: false,
            params: [{
                "key": "speed",
                "label": "Trip",
                "type": "range",
                "min": 0,
                "max": 4,
                "step": 0.05,
                "default": 2.5
            }],
            fragBody: "float t = u_time * u_speed; float z = 1.0; for(int i=0; i<3; i++){ z = fbm(vObjPos * z * 2.0 + t * 0.2) * 2.0; } vec3 c = hsv2rgb(vec3(z*0.8+t*0.1, 1.0, 1.0)); col = c * diff; col += vec3(1.0)*pow(fres,4.0)*2.0;"
        },
        {
            id: "nebulaecho",
            name: "Nebula Echo",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#ff80c0', '#200840'],
            gravity: true,
            fragBody: `\n float t = u_time * u_speed; float cloud = fbm(vObjPos*2.5 + t*0.15); float echo = sin(cloud*12.0)*0.5+0.5; vec3 col1 = hsv2rgb(vec3(0.8+cloud*0.2, 0.7, 0.5)); vec3 col2 = hsv2rgb(vec3(0.6+cloud*0.3, 0.8, 0.8)); col = mix(col1, col2, echo) * diff; col += vec3(1.0,0.7,1.0)*pow(fres,3.0)*0.9;\n`,
            params: [{
                key: 'speed',
                label: 'Drift',
                type: 'range',
                min: 0,
                max: 3,
                step: 0.05,
                default: 1.0
            }, ]
        },
        {
            id: "electricarc",
            name: "Electric Arc",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#ffffff', '#0022ff'],
            gravity: true,
            transparent: true,
            additive: true,
            depthWrite: false,
            fragBody: `\`
        float t = u_time * u_speed; float arc = 0.0; for(int i=0; i<3; i++){ float fi = float(i); arc += pow(max(0.0, 1.0 - abs(sin(rad*(7.0+fi*3.0)+t*2.0 + fi*1.2)*0.5+0.5 - 0.7)*15.0), 3.0); } arc = clamp(arc,0.0,1.0); col = vec3(0.8,0.9,1.0) * arc * 4.0; col += vec3(1.0)*pow(fres,5.0)*0.8; alpha = arc*0.95;
      \``,
            params: [{
                key: 'speed',
                label: 'Zap',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.05,
                default: 2.5
            }]
        },
        {
            id: "flowymercury",
            name: "Flow Mercury",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#c0c0d0', '#3a3a4a'],
            gravity: true,
            fragBody: `\n float t = u_time * u_speed; float flow = fbm(vObjPos*3.0 + t*0.4); float streak = abs(sin(ang*4.0 + flow*7.0 + t)*0.5+0.5); col = mix(vec3(0.2,0.22,0.3), vec3(0.8,0.82,0.9), streak) * diff; col += vec3(1.0)*pow(fres,3.0)*0.7;\n`,
            params: [{
                key: 'speed',
                label: 'Flow',
                type: 'range',
                min: 0,
                max: 3,
                step: 0.05,
                default: 1.3
            }, ]
        },
        {
            id: "candyfloss",
            name: "Candy Floss",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#ffaacc', '#ffffff'],
            gravity: false,
            fragBody: "float t = u_time * u_speed; float fluff = fbm(vObjPos*5.0 + t*0.6); float thickness = smoothstep(0.3, 0.8, fluff); vec3 cotton = mix(vec3(1.0,0.7,0.8), vec3(1.0,0.9,0.95), thickness); col = cotton * diff; col += vec3(1.0)*pow(fres,2.0)*1.2;",
            params: [{
                key: 'speed',
                label: 'Spin',
                type: 'range',
                min: 0,
                max: 3,
                step: 0.05,
                default: 0.8
            }, ]
        },
        {
            id: "glitchgrid",
            name: "Glitch Grid",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#ff00ff', '#000000'],
            gravity: true,
            transparent: true,
            additive: true,
            depthWrite: false,
            fragBody: `\n float t = u_time * u_speed; vec3 grid = fract(vObjPos*6.0); float gx = smoothstep(0.08,0.1,grid.x); float gy = smoothstep(0.08,0.1,grid.y); float gz = smoothstep(0.08,0.1,grid.z); float g = max(max(gx,gy),gz); float glitch = step(0.92, snoise(vObjPos*3.0 + t*10.0)); g = max(g, glitch); col = vec3(1.0,0.2,1.0) * g * 3.0; alpha = g*0.9;\n`,
            params: [{
                key: 'speed',
                label: 'Scan',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.05,
                default: 1.8
            }, ]
        },
        {
            id: "sparkleswirl",
            name: "Sparkle Swirl",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#ffdd55', '#550066'],
            gravity: true,
            fragBody: `\n float t = u_time * u_speed; float a = ang + t*2.0 + rad*8.0; float spark = pow(max(sin(a*6.0)*0.5+0.5, 0.0), 20.0); vec3 base = hsv2rgb(vec3(0.15+rad*0.2, 0.9, 0.7)); col = base * diff + vec3(1.0,0.9,0.3)*spark*5.0; col += vec3(1.0)*pow(fres,4.0)*0.5;\n`,
            params: [{
                key: 'speed',
                label: 'Spin',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.05,
                default: 1.5
            }, ]
        },
        {
            id: "bubblecolumn",
            name: "Bubble Column",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#80d0ff', '#0a2a4a'],
            gravity: true,
            transparent: true,
            additive: true,
            depthWrite: false,
            fragBody: `\n float t = u_time * u_speed; float rise = fract(h*4.0 - t*0.9); float bubble = step(0.85, snoise(vObjPos*10.0 + vec3(0.0,t*2.5,0.0))); bubble *= smoothstep(0.0, 0.2, rise)*smoothstep(0.8,0.6,rise); col = vec3(0.3,0.8,1.0) * bubble * 2.5; col += vec3(1.0)*pow(fres,3.0)*0.4; alpha = bubble*0.9;\n`,
            params: [{
                key: 'speed',
                label: 'Speed',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.05,
                default: 1.6
            }, ]
        },
        {
            id: "hologlitch",
            name: "Holo-Glitch",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#ffaa00', '#0055ff'],
            gravity: true,
            fragBody: `\n float t = u_time * u_speed; float holo = fbm(vObjPos*4.0 + t*0.5); float g = step(0.65, holo) * smoothstep(0.3,0.8,holo); vec3 c1 = hsv2rgb(vec3(0.6+holo*0.2,1.0,1.0)); vec3 c2 = vec3(0.9); col = mix(c1*0.3, c2, g) * diff; col += vec3(1.0)*pow(fres,4.0)*0.8;\n`,
            params: [{
                key: 'speed',
                label: 'Shift',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.05,
                default: 1.0
            }, ]
        },
        {
            id: "rainbowwave",
            name: "Rainbow Wave",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#ff0066', '#00ffcc'],
            gravity: true,
            fragBody: `\`
        float t = u_time * u_speed; float wave = sin(rad*6.0 + h*8.0 - t*4.0)*0.5+0.5; col = hsv2rgb(vec3(fract(h*2.0+t*0.1), 1.0, 1.0)) * wave * 2.0 * diff; col += vec3(1.0)*pow(fres,3.0)*1.2;
      \``,
            params: [{
                key: 'speed',
                label: 'Speed',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.05,
                default: 2.0
            }]
        },
        {
            id: "octarine",
            name: "Octarine",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#7a00ff', '#ff0088'],
            gravity: true,
            fragBody: `\n float t = u_time * u_speed; float n = fbm(vObjPos*5.0 + t*0.4); float magic = sin(n*12.0)*0.5+0.5; vec3 c = hsv2rgb(vec3(0.8+n*0.2, 0.9, 1.0)); col = c * magic * 2.0 * diff; col += vec3(0.8,0.2,1.0)*pow(fres,4.0)*0.6;\n`,
            params: []
        },
        {
            id: "shatteredglass",
            name: "Shattered Glass",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#88ccff', '#1a2a3a'],
            gravity: true,
            fragBody: `\n float t = u_time * u_speed; vec3 p = vObjPos*12.0; vec3 f1 = floor(p); float crack = abs(snoise(f1 + t*0.1)); float shard = step(0.7, crack); col = mix(vec3(0.1,0.2,0.3), vec3(0.6,0.8,1.0), shard) * diff; col += vec3(1.0)*pow(fres,5.0)*0.9;\n`,
            params: [{
                key: 'speed',
                label: 'Stress',
                type: 'range',
                min: 0,
                max: 3,
                step: 0.05,
                default: 1.0
            }, ]
        },
        {
            id: "phaseghost",
            name: "Phase Ghost",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#d0f0ff', '#4a6a8a'],
            gravity: true,
            transparent: true,
            additive: true,
            depthWrite: false,
            fragBody: `\`
        float t = u_time * u_speed; float phase = sin(t*3.0 + rad*10.0)*0.5+0.5; float v = fbm(vObjPos*2.0 + t*0.7); col = vec3(0.8,0.9,1.0) * v * phase * 2.0; alpha = v*phase*0.85;
      \``,
            params: [{
                key: 'speed',
                label: 'Phase',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.05,
                default: 1.5
            }]
        },
        {
            id: "liquidmetal",
            name: "Liquid Metal",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#b0b0c0', '#1a1a2a'],
            gravity: true,
            fragBody: `\n float t = u_time * u_speed; float flow = fbm(vObjPos*2.5 + t*0.3); float spec = pow(max(dot(reflect(-V, N), KL), 0.0), 30.0); col = mix(vec3(0.1,0.12,0.18), vec3(0.8,0.82,0.9), flow) * diff; col += vec3(1.0)*spec*1.8;\n`,
            params: [{
                key: 'speed',
                label: 'Flow',
                type: 'range',
                min: 0,
                max: 3,
                step: 0.05,
                default: 1.0
            }, ]
        },
        {
            id: "inkdrop",
            name: "Ink Drop",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#000000', '#ffffff'],
            gravity: true,
            fragBody: `\n float t = u_time * u_speed; float n = fbm(vObjPos*3.0 + t*0.2); float ink = smoothstep(0.4, 0.7, n); col = vec3(1.0-ink) * diff; col += vec3(0.1)*pow(fres,2.0);\n`,
            params: []
        },
        {
            id: "shockwave",
            name: "Shockwave",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#ffaa00', '#550000'],
            gravity: true,
            transparent: true,
            additive: true,
            depthWrite: false,
            fragBody: `\n float t = u_time * u_speed; float wave = sin(rad*15.0 - t*6.0)*0.5+0.5; wave = smoothstep(0.2,0.6,wave) * exp(-rad*2.0); col = vec3(1.0,0.6,0.1) * wave * 3.0; alpha = wave*0.9;\n`,
            params: [{
                key: 'speed',
                label: 'Boom',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.05,
                default: 2.0
            }, ]
        },
        {
            id: "tvstatic",
            name: "TV Static",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#888888', '#000000'],
            gravity: false,
            fragBody: "float t = u_time * u_speed; float noise = snoise(vObjPos*30.0 + t*50.0); float mono = noise*0.5+0.5; col = vec3(mono) * diff; col += vec3(1.0)*pow(fres,2.0)*0.2;",
            params: [{
                key: 'speed',
                label: 'Noise',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.05,
                default: 1.0
            }, ]
        },
        {
            id: "motherofflowers",
            name: "Mother of Flowers",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#ff88bb', '#2288ff'],
            gravity: true,
            fragBody: `\n float t = u_time * u_speed; float petal = sin(ang*5.0 + rad*12.0 + t)*0.5+0.5; petal = pow(petal, 3.0); vec3 c = hsv2rgb(vec3(0.8+petal*0.2, 0.8, 0.9)); col = c * petal * 2.0 * diff; col += vec3(1.0,0.6,0.8)*pow(fres,3.0)*0.7;\n`,
            params: [{
                key: 'speed',
                label: 'Bloom',
                type: 'range',
                min: 0,
                max: 3,
                step: 0.05,
                default: 1.0
            }, ]
        },
        {
            id: "quantumfoam",
            name: "Quantum Foam",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#00ffff', '#ff00ff'],
            gravity: true,
            transparent: true,
            additive: true,
            depthWrite: false,
            fragBody: ` float t = u_time * u_speed; float n = fbm(vObjPos * 8.0 + t * 0.3); float bubbles = step(0.6, n) * n; vec3 c1 = hsv2rgb(vec3(fract(h*2.0 + t*0.2), 1.0, 1.0)); vec3 c2 = vec3(1.0); col = mix(c1*0.3, c2, bubbles) * diff; col += vec3(1.0,0.7,1.0) * pow(fres,4.0)*1.5; alpha = bubbles*0.9; `,
            params: [{
                key: 'speed',
                label: 'Brew',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.05,
                default: 1.2
            }]
        },
        {
            id: "plasmavortex",
            name: "Plasma Vortex",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#ffaa00', '#00ffff'],
            gravity: true,
            transparent: true,
            additive: true,
            depthWrite: false,
            fragBody: ` float t = u_time * u_speed; float r = rad; float a = ang + t*4.0 + r*15.0; float vortex = sin(a*5.0)*0.5+0.5; float rim = smoothstep(0.7,1.0,r); vec3 c = hsv2rgb(vec3(0.1+r*0.2+t*0.1,1.0,1.0)) * vortex; col = c * (0.3+diff*0.7); col += vec3(1.0,0.5,0.2)*pow(rim,2.0)*2.0; alpha = vortex*0.8+rim; `,
            params: [{
                key: 'speed',
                label: 'Spin',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.05,
                default: 2.0
            }]
        },
        {
            id: "datacorruption",
            name: "Data Corruption",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#00ff00', '#000000'],
            gravity: true,
            transparent: true,
            additive: true,
            depthWrite: false,
            fragBody: ` float t = u_time * u_speed; float block = floor(h*20.0+t*2.0)*0.05; float glitch = step(0.9, snoise(vec3(block, t*5.0, 0.0))); float g2 = step(0.95, fbm(vObjPos*30.0+t*10.0)); float bright = max(glitch,g2); col = vec3(0.2,1.0,0.2) * bright * 2.0; alpha = bright*0.9; `,
            params: []
        },
        {
            id: "oilslick",
            name: "Oil Slick",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#ff00ff', '#00ff88'],
            gravity: true,
            transparent: true,
            depthWrite: false,
            fragBody: ` float t = u_time * u_speed; float film = fbm(vObjPos*2.0 + t*0.3)*0.7; vec3 iri = hsv2rgb(vec3(fract(h*5.0+film), 1.0, 1.0)); col = iri * (0.5+diff*0.5); col += vec3(1.0)*pow(fres,3.0)*1.2; alpha = 0.7+film*0.3; `,
            params: [{
                key: 'speed',
                label: 'Flow',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.05,
                default: 1.0
            }]
        },
        {
            id: "cellularautomata",
            name: "Cellular Automata",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#ff8800', '#ff0088'],
            gravity: true,
            fragBody: ` float t = u_time * u_speed; vec3 cell = floor(vObjPos*5.0) / 5.0; float n = snoise(cell + t*0.2); float state = step(0.3, n) * step(0.7, n); vec3 c1 = hsv2rgb(vec3(0.05, 1.0, 1.0)); vec3 c2 = hsv2rgb(vec3(0.8, 1.0, 1.0)); col = mix(c1, c2, state) * diff; col += vec3(1.0)*pow(fres,4.0)*0.8; `,
            params: [{
                key: 'speed',
                label: 'Evolve',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.05,
                default: 1.8
            }]
        },
        {
            id: "magneticflux",
            name: "Magnetic Flux",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#00ff00', '#0000ff'],
            gravity: true,
            transparent: true,
            additive: true,
            depthWrite: false,
            fragBody: ` float t = u_time * u_speed; float field = sin(rad*10.0 - t*3.0 + h*5.0)*0.5+0.5; float lines = abs(field - floor(field*5.0 + 0.5)/5.0)*10.0; lines = smoothstep(0.8,1.0,lines); col = vec3(0.2,1.0,0.2) * lines * 2.0; col += vec3(0.2,0.5,1.0) * pow(fres,4.0)*1.5; alpha = lines*0.9; `,
            params: [{
                key: 'speed',
                label: 'Flux',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.05,
                default: 1.5
            }]
        },
        {
            id: "spectralmelt",
            name: "Spectral Melt",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#ff0000', '#0000ff'],
            gravity: true,
            fragBody: ` float t = u_time * u_speed; float melt = sin(vObjPos.y*10.0 + t*2.0 + fbm(vObjPos*2.0)*5.0)*0.5+0.5; vec3 hot = vec3(1.0,0.3,0.1) * (1.0-melt) * 2.0; vec3 cold = vec3(0.1,0.2,1.0) * melt * 2.0; col = mix(hot, cold, melt) * diff; col += vec3(1.0)*pow(fres,3.0)*1.5; `,
            params: []
        },
        {
            id: "timedilated",
            name: "Time Dilated",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#00ffff', '#ff00ff'],
            gravity: true,
            transparent: true,
            additive: true,
            depthWrite: false,
            fragBody: ` float t = u_time * u_speed; float distort = sin(t*2.0)*0.5+0.5; float hh = h + distort*0.2*sin(rad*10.0); float stripe = sin(hh*20.0 - t*8.0)*0.5+0.5; stripe = smoothstep(0.3,0.7,stripe); vec3 c1 = hsv2rgb(vec3(0.6+distort*0.3,1.0,1.0)); vec3 c2 = vec3(1.0); col = mix(c1, c2, stripe) * diff; col += vec3(1.0)*pow(fres,4.0)*1.2; alpha = stripe*0.9; `,
            params: [{
                key: 'speed',
                label: 'Warp',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.05,
                default: 2.0
            }]
        },
        {
            id: "holographicnoise",
            name: "Holographic Noise",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#ff8800', '#00ff88'],
            gravity: true,
            fragBody: ` float t = u_time * u_speed; float n = fbm(vObjPos*6.0 + t*0.5); float hologram = sin(n*15.0)*0.5+0.5; vec3 c = hsv2rgb(vec3(fract(h*2.0+t*0.1), 1.0, 1.0)); col = c * hologram * 2.0 * diff; col += vec3(1.0)*pow(fres,4.0)*0.8; `,
            params: [{
                key: 'speed',
                label: 'Shift',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.05,
                default: 1.3
            }]
        },
        {
            id: "crackedenergy",
            name: "Cracked Energy",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#4400ff', '#ffaa00'],
            gravity: true,
            transparent: true,
            additive: true,
            depthWrite: false,
            fragBody: ` float t = u_time * u_speed; float crack = max(0.0, 1.0 - abs(sin(rad*10.0 + t*2.0)*0.5+0.5 - 0.8)*10.0); crack += max(0.0, 1.0 - abs(sin(rad*7.0 - t*1.5 + h*3.0)*0.5+0.5 - 0.8)*10.0); crack = clamp(crack, 0.0, 1.0); col = vec3(0.6,0.2,1.0) * crack * 2.0; col += vec3(1.0,0.5,0.2)*pow(fres,3.0)*1.5; alpha = crack*0.9; `,
            params: [{
                key: 'speed',
                label: 'Charge',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.05,
                default: 2.2
            }]
        },
        {
            id: "warpcore",
            name: "Warp Core",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#00ffff', '#ffffff'],
            gravity: true,
            transparent: true,
            additive: true,
            depthWrite: false,
            fragBody: ` float t = u_time * u_speed; float pulse = sin(rad*5.0 - t*3.0)*0.5+0.5; float intensity = pow(pulse, 4.0) * 3.0; col = vec3(0.2,1.0,1.0) * intensity; col += vec3(1.0)*pow(fres,4.0)*1.5; alpha = intensity*0.8; `,
            params: [{
                key: 'speed',
                label: 'Throttle',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.05,
                default: 2.5
            }]
        },
        {
            id: "chromaticaberration",
            name: "Chromatic Aberration",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#ff0000', '#00ff00'],
            gravity: true,
            transparent: true,
            additive: true,
            depthWrite: false,
            fragBody: ` float t = u_time * u_speed; float hNorm = (h - u_levMin)/(u_levMax - u_levMin); vec3 colR = vec3(1.0,0.2,0.2) * step(0.7, fbm(vObjPos*10.0 + hNorm*0.1 + t*0.1)); vec3 colG = vec3(0.2,1.0,0.2) * step(0.7, fbm(vObjPos*10.0 - hNorm*0.1 + t*0.1)); vec3 colB = vec3(0.2,0.2,1.0) * step(0.7, fbm(vObjPos*10.0 + t*0.1)); col = (colR+colG+colB)*1.5; alpha = max(max(colR.r,colG.g),colB.b)*0.9; `,
            params: [{
                key: 'speed',
                label: 'Fringe',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.05,
                default: 1.0
            }]
        },
        {
            id: "particlestorm",
            name: "Particle Storm",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#ff5500', '#111111'],
            gravity: true,
            transparent: true,
            additive: true,
            depthWrite: false,
            fragBody: ` float t = u_time * u_speed; float p = 0.0; for(int i=0; i<5; i++){ float fi = float(i); p += step(0.85, snoise(vObjPos* (10.0+fi*2.0) + vec3(fi*t*1.5, -fi*t*0.8, 0.0))); } col = vec3(1.0,0.4,0.1) * p * 2.0; alpha = p*0.8; `,
            params: [{
                key: 'speed',
                label: 'Storm',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.05,
                default: 2.3
            }]
        },
        {
            id: "bubbleuniverse",
            name: "Bubble Universe",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#ff00cc', '#00ffcc'],
            gravity: true,
            transparent: true,
            additive: true,
            depthWrite: false,
            fragBody: ` float t = u_time * u_speed; float bubbles = step(0.5, snoise(vObjPos*5.0 + t*0.3)); bubbles *= smoothstep(0.3, 0.5, fbm(vObjPos*3.0)); col = hsv2rgb(vec3(fract(rad*2.0 + t*0.1), 1.0, 1.0)) * bubbles * 2.0; col += vec3(1.0)*pow(fres,4.0)*0.5; alpha = bubbles*0.9; `,
            params: [{
                key: 'speed',
                label: 'Fizz',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.05,
                default: 1.0
            }]
        },
        {
            id: "mandala",
            name: "Mandala",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#ffaa00', '#aa00ff'],
            gravity: true,
            transparent: true,
            additive: true,
            depthWrite: false,
            fragBody: ` float t = u_time * u_speed; float r = rad; float a = ang + t; float symmetry = 6.0; float pattern = sin(a*symmetry + r*10.0)*0.5+0.5; pattern = smoothstep(0.5,0.7,pattern); vec3 c1 = hsv2rgb(vec3(0.1+r*0.2+t*0.05,1.0,1.0)); col = c1 * pattern * 2.0 * diff; col += vec3(1.0)*pow(fres,3.0)*0.6; alpha = pattern*0.9; `,
            params: [{
                key: 'speed',
                label: 'Rotate',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.05,
                default: 1.0
            }]
        },
        {
            id: "vaporwave",
            name: "Vaporwave",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#ff80c0', '#80ff80'],
            gravity: true,
            fragBody: ` float t = u_time * u_speed; float wave = sin(rad*3.0 - t*2.0 + h*5.0)*0.5+0.5; vec3 pastel1 = vec3(1.0, 0.5, 0.7); vec3 pastel2 = vec3(0.5, 1.0, 0.7); col = mix(pastel1, pastel2, wave) * diff; col += vec3(1.0)*pow(fres,2.0)*1.0; `,
            params: [{
                key: 'speed',
                label: 'Chill',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.05,
                default: 0.8
            }]
        },
        {
            id: "cyberpunkgrid",
            name: "Cyberpunk Grid",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#ff00ff', '#00ffff'],
            gravity: true,
            transparent: true,
            additive: true,
            depthWrite: false,
            fragBody: ` float t = u_time * u_speed; vec3 g = fract(vObjPos * 4.0); float grid = min(min(step(0.9,g.x), step(0.9,g.y)), step(0.9,g.z)); grid = smoothstep(0.0,0.1,grid); col = vec3(1.0,0.2,1.0) * grid * 2.0; col += vec3(0.2,1.0,1.0)*pow(fres,5.0)*1.2; alpha = grid*0.9; `,
            params: [{
                key: 'speed',
                label: 'Scan',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.05,
                default: 1.5
            }]
        },
        {
            id: "radiatedglow",
            name: "Radiated Glow",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#00ff44', '#000000'],
            gravity: true,
            transparent: true,
            additive: true,
            depthWrite: false,
            fragBody: ` float t = u_time * u_speed; float glow = exp(-rad * 5.0) * sin(t * 2.0 + h*10.0) * 0.5 + 0.5; glow *= smoothstep(0.0,0.2,glow); col = vec3(0.2,1.0,0.4) * glow * 2.0; alpha = glow*0.8; `,
            params: [{
                key: 'speed',
                label: 'Pulse',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.05,
                default: 1.7
            }]
        },
        {
            id: "wormhole",
            name: "Wormhole",
            category: "Psychedelic & mind-bending",
            kind: "shader",
            swatch: ['#4400ff', '#ff00aa'],
            gravity: true,
            transparent: true,
            additive: true,
            depthWrite: false,
            fragBody: `\`
        float r = rad;
        float a = ang + u_time * u_speed * 3.0 + r * 20.0;
        float tunnel = sin(a * 5.0) * 0.5 + 0.5;
        float rim = smoothstep(0.8, 1.0, r);
        vec3 col1 = hsv2rgb(vec3(0.7 + r * 0.2, 1.0, 1.0));
        vec3 col2 = hsv2rgb(vec3(0.9 + r * 0.3, 1.0, 0.8));
        col = mix(col1, col2, tunnel);
        col += vec3(1.0, 0.2, 0.5) * pow(rim, 2.0) * 2.0;
        alpha = clamp(tunnel * 0.8 + rim, 0.0, 1.0);
      \``
        }
    ],
    "Liquid & organic": [{
            id: "mercury",
            name: "Liquid metal",
            category: "Liquid & organic",
            kind: "shader",
            swatch: ['#eef2f8', '#5a636e'],
            params: [{
                    key: 'tint',
                    label: 'Tint',
                    type: 'color',
                    default: '#cdd4de'
                },
                {
                    key: 'ripple',
                    label: 'Ripple',
                    type: 'range',
                    min: 0,
                    max: 0.6,
                    step: 0.01,
                    default: 0.25
                },
                {
                    key: 'speed',
                    label: 'Flow',
                    type: 'range',
                    min: 0,
                    max: 3,
                    step: 0.05,
                    default: 1
                },
            ],
            fragUniforms: "uniform vec3 u_tint; uniform float u_ripple;",
            fragBody: `
      float w = fbm(vObjPos*8.0 + u_time*u_speed*0.5);
      float w2 = fbm(vObjPos*8.0 + 30.0 - u_time*u_speed*0.4);
      vec3 nn = normalize(N + u_ripple*vec3(w, (w+w2)*0.5, w2));
      vec3 r = reflect(-V, nn);
      float up = clamp(r.y*0.5+0.5, 0.0, 1.0);
      vec3 sky = mix(vec3(0.05,0.06,0.08), vec3(0.92,0.95,1.0), pow(up,1.5));
      float spec = pow(up, 8.0);
      col = sky * u_tint + spec*0.6;
      col += pow(1.0 - max(dot(nn,V),0.0), 3.0) * 0.2;
    `
        },
        {
            id: "jelly",
            name: "Jelly / goo",
            category: "Liquid & organic",
            kind: "shader",
            swatch: ['#ff8fc4', '#a83e78'],
            transparent: false,
            displaceParam: true,
            params: [{
                    key: 'color',
                    label: 'Jelly',
                    type: 'color',
                    default: '#ff7ab8'
                },
                {
                    key: 'color2',
                    label: 'Rim',
                    type: 'color',
                    default: '#fff3a0'
                },
                {
                    key: 'displace',
                    label: 'Wobble',
                    type: 'range',
                    min: 0,
                    max: 0.3,
                    step: 0.005,
                    default: 0.12
                },
                {
                    key: 'speed',
                    label: 'Speed',
                    type: 'range',
                    min: 0,
                    max: 3,
                    step: 0.05,
                    default: 1
                },
            ],
            fragUniforms: "uniform vec3 u_color; uniform vec3 u_color2;",
            fragBody: `
      vec3 sss = u_color * (0.55 + 0.6*diff);
      float rim = pow(1.0 - max(dot(N,V),0.0), 2.0);
      col = mix(sss, u_color2, rim*0.7);
      col += rim*0.3;
    `
        },
        {
            id: "lava",
            name: "Lava / magma",
            category: "Liquid & organic",
            kind: "shader",
            swatch: ['#ffd24a', '#8a1c05'],
            params: [{
                    key: 'intensity',
                    label: 'Heat',
                    type: 'range',
                    min: 0.5,
                    max: 2.2,
                    step: 0.05,
                    default: 1.2
                },
                {
                    key: 'scale',
                    label: 'Scale',
                    type: 'range',
                    min: 1,
                    max: 8,
                    step: 0.1,
                    default: 3
                },
                {
                    key: 'speed',
                    label: 'Flow',
                    type: 'range',
                    min: 0,
                    max: 3,
                    step: 0.05,
                    default: 1
                },
            ],
            fragUniforms: "uniform float u_intensity; uniform float u_scale;",
            fragBody: `
      vec3 q = vObjPos*u_scale;
      float n = fbm(q + vec3(0.0, -u_time*u_speed*0.4, 0.0));
      float n2 = fbm(q*2.0 + vec3(u_time*u_speed*0.3, 0.0, 0.0));
      float h = (n*0.65 + n2*0.35)*0.5 + 0.5;
      vec3 cool = vec3(0.11,0.02,0.01);
      vec3 mid  = vec3(0.85,0.18,0.03);
      vec3 hot  = vec3(1.0,0.85,0.35);
      col = mix(cool, mid, smoothstep(0.35,0.6,h));
      col = mix(col, hot, smoothstep(0.62,0.86,h));
      col *= u_intensity;
    `
        }
    ],
    "Sci-fi FX": [{
            id: "shield",
            name: "Energy shield",
            category: "Sci-fi FX",
            kind: "shader",
            swatch: ['#5effc8', '#1c9a70'],
            transparent: true,
            additive: true,
            depthWrite: false,
            params: [{
                    key: 'color',
                    label: 'Colour',
                    type: 'color',
                    default: '#5effc8'
                },
                {
                    key: 'power',
                    label: 'Edge',
                    type: 'range',
                    min: 0.8,
                    max: 4,
                    step: 0.05,
                    default: 2
                },
                {
                    key: 'opacity',
                    label: 'Opacity',
                    type: 'range',
                    min: 0,
                    max: 1,
                    step: 0.01,
                    default: 0.8
                },
                {
                    key: 'speed',
                    label: 'Pulse',
                    type: 'range',
                    min: 0,
                    max: 3,
                    step: 0.05,
                    default: 1
                },
            ],
            fragUniforms: "uniform vec3 u_color; uniform float u_power; uniform float u_opacity;",
            fragBody: `
      float fp = pow(fres, u_power);
      float bands = 0.5 + 0.5*sin(vWorldPos.y*20.0 - u_time*3.0*u_speed);
      float pulse = 0.6 + 0.4*sin(u_time*2.0*u_speed);
      col = u_color * (fp*1.5 + 0.15) + u_color*bands*fp*0.4;
      alpha = clamp(fp*pulse, 0.0, 1.0) * u_opacity + 0.04;
    `
        },
        {
            id: "plasma",
            name: "Plasma / electric",
            category: "Sci-fi FX",
            kind: "shader",
            swatch: ['#c08bff', '#2a1258'],
            params: [{
                    key: 'color',
                    label: 'Core',
                    type: 'color',
                    default: '#2a1258'
                },
                {
                    key: 'color2',
                    label: 'Arc',
                    type: 'color',
                    default: '#c08bff'
                },
                {
                    key: 'scale',
                    label: 'Scale',
                    type: 'range',
                    min: 1,
                    max: 8,
                    step: 0.1,
                    default: 3
                },
                {
                    key: 'speed',
                    label: 'Speed',
                    type: 'range',
                    min: 0,
                    max: 3,
                    step: 0.05,
                    default: 1
                },
            ],
            fragUniforms: "uniform vec3 u_color; uniform vec3 u_color2; uniform float u_scale;",
            fragBody: `
      vec3 q = vObjPos*u_scale;
      float n = fbm(q*2.0 + u_time*u_speed*0.6);
      float v = abs(sin(n*6.2831 + u_time*u_speed*3.0));
      float bolt = pow(1.0 - v, 8.0);
      col = mix(u_color*0.15, u_color2*2.2, bolt) + bolt*0.6;
    `
        },
        {
            id: "xray",
            name: "X-ray",
            category: "Sci-fi FX",
            kind: "shader",
            swatch: ['#9adfff', '#155a80'],
            transparent: true,
            additive: true,
            depthWrite: false,
            params: [{
                    key: 'color',
                    label: 'Colour',
                    type: 'color',
                    default: '#7ad0ff'
                },
                {
                    key: 'power',
                    label: 'Falloff',
                    type: 'range',
                    min: 0.5,
                    max: 3,
                    step: 0.05,
                    default: 1.4
                },
                {
                    key: 'opacity',
                    label: 'Opacity',
                    type: 'range',
                    min: 0,
                    max: 1,
                    step: 0.01,
                    default: 0.7
                },
            ],
            fragUniforms: "uniform vec3 u_color; uniform float u_power; uniform float u_opacity;",
            fragBody: `
      float fp = pow(fres, u_power);
      col = u_color * fp * 2.2;
      alpha = clamp(fp, 0.0, 1.0) * u_opacity;
    `
        },
        {
            id: "neonwire",
            name: "Neon wireframe",
            category: "Sci-fi FX",
            kind: "shader",
            swatch: ['#5effd0', '#12403a'],
            transparent: true,
            additive: true,
            depthWrite: false,
            wireframe: true,
            params: [{
                key: 'color',
                label: 'Wire',
                type: 'color',
                default: '#5effd0'
            }, ],
            fragUniforms: "uniform vec3 u_color;",
            fragBody: `
      col = u_color * (1.2 + fres);
    `
        },
        {
            id: "dissolve",
            name: "Dissolve burn",
            category: "Sci-fi FX",
            kind: "shader",
            swatch: ['#ff6a2a', '#cfcfcf'],
            params: [{
                    key: 'color',
                    label: 'Surface',
                    type: 'color',
                    default: '#c9c9c9'
                },
                {
                    key: 'glow',
                    label: 'Ember',
                    type: 'color',
                    default: '#ff6a2a'
                },
                {
                    key: 'scale',
                    label: 'Grain',
                    type: 'range',
                    min: 1,
                    max: 12,
                    step: 0.1,
                    default: 4
                },
                {
                    key: 'edge',
                    label: 'Reveal',
                    type: 'range',
                    min: 0,
                    max: 1,
                    step: 0.01,
                    default: 0.35
                },
                {
                    key: 'width',
                    label: 'Ember width',
                    type: 'range',
                    min: 0.01,
                    max: 0.3,
                    step: 0.005,
                    default: 0.08
                },
            ],
            fragUniforms: "uniform vec3 u_color; uniform vec3 u_glow; uniform float u_scale; uniform float u_edge; uniform float u_width;",
            fragBody: `
      float n = fbm(vObjPos*u_scale)*0.5 + 0.5;
      if (n < u_edge) discard;
      float e = smoothstep(u_edge, u_edge + u_width, n);
      col = mix(u_glow*2.6, u_color*diff, e);
    `
        }
    ],
    "Potions & liquids": [{
            id: "poison",
            name: "Poison vial",
            category: "Potions & liquids",
            kind: "shader",
            swatch: ['#7dff3a', '#123c05'],
            transparent: true,
            depthWrite: false,
            gravity: true,
            params: [{
                    key: 'color',
                    label: 'Venom',
                    type: 'color',
                    default: '#4fdc1e'
                },
                {
                    key: 'glow',
                    label: 'Fume',
                    type: 'color',
                    default: '#c8ff5a'
                },
                {
                    key: 'fill',
                    label: 'Fill',
                    type: 'range',
                    min: 0.02,
                    max: 0.98,
                    step: 0.01,
                    default: 0.55
                },
                {
                    key: 'speed',
                    label: 'Boil',
                    type: 'range',
                    min: 0,
                    max: 3,
                    step: 0.05,
                    default: 1
                },
            ],
            fragUniforms: "uniform vec3 u_color; uniform vec3 u_glow;",
            fragBody: `LIQ + \`
      if (dSurf > 0.0) {\` + GLASS_ABOVE + \`} else {
        float murk = fbm(vObjPos * 3.0 - upA * (u_time * u_speed * 0.4));
        vec3 body = mix(u_color * 0.14, u_color, clamp(0.35 + murk * 0.5, 0.0, 1.0));
        float bub = snoise(vObjPos * 7.0 - upA * (u_time * u_speed * 1.1));
        float dots = smoothstep(0.55, 0.72, bub);
        col = body * (0.6 + 0.5 * diff);
        col += u_glow * dots * 1.1;
        col += u_glow * menis * 1.8;
        col += u_glow * pow(fres, 2.5) * 0.6;
        alpha = 0.94;
      }
    \``
        },
        {
            id: "lovepotion",
            name: "Love potion",
            category: "Potions & liquids",
            kind: "shader",
            swatch: ['#ff9ad2', '#b0125f'],
            transparent: true,
            depthWrite: false,
            gravity: true,
            params: [{
                    key: 'color',
                    label: 'Blush',
                    type: 'color',
                    default: '#ff5aa8'
                },
                {
                    key: 'color2',
                    label: 'Swirl',
                    type: 'color',
                    default: '#ffc4e8'
                },
                {
                    key: 'fill',
                    label: 'Fill',
                    type: 'range',
                    min: 0.02,
                    max: 0.98,
                    step: 0.01,
                    default: 0.7
                },
                {
                    key: 'speed',
                    label: 'Swirl',
                    type: 'range',
                    min: 0,
                    max: 3,
                    step: 0.05,
                    default: 1
                },
            ],
            fragUniforms: "uniform vec3 u_color; uniform vec3 u_color2;",
            fragBody: `LIQ + \`
      if (dSurf > 0.0) {\` + GLASS_ABOVE + \`} else {
        float swirl = fbm(vObjPos * 2.5 + vec3(u_time * u_speed * 0.22));
        vec3 body = mix(u_color, u_color2, 0.5 + 0.5 * swirl) * (0.85 - depth * 0.45);
        float gl = pow(max(snoise(vObjPos * 26.0 + upA * (u_time * u_speed)), 0.0), 10.0);
        col = body * (0.7 + 0.4 * diff);
        col += vec3(1.0, 0.92, 0.97) * gl * 2.2;
        col += u_color2 * menis * 2.0;
        col += u_color2 * pow(fres, 3.0) * 0.5;
        alpha = 0.92;
      }
    \``
        },
        {
            id: "galaxyjar",
            name: "Bottled galaxy",
            category: "Potions & liquids",
            kind: "shader",
            swatch: ['#7a8dff', '#12083a'],
            transparent: true,
            depthWrite: false,
            gravity: true,
            params: [{
                    key: 'color',
                    label: 'Glass',
                    type: 'color',
                    default: '#8a9aff'
                },
                {
                    key: 'fill',
                    label: 'Fill',
                    type: 'range',
                    min: 0.02,
                    max: 0.98,
                    step: 0.01,
                    default: 0.8
                },
                {
                    key: 'scale',
                    label: 'Nebula',
                    type: 'range',
                    min: 1,
                    max: 8,
                    step: 0.1,
                    default: 3
                },
                {
                    key: 'speed',
                    label: 'Drift',
                    type: 'range',
                    min: 0,
                    max: 3,
                    step: 0.05,
                    default: 1
                },
            ],
            fragUniforms: "uniform vec3 u_color; uniform float u_scale;",
            fragBody: `LIQ + \`
      if (dSurf > 0.0) {\` + GLASS_ABOVE + \`} else {
        float neb = fbm(vObjPos * u_scale + vec3(0.0, u_time * u_speed * 0.06, 0.0));
        vec3 nebc = hsv2rgb(vec3(fract(0.68 + neb * 0.22), 0.75, clamp(0.22 + neb * 0.6, 0.0, 1.0)));
        float star = pow(max(snoise(vObjPos * 40.0), 0.0), 14.0);
        col = nebc + vec3(star) * 2.2;
        col += nebc * menis * 2.5;
        col += vec3(0.6, 0.7, 1.0) * pow(fres, 3.0) * 0.4;
        alpha = 0.96;
      }
    \``
        },
        {
            id: "lavalamp",
            name: "Lava lamp",
            category: "Potions & liquids",
            kind: "shader",
            swatch: ['#ff7a3a', '#4a0a5e'],
            transparent: true,
            depthWrite: false,
            gravity: true,
            params: [{
                    key: 'color',
                    label: 'Fluid',
                    type: 'color',
                    default: '#5a1470'
                },
                {
                    key: 'color2',
                    label: 'Blob',
                    type: 'color',
                    default: '#ff6a2a'
                },
                {
                    key: 'fill',
                    label: 'Fill',
                    type: 'range',
                    min: 0.02,
                    max: 0.98,
                    step: 0.01,
                    default: 0.9
                },
                {
                    key: 'speed',
                    label: 'Rise',
                    type: 'range',
                    min: 0,
                    max: 3,
                    step: 0.05,
                    default: 1
                },
            ],
            fragUniforms: "uniform vec3 u_color; uniform vec3 u_color2;",
            fragBody: `LIQ + \`
      if (dSurf > 0.0) {\` + GLASS_ABOVE + \`} else {
        vec3 q = pPerp * (2.6 / E) + upA * (h * 1.6 / E - u_time * u_speed * 0.5);
        float blob = snoise(q + vec3(7.7)) * 0.6 + snoise(q * 2.1 - vec3(3.1)) * 0.4;
        float bm = smoothstep(0.16, 0.42, blob);
        vec3 body = mix(u_color * 0.6, u_color2 * 1.5, bm);
        col = body * (0.55 + 0.5 * diff);
        col += u_color2 * pow(depth, 1.7) * 0.8;
        col += u_color2 * menis * 1.4;
        col += u_color2 * pow(fres, 3.0) * 0.4;
        alpha = 0.95;
      }
    \``
        },
        {
            id: "melting",
            name: "Melting / drip",
            category: "Potions & liquids",
            kind: "shader",
            swatch: ['#ffd9a0', '#a85a1e'],
            gravity: true,
            params: [{
                    key: 'color',
                    label: 'Wax',
                    type: 'color',
                    default: '#f2b56a'
                },
                {
                    key: 'sag',
                    label: 'Melt',
                    type: 'range',
                    min: 0,
                    max: 0.45,
                    step: 0.005,
                    default: 0.2
                },
                {
                    key: 'speed',
                    label: 'Ooze',
                    type: 'range',
                    min: 0,
                    max: 3,
                    step: 0.05,
                    default: 0.6
                },
            ],
            fragUniforms: "uniform vec3 u_color;",
            fragBody: `\`
      float streak = fbm(pPerp * (6.0 / E) + upA * (h * 3.0 / E));
      vec3 base = u_color * (0.35 + 0.65 * diff);
      col = mix(base, u_color * 1.25, smoothstep(0.2, 0.8, streak) * 0.35);
      col += vec3(1.0) * pow(max(dot(reflect(-V, N), KL), 0.0), 24.0) * 0.7;
      col += u_color * pow(fres, 3.0) * 0.4;
    \``
        },
        {
            id: "acidbath",
            name: "Acid bath",
            category: "Potions & liquids",
            kind: "shader",
            swatch: ['#ccff00', '#1a2200'],
            transparent: true,
            depthWrite: false,
            gravity: true,
            params: [{
                    key: 'color',
                    label: 'Acid',
                    type: 'color',
                    default: '#a5ff00'
                },
                {
                    key: 'fill',
                    label: 'Fill',
                    type: 'range',
                    min: 0.02,
                    max: 0.98,
                    step: 0.01,
                    default: 0.6
                },
                {
                    key: 'speed',
                    label: 'Boil',
                    type: 'range',
                    min: 0,
                    max: 4,
                    step: 0.05,
                    default: 1.5
                },
            ],
            fragUniforms: "uniform vec3 u_color;",
            fragBody: `LIQ + \`
      if (dSurf > 0.0) {\` + GLASS_ABOVE + \`} else {
        float t = u_time * u_speed;
        float boil = fbm(vObjPos * 10.0 + upA * (t * 3.0));
        vec3 body = mix(vec3(0.2, 0.4, 0.0), u_color, boil);
        col = body * (0.5 + diff * 0.8);
        col += vec3(1.0, 1.0, 0.5) * pow(boil, 6.0) * 5.0; // Blinding boiling glints
        col += u_color * menis * 4.0;
        col += u_color * pow(fres, 2.0) * 1.5;
        alpha = 0.92;
      }
    \``
        },
        {
            id: "cthulhublood",
            name: "Cthulhu blood",
            category: "Potions & liquids",
            kind: "shader",
            swatch: ['#6a00b0', '#1a0022'],
            transparent: true,
            depthWrite: false,
            gravity: true,
            params: [{
                    key: 'color',
                    label: 'Ichor',
                    type: 'color',
                    default: '#8000ff'
                },
                {
                    key: 'fill',
                    label: 'Fill',
                    type: 'range',
                    min: 0.02,
                    max: 0.98,
                    step: 0.01,
                    default: 0.7
                },
                {
                    key: 'speed',
                    label: 'Throb',
                    type: 'range',
                    min: 0,
                    max: 3,
                    step: 0.05,
                    default: 1
                },
            ],
            fragUniforms: "uniform vec3 u_color;",
            fragBody: `LIQ + \`
      if (dSurf > 0.0) {\` + GLASS_ABOVE + \`} else {
        float t = u_time * u_speed;
        float ripple = fbm(vObjPos * 15.0 + upA * (t * 1.5));
        vec3 body = mix(vec3(0.1, 0.0, 0.1), u_color, clamp(0.5 + ripple, 0.0, 1.0));
        col = body * (0.4 + diff * 0.6);
        col += vec3(1.0, 0.2, 0.8) * pow(fres, 3.0) * 1.2;
        col += u_color * menis * 3.0;
        alpha = 1.0;
      }
    \``
        }
    ],
    "Candy & crystal": [{
            id: "soapbubble",
            name: "Soap bubble",
            category: "Candy & crystal",
            kind: "physical",
            swatch: ['#d0f0ff', '#c39ae0'],
            params: [{
                    key: 'iridescence',
                    label: 'Film',
                    type: 'range',
                    min: 0,
                    max: 1,
                    step: 0.01,
                    default: 1,
                    apply: (m, v) => {
                        m.iridescence = v;
                    }
                },
                {
                    key: 'ior',
                    label: 'Thin-ness',
                    type: 'range',
                    min: 1,
                    max: 1.6,
                    step: 0.01,
                    default: 1.1,
                    apply: (m, v) => {
                        m.ior = v;
                    }
                },
            ],
            make: `() => new THREE.MeshPhysicalMaterial({
            transmission: 1,
            thickness: 0.05,
            roughness: 0.02,
            metalness: 0,
            ior: 1.1,
            iridescence: 1,
            iridescenceIOR: 1.3,
            transparent: true,
            side: THREE.DoubleSide,
            envMapIntensity: 1.3
        })`
        },
        {
            id: "lollipop",
            name: "Lollipop swirl",
            category: "Candy & crystal",
            kind: "shader",
            swatch: ['#ff4a4a', '#fff3e0'],
            gravity: true,
            params: [{
                    key: 'color',
                    label: 'Stripe A',
                    type: 'color',
                    default: '#ff3a3a'
                },
                {
                    key: 'color2',
                    label: 'Stripe B',
                    type: 'color',
                    default: '#fff2e2'
                },
                {
                    key: 'arms',
                    label: 'Stripes',
                    type: 'range',
                    min: 2,
                    max: 12,
                    step: 1,
                    default: 4
                },
                {
                    key: 'twist',
                    label: 'Twist',
                    type: 'range',
                    min: 0,
                    max: 4,
                    step: 0.1,
                    default: 1.4
                },
            ],
            fragUniforms: "uniform vec3 u_color; uniform vec3 u_color2; uniform float u_arms; uniform float u_twist;",
            fragBody: `\`
      float band = sin(ang * u_arms + hn * u_twist * 6.2831);
      vec3 base = mix(u_color, u_color2, smoothstep(-0.2, 0.2, band));
      col = base * (0.35 + 0.75 * diff);
      col += vec3(1.0) * pow(max(dot(reflect(-V, N), KL), 0.0), 60.0) * 1.2;
      col += base * pow(fres, 3.0) * 0.5;
    \``
        },
        {
            id: "disco",
            name: "Disco ball",
            category: "Candy & crystal",
            kind: "shader",
            swatch: ['#f0f4ff', '#5a5e8a'],
            params: [{
                    key: 'facets',
                    label: 'Facets',
                    type: 'range',
                    min: 4,
                    max: 24,
                    step: 1,
                    default: 10
                },
                {
                    key: 'speed',
                    label: 'Party',
                    type: 'range',
                    min: 0,
                    max: 3,
                    step: 0.05,
                    default: 1
                },
            ],
            fragUniforms: "uniform float u_facets;",
            fragBody: `
      vec3 qn = normalize((floor(N * u_facets) + 0.5) / u_facets);
      float gap = smoothstep(0.25, 0.55, length(N - qn) * u_facets * 0.5);
      vec3 r = reflect(-V, qn);
      float up = clamp(r.y * 0.5 + 0.5, 0.0, 1.0);
      vec3 sky = mix(vec3(0.06, 0.07, 0.1), vec3(1.0), pow(up, 3.0));
      float spark = pow(max(dot(r, KL), 0.0), 90.0) * 4.0;
      float hueSpark = pow(max(dot(r, normalize(vec3(-0.4, 0.7, 0.3))), 0.0), 80.0);
      col = sky * 0.8 + vec3(spark) + hsv2rgb(vec3(fract(u_time * 0.15 * u_speed), 0.7, 1.0)) * hueSpark * 2.0;
      col *= 1.0 - gap * 0.6;
    `
        },
        {
            id: "ice",
            name: "Glacier ice",
            category: "Candy & crystal",
            kind: "shader",
            swatch: ['#d8f4ff', '#1a5e8a'],
            params: [{
                key: 'scale',
                label: 'Cracks',
                type: 'range',
                min: 1,
                max: 12,
                step: 0.1,
                default: 5
            }, ],
            fragUniforms: "uniform float u_scale;",
            fragBody: `
      float crack = pow(1.0 - abs(snoise(vObjPos * u_scale)), 11.0);
      float glint = pow(max(snoise(vObjPos * 30.0), 0.0), 9.0);
      col = mix(vec3(0.10, 0.28, 0.42), vec3(0.85, 0.95, 1.0), clamp(0.35 + 0.35 * diff + pow(fres, 2.0) * 0.6, 0.0, 1.0));
      col += vec3(1.0) * crack * 0.5 + vec3(1.0) * glint * 1.4;
    `
        },
        {
            id: "gemstone",
            name: "Faceted gem",
            category: "Candy & crystal",
            kind: "shader",
            swatch: ['#4affd2', '#0a4a6e'],
            params: [{
                    key: 'color',
                    label: 'Stone',
                    type: 'color',
                    default: '#18c8a8'
                },
                {
                    key: 'fire',
                    label: 'Fire',
                    type: 'range',
                    min: 0,
                    max: 1.5,
                    step: 0.02,
                    default: 0.8
                },
                {
                    key: 'speed',
                    label: 'Shimmer',
                    type: 'range',
                    min: 0,
                    max: 3,
                    step: 0.05,
                    default: 1
                },
            ],
            fragUniforms: "uniform vec3 u_color; uniform float u_fire;",
            fragBody: `
      vec3 fN = normalize(cross(dFdx(vWorldPos), dFdy(vWorldPos)));
      if (dot(fN, V) < 0.0) fN = -fN;
      float fd = max(dot(fN, KL), 0.0);
      float fr = pow(1.0 - max(dot(fN, V), 0.0), 2.0);
      vec3 disp = hsv2rgb(vec3(fract(snoise(fN * 2.7) * 0.5 + u_time * u_speed * 0.02), 0.7, 1.0));
      float spec = pow(max(dot(reflect(-V, fN), KL), 0.0), 50.0);
      col = u_color * (0.25 + 0.75 * fd) + disp * fr * u_fire + vec3(spec) * 1.6;
    `
        },
        {
            id: "tesseract",
            name: "Quantum tesseract",
            category: "Candy & crystal",
            kind: "shader",
            swatch: ['#00ffff', '#000033'],
            transparent: true,
            depthWrite: false,
            additive: true,
            params: [{
                key: 'speed',
                label: 'Shift',
                type: 'range',
                min: 0,
                max: 3,
                step: 0.05,
                default: 1
            }, ],
            fragBody: `
      float t = u_time * u_speed;
      vec3 fN = normalize(floor(N * 4.0) / 4.0);
      float fd = max(dot(fN, KL), 0.0);
      float fr = pow(1.0 - max(dot(fN, V), 0.0), 3.0);
      float grid = smoothstep(0.9, 1.0, sin(vObjPos.x * 15.0 + t) * sin(vObjPos.y * 15.0 + t) * sin(vObjPos.z * 15.0 + t));
      vec3 base = hsv2rgb(vec3(fract(t * 0.05), 0.8, 0.1 + fd * 0.2));
      col = base + vec3(fr) * 0.8;
      col += vec3(1.0, 0.5, 1.0) * grid * 3.0; // Pulsing hyper-dimensional intersections
      alpha = clamp(0.2 + fr + grid, 0.0, 1.0);
    `
        }
    ],
    "Chaos & Eldritch": [{
        id: "meatgrinder",
        name: "Raw meat",
        category: "Chaos & Eldritch",
        kind: "shader",
        swatch: ['#aa0000', '#330000'],
        params: [{
            key: 'speed',
            label: 'Pulse',
            type: 'range',
            min: 0,
            max: 3,
            step: 0.05,
            default: 1
        }, ],
        fragBody: `
      float t = u_time * u_speed;
      float sinew = fbm(vObjPos * 8.0 + t * 0.2);
      float grain = abs(snoise(vObjPos * 20.0));
      vec3 flesh = mix(vec3(0.3, 0.0, 0.0), vec3(0.8, 0.2, 0.2), sinew);
      col = flesh * (0.3 + diff * 0.7);
      col += vec3(1.0, 0.8, 0.8) * pow(grain, 8.0) * (0.5 + 0.5 * sin(t * 5.0)); // Pulsing fibers
      col += vec3(0.5, 0.0, 0.0) * pow(fres, 2.0) * 0.8;
    `
    }],
    "Biomimetic & organic": [{
            id: "turing",
            name: "Turing Patterns",
            category: "Biomimetic & organic",
            kind: "shader",
            swatch: ['#ff6a9a', '#1a0a3a'],
            gravity: true,
            params: [{
                    key: 'feed',
                    label: 'Feed Rate',
                    type: 'range',
                    min: 0.01,
                    max: 0.12,
                    step: 0.001,
                    default: 0.055
                },
                {
                    key: 'kill',
                    label: 'Kill Rate',
                    type: 'range',
                    min: 0.03,
                    max: 0.18,
                    step: 0.001,
                    default: 0.062
                },
                {
                    key: 'iter',
                    label: 'Iterations',
                    type: 'range',
                    min: 1,
                    max: 6,
                    step: 1,
                    default: 4
                },
                {
                    key: 'scaleRd',
                    label: 'Detail',
                    type: 'range',
                    min: 2,
                    max: 15,
                    step: 0.5,
                    default: 6
                },
                {
                    key: 'speed',
                    label: 'Morph',
                    type: 'range',
                    min: 0,
                    max: 2,
                    step: 0.05,
                    default: 0.5
                },
            ],
            fragUniforms: "uniform float u_feed; uniform float u_kill; uniform float u_iter; uniform float u_scaleRd;",
            fragBody: `\`
      // Reaction-diffusion (Gray-Scott style) approximated via iterative fbm
      // u_feed = F, u_kill = K — the classic Turing parameter space
      float t = u_time * u_speed;
      vec3 q = vObjPos * u_scaleRd;
      float u = fbm(q + vec3(t * 0.1, 0.0, 0.0));
      float v = fbm(q + vec3(0.0, 0.0, t * 0.1) + 3.7);
      // Crude iterative Laplacian-like blending
      for (int i = 0; i < 6; i++) {
        if (float(i) >= u_iter) break;
        float fi = float(i) * 0.15;
        float lapU = fbm(q * (1.0 + fi * 0.5) + vec3(t * 0.08 * fi, 0.0, 0.0));
        float lapV = fbm(q * (1.0 + fi * 0.5) + vec3(0.0, 0.0, t * 0.08 * fi) + 5.1);
        float uvv = u * v * v;
        u += (u_feed * (1.0 - u) - uvv + 0.012 * lapU) * 0.3;
        v += (uvv - (u_feed + u_kill) * v + 0.012 * lapV) * 0.3;
      }
      float spot = clamp(u * 0.7 + v * 0.3, 0.0, 1.0);
      float stripe = clamp(abs(u - v), 0.0, 1.0);
      float pattern = mix(spot, stripe, 0.45);
      // Map to rich organic palette
      vec3 lo = vec3(0.06, 0.01, 0.08);
      vec3 hiA = hsv2rgb(vec3(0.92, 0.9, 1.0));  // hot pink
      vec3 hiB = hsv2rgb(vec3(0.15, 0.9, 1.0));  // gold
      vec3 hi = mix(hiA, hiB, smoothstep(0.3, 0.7, pattern));
      col = mix(lo, hi, pattern) * (0.55 + diff * 0.6);
      col += hi * pow(fres, 2.5) * 0.9;
    \``
        },
        {
            id: "bioluminescent",
            name: "Bioluminescent Abyss",
            category: "Biomimetic & organic",
            kind: "shader",
            swatch: ['#001a2a', '#3affd8'],
            gravity: true,
            transparent: true,
            additive: true,
            depthWrite: false,
            params: [{
                    key: 'baseDark',
                    label: 'Abyss',
                    type: 'color',
                    default: '#00101e'
                },
                {
                    key: 'glowA',
                    label: 'Glow A',
                    type: 'color',
                    default: '#2effc8'
                },
                {
                    key: 'glowB',
                    label: 'Glow B',
                    type: 'color',
                    default: '#ff3a8a'
                },
                {
                    key: 'density',
                    label: 'Spot Density',
                    type: 'range',
                    min: 2,
                    max: 18,
                    step: 0.5,
                    default: 7
                },
                {
                    key: 'pulseSpd',
                    label: 'Pulse Speed',
                    type: 'range',
                    min: 0,
                    max: 4,
                    step: 0.05,
                    default: 1.5
                },
                {
                    key: 'flicker',
                    label: 'Flicker',
                    type: 'range',
                    min: 0,
                    max: 1,
                    step: 0.01,
                    default: 0.6
                },
            ],
            fragUniforms: "uniform vec3 u_baseDark; uniform vec3 u_glowA; uniform vec3 u_glowB; uniform float u_density; uniform float u_pulseSpd; uniform float u_flicker;",
            fragBody: `\`
      float t = u_time * u_pulseSpd;
      // Organic spot field — scattered bioluminescent colonies
      float n1 = snoise(vObjPos * u_density + vec3(0.0, t * 0.3, 0.0));
      float n2 = snoise(vObjPos * u_density * 1.7 + vec3(t * 0.2, 0.0, 0.0) + 3.7);
      float n3 = snoise(vObjPos * u_density * 2.3 + vec3(0.0, 0.0, t * 0.25) + 7.1);
      // Multi-scale spot detection
      float spot = max(max(
        smoothstep(0.72, 0.88, n1),
        smoothstep(0.75, 0.90, n2)),
        smoothstep(0.78, 0.92, n3)
      );
      // Each spot pulses independently via secondary noise
      float pulsePhase = snoise(vObjPos * 3.0 + vec3(t * 0.7));
      float pulse = 0.55 + 0.45 * sin(t * 2.5 + pulsePhase * 8.0);
      // Flicker: random dropouts
      float flick = 1.0 - u_flicker * step(0.88, snoise(vObjPos * 45.0 + t * 30.0));
      float glow = spot * pulse * flick;
      // Depth haze: spots fade in the depths
      float depthAtten = 1.0 - pow(clamp(-dSurf / E, 0.0, 1.0) * 0.6, 1.5);
      // Blend between two bioluminescent colors
      float colorMix = snoise(vObjPos * 2.0 + vec3(0.0, 0.0, t * 0.1));
      vec3 glowCol = mix(u_glowA, u_glowB, smoothstep(-0.3, 0.3, colorMix));
      // Compose: dark base + glowing spots
      col = u_baseDark * (0.15 + diff * 0.3);
      col += glowCol * glow * depthAtten * 3.5;
      col += glowCol * pow(fres, 3.5) * 0.6;
      alpha = clamp(0.15 + glow * depthAtten * 0.85, 0.0, 1.0);
    \``
        }
    ],
    "Synthwave & retro": [{
        id: "vhsglitch",
        name: "VHS Tracking",
        category: "Synthwave & retro",
        kind: "shader",
        swatch: ['#ff55aa', '#111111'],
        gravity: true,
        params: [{
                key: 'trackErr',
                label: 'Tracking Error',
                type: 'range',
                min: 0,
                max: 1,
                step: 0.01,
                default: 0.5
            },
            {
                key: 'staticA',
                label: 'Static Amount',
                type: 'range',
                min: 0,
                max: 1,
                step: 0.01,
                default: 0.35
            },
            {
                key: 'colorBleed',
                label: 'Color Bleed',
                type: 'range',
                min: 0,
                max: 1,
                step: 0.01,
                default: 0.6
            },
            {
                key: 'scanlines',
                label: 'Scanlines',
                type: 'range',
                min: 0,
                max: 1,
                step: 0.01,
                default: 0.8
            },
            {
                key: 'speed',
                label: 'Jitter',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.05,
                default: 1.8
            },
        ],
        fragUniforms: "uniform float u_trackErr; uniform float u_staticA; uniform float u_colorBleed; uniform float u_scanlines;",
        fragBody: `\`
      float t = u_time * u_speed;
      // VHS: scanlines, tracking tearing, chroma bleed, static
      float scanline = sin(hn * 400.0) * 0.5 + 0.5;
      float scanMask = 0.75 + 0.25 * scanline * u_scanlines;
      // Tracking: horizontal bands of corruption
      float track = snoise(vec3(0.0, hn * 8.0 + t * 0.9, t * 4.0));
      float tear = smoothstep(0.82, 0.95, track) * u_trackErr;
      // Color channel displacement (chroma bleed)
      float rOff = u_colorBleed * 0.025 * snoise(vec3(hn * 30.0, t * 12.0, 0.0));
      float bOff = u_colorBleed * 0.025 * snoise(vec3(hn * 30.0, t * 12.0, 3.3));
      // Sample "image" via fbm (simulates recorded content)
      vec3 pR = vObjPos + upA * rOff;
      vec3 pB = vObjPos + upA * bOff;
      float imgR = fbm(pR * 3.0 + vec3(t * 0.15, 0.0, 0.0));
      float imgG = fbm(vObjPos * 3.0 + vec3(0.0, t * 0.15, 0.0));
      float imgB = fbm(pB * 3.0 + vec3(0.0, 0.0, t * 0.15));
      vec3 img = vec3(imgR, imgG, imgB) * 0.5 + 0.5;
      // Static snow
      float snow = snoise(vObjPos * 60.0 + t * 55.0) * 0.5 + 0.5;
      snow = smoothstep(0.65, 0.8, snow) * u_staticA;
      // Color-shifted fringe on tears
      vec3 tearCol = vec3(1.0, 0.1, 0.3) * tear;
      // Compose
      col = img * scanMask * (0.5 + diff * 0.5);
      col += vec3(snow);
      col += tearCol;
      col += vec3(0.8, 0.2, 0.6) * pow(fres, 4.0) * 0.4;
    \``
    }],
    "Textiles": [{
        id: "woven",
        name: "Woven Fabric",
        category: "Textiles",
        kind: "shader",
        swatch: ["#5d4037", "#8d6e63"],
        transparent: false,
        additive: false,
        depthWrite: true,
        params: [{
                key: "thread1Color",
                label: "Thread Color 1",
                type: "color",
                default: "#a0522d"
            },
            {
                key: "thread2Color",
                label: "Thread Color 2",
                type: "color",
                default: "#deb887"
            },
            {
                key: "weaveScale",
                label: "Weave Scale",
                type: "float",
                default: 8.0,
                min: 2.0,
                max: 20.0
            },
            {
                key: "fuzziness",
                label: "Fuzziness",
                type: "float",
                default: 0.3,
                min: 0.0,
                max: 1.0
            },
            {
                key: "specular",
                label: "Specular",
                type: "float",
                default: 0.6,
                min: 0.0,
                max: 1.5
            },
            {
                key: "twist",
                label: "Twist",
                type: "float",
                default: 0.2,
                min: 0.0,
                max: 1.0
            }
        ],
        fragUniforms: `
      uniform vec3 u_thread1Color;
      uniform vec3 u_thread2Color;
      uniform float u_weaveScale;
      uniform float u_fuzziness;
      uniform float u_specular;
      uniform float u_twist;
    `,
        fragBody: `
      vec2 uv = vWorldPos.xy * u_weaveScale;
      float patternX = abs(sin(uv.x * 3.14159 + uv.y * u_twist)) * 0.5 + 0.5;
      float patternY = abs(sin(uv.y * 3.14159 + uv.x * u_twist)) * 0.5 + 0.5;
      float weave = (patternX * patternY) * 0.8 + 0.2;
      float fuzzy = fbm(vec3(uv * 0.5, 0.3)) * u_fuzziness;
      vec3 base = mix(u_thread1Color, u_thread2Color, weave);
      base += fuzzy * 0.15;
      float spec = pow(max(0.0, dot(N, normalize(V + KL))), 20.0) * u_specular;
      spec *= 0.5 + 0.5 * weave;
      col = base * (diff * 0.7 + 0.4) + spec * base * 0.5;
      alpha = 1.0;
    `
    }],
    "Architectural": [{
        id: "stained_glass",
        name: "Stained Glass",
        category: "Architectural",
        kind: "shader",
        swatch: ["#000000", "#ffcc00"],
        transparent: true,
        additive: false,
        depthWrite: false,
        params: [{
                key: "leadColor",
                label: "Lead Color",
                type: "color",
                default: "#222222"
            },
            {
                key: "glassOpacity",
                label: "Glass Opacity",
                type: "float",
                default: 0.85,
                min: 0.2,
                max: 1.0
            },
            {
                key: "panelScale",
                label: "Panel Scale",
                type: "float",
                default: 4.0,
                min: 1.0,
                max: 10.0
            },
            {
                key: "hueVar",
                label: "Hue Variation",
                type: "float",
                default: 0.6,
                min: 0.0,
                max: 1.0
            },
            {
                key: "brightness",
                label: "Brightness",
                type: "float",
                default: 1.2,
                min: 0.5,
                max: 2.0
            },
            {
                key: "specular",
                label: "Specular",
                type: "float",
                default: 0.5,
                min: 0.0,
                max: 1.0
            }
        ],
        fragUniforms: `
      uniform vec3 u_leadColor;
      uniform float u_glassOpacity;
      uniform float u_panelScale;
      uniform float u_hueVar;
      uniform float u_brightness;
      uniform float u_specular;
    `,
        fragBody: `
      vec2 st = vWorldPos.xy * u_panelScale;
      vec2 i_st = floor(st);
      vec2 f_st = fract(st);
      float minDist = 1.0;
      for (int m = -1; m <= 1; m++) {
        for (int n = -1; n <= 1; n++) {
          vec2 neighbor = vec2(float(m), float(n));
          vec2 point = vec2(
            snoise(vec3(i_st + neighbor, 0.0)),
            snoise(vec3(i_st + neighbor, 1.0))
          ) * 0.5 + 0.5;
          vec2 diff = neighbor + point - f_st;
          float d = dot(diff, diff);
          minDist = min(minDist, d);
        }
      }
      float cellIndex = snoise(vec3(i_st, 2.0)) * 0.5 + 0.5;
      float hue = fract(cellIndex * u_hueVar + u_time * 0.01);
      vec3 glassCol = hsv2rgb(vec3(hue, 0.6, 0.7 * u_brightness));
      float border = 1.0 - smoothstep(0.0, 0.08, sqrt(minDist));
      vec3 baseCol = mix(glassCol, u_leadColor, border);
      float spec = pow(max(0.0, dot(N, normalize(V + KL))), 40.0) * u_specular;
      col = baseCol * (diff * 0.7 + 0.4) + spec * glassCol * 0.4;
      alpha = mix(u_glassOpacity, 1.0, border);
    `
    }],
    "Biomimetic & Organic": [{
            id: "subsurface_scatter",
            name: "Subsurface Scatter",
            category: "Biomimetic & Organic",
            kind: "shader",
            swatch: ["#ffccaa", "#ff8866"],
            transparent: false,
            additive: false,
            depthWrite: true,
            params: [{
                    key: "surfaceColor",
                    label: "Surface Color",
                    type: "color",
                    default: "#f4a460"
                },
                {
                    key: "subsurfaceColor",
                    label: "Subsurface Color",
                    type: "color",
                    default: "#ff4500"
                },
                {
                    key: "scatterDepth",
                    label: "Scatter Depth",
                    type: "float",
                    default: 0.6,
                    min: 0.1,
                    max: 1.5
                },
                {
                    key: "fuzziness",
                    label: "Fuzziness",
                    type: "float",
                    default: 0.4,
                    min: 0.0,
                    max: 1.0
                },
                {
                    key: "backlightStr",
                    label: "Backlight Strength",
                    type: "float",
                    default: 0.7,
                    min: 0.0,
                    max: 2.0
                },
                {
                    key: "radius",
                    label: "Radius",
                    type: "float",
                    default: 0.5,
                    min: 0.1,
                    max: 1.0
                }
            ],
            fragUniforms: `
      uniform vec3 u_surfaceColor;
      uniform vec3 u_subsurfaceColor;
      uniform float u_scatterDepth;
      uniform float u_fuzziness;
      uniform float u_backlightStr;
      uniform float u_radius;
    `,
            fragBody: `
      float NoL = max(0.0, dot(N, KL));
      float backLight = max(0.0, dot(N, -KL));
      float thickness = fbm(vec3(vWorldPos * 3.0, 0.2)) * u_radius;
      thickness += snoise(vec3(vWorldPos * 8.0, 0.7)) * u_fuzziness * 0.3;
      float scatter = exp(-thickness * u_scatterDepth);
      vec3 sssCol = u_subsurfaceColor * scatter * backLight * u_backlightStr;
      vec3 surface = u_surfaceColor * (NoL * 0.7 + 0.3);
      vec3 spec = pow(max(0.0, dot(N, normalize(V + KL))), 20.0) * 0.25;
      col = surface + sssCol + spec;
      col = mix(col, u_surfaceColor, 0.1 * u_fuzziness);
      alpha = 1.0;
    `
        },
        {
            id: "moss",
            name: "Moss",
            category: "Biomimetic & Organic",
            kind: "shader",
            swatch: ["#2e4a22", "#1b2e0e"],
            transparent: false,
            additive: false,
            depthWrite: true,
            params: [{
                    key: "mossColor",
                    label: "Moss Color",
                    type: "color",
                    default: "#3a5f2d"
                },
                {
                    key: "baseColor",
                    label: "Base Color",
                    type: "color",
                    default: "#4a3b32"
                },
                {
                    key: "density",
                    label: "Density",
                    type: "float",
                    default: 0.7,
                    min: 0.0,
                    max: 1.0
                },
                {
                    key: "clumpSize",
                    label: "Clump Size",
                    type: "float",
                    default: 3.0,
                    min: 1.0,
                    max: 8.0
                },
                {
                    key: "humidity",
                    label: "Humidity Sheen",
                    type: "float",
                    default: 0.4,
                    min: 0.0,
                    max: 1.0
                },
                {
                    key: "coverage",
                    label: "Coverage",
                    type: "float",
                    default: 0.6,
                    min: 0.0,
                    max: 1.0
                }
            ],
            fragUniforms: `
      uniform vec3 u_mossColor;
      uniform vec3 u_baseColor;
      uniform float u_density;
      uniform float u_clumpSize;
      uniform float u_humidity;
      uniform float u_coverage;
    `,
            fragBody: `
      vec3 pos = vWorldPos * u_clumpSize;
      float n = fbm(vec3(pos.xy, 0.2)) * 0.6 + fbm(vec3(pos.xy * 2.0, 0.8)) * 0.4;
      float mossMask = smoothstep(0.4, 0.6, n) * u_coverage;
      mossMask *= smoothstep(0.2, 0.4, vWorldPos.y + 0.5) * u_density;
      vec3 mossCol = u_mossColor * (0.8 + 0.2 * fbm(vec3(pos.xy * 3.0, u_time * 0.02)));
      float mossSpec = pow(max(0.0, dot(N, normalize(V + KL))), 10.0) * u_humidity;
      col = mix(u_baseColor, mossCol, mossMask) * (diff * 0.7 + 0.4);
      col += mossSpec * u_mossColor * 0.3 * mossMask;
      alpha = 1.0;
    `
        },
        {
            id: "snake_skin",
            name: "Snake Skin",
            category: "Biomimetic & Organic",
            kind: "shader",
            swatch: "#556B2F",
            flags: {},
            params: [{
                    name: 'Scale Size',
                    default: 1.5,
                    min: 0.5,
                    max: 3.0
                },
                {
                    name: 'Iridescence',
                    default: 0.7,
                    min: 0.0,
                    max: 1.0
                },
                {
                    name: 'Pattern Complexity',
                    default: 0.6,
                    min: 0.0,
                    max: 1.0
                },
                {
                    name: 'Color Shift',
                    default: 0.4,
                    min: 0.0,
                    max: 1.0
                },
                {
                    name: 'Contrast',
                    default: 1.0,
                    min: 0.5,
                    max: 2.0
                },
            ],
            fragUniforms: `
      uniform float u_scaleSize;
      uniform float u_iridescence;
      uniform float u_patternComplexity;
      uniform float u_colorShift;
      uniform float u_contrast;
    `,
            fragBody: `
      vec2 uv = vUv * u_scaleSize;
      vec2 id = floor(uv);
      vec2 gv = fract(uv) - 0.5;
      float shape = length(gv) * 2.0;
      float hex = abs(gv.x) + abs(gv.y) * 0.7;
      float scale = mix(hex, shape, u_patternComplexity);

      float edge = 1.0 - smoothstep(0.35, 0.5, scale);
      float inner = smoothstep(0.15, 0.3, scale) * 0.6;
      float pattern = edge + inner;

      float hue = fract((id.x * 0.3 + id.y * 0.5) * 0.7 + uTime * 0.05 * u_colorShift);
      vec3 baseColor = hsv2rgb(vec3(hue, 0.6, 0.7));
      vec3 iri = hsv2rgb(vec3(fract(hue + 0.5), 0.8, 0.9));
      vec3 col = mix(baseColor, iri, u_iridescence * pattern);
      col = mix(vec3(0.5), col, u_contrast);
      gl_FragColor = vec4(col, 1.0);
    `
        }
    ],
    "Water & Fluid": [{
        id: "caustics",
        name: "Caustics",
        category: "Water & Fluid",
        kind: "shader",
        swatch: ["#003344", "#88ccff"],
        transparent: false,
        additive: false,
        depthWrite: true,
        params: [{
                key: "depth",
                label: "Depth",
                type: "float",
                default: 1.0,
                min: 0.1,
                max: 3.0
            },
            {
                key: "intensity",
                label: "Intensity",
                type: "float",
                default: 1.0,
                min: 0.0,
                max: 2.0
            },
            {
                key: "waveFreq",
                label: "Wave Frequency",
                type: "float",
                default: 2.0,
                min: 0.5,
                max: 8.0
            },
            {
                key: "sharpness",
                label: "Caustic Sharpness",
                type: "float",
                default: 0.7,
                min: 0.1,
                max: 1.5
            },
            {
                key: "dispersion",
                label: "Color Dispersion",
                type: "float",
                default: 0.3,
                min: 0.0,
                max: 1.0
            }
        ],
        fragUniforms: `
      uniform float u_depth;
      uniform float u_intensity;
      uniform float u_waveFreq;
      uniform float u_sharpness;
      uniform float u_dispersion;
    `,
        fragBody: `
      vec2 uv = vWorldPos.xy * u_waveFreq;
      float t = u_time;
      float n1 = snoise(vec3(uv + vec2(0.0, t * 0.3), 0.5));
      float n2 = snoise(vec3(uv * 1.7 - vec2(t * 0.2, 0.0), 0.9));
      float ddx = n1 - snoise(vec3(uv + vec2(0.01, 0.0), 0.5));
      float ddy = n1 - snoise(vec3(uv + vec2(0.0, 0.01), 0.5));
      float curvature = abs(ddx) + abs(ddy);
      float disp = u_dispersion * 0.01;
      float causticR = 1.0 - smoothstep(0.0, u_sharpness, abs(snoise(vec3(uv + disp, 0.5)) - curvature));
      float causticG = 1.0 - smoothstep(0.0, u_sharpness, abs(snoise(vec3(uv, 0.6)) - curvature));
      float causticB = 1.0 - smoothstep(0.0, u_sharpness, abs(snoise(vec3(uv - disp, 0.7)) - curvature));
      float mask = (causticR + causticG + causticB) * 0.33;
      vec3 causticCol = vec3(causticR, causticG, causticB) * u_intensity;
      vec3 waterCol = vec3(0.0, 0.1, 0.2) * u_depth;
      col = waterCol + causticCol * 1.5;
      alpha = 1.0;
    `
    }],
    "Candy & Crystal": [{
            id: "crystal_lattice",
            name: "Crystal Lattice",
            category: "Candy & Crystal",
            kind: "shader",
            swatch: ["#ee82ee", "#ffffff"],
            transparent: false,
            additive: false,
            depthWrite: true,
            params: [{
                    key: "baseHue",
                    label: "Base Hue",
                    type: "float",
                    default: 0.8,
                    min: 0.0,
                    max: 1.0
                },
                {
                    key: "facetScale",
                    label: "Facet Scale",
                    type: "float",
                    default: 6.0,
                    min: 2.0,
                    max: 20.0
                },
                {
                    key: "impurity",
                    label: "Impurity",
                    type: "float",
                    default: 0.2,
                    min: 0.0,
                    max: 1.0
                },
                {
                    key: "growthRings",
                    label: "Growth Rings",
                    type: "float",
                    default: 0.4,
                    min: 0.0,
                    max: 1.0
                },
                {
                    key: "specHard",
                    label: "Specular Hardness",
                    type: "float",
                    default: 80.0,
                    min: 10.0,
                    max: 200.0
                },
                {
                    key: "internalGlow",
                    label: "Internal Glow",
                    type: "float",
                    default: 0.15,
                    min: 0.0,
                    max: 0.5
                }
            ],
            fragUniforms: `
      uniform float u_baseHue;
      uniform float u_facetScale;
      uniform float u_impurity;
      uniform float u_growthRings;
      uniform float u_specHard;
      uniform float u_internalGlow;
    `,
            fragBody: `
      vec3 p = vWorldPos * u_facetScale;
      vec3 p_fract = fract(p) - 0.5;
      vec3 p_floor = floor(p);
      float seed = snoise(vec3(p_floor, 0.3));
      float impurityNoise = snoise(vec3(p_floor * 2.0, 0.9)) * u_impurity;
      float hue = fract(u_baseHue + impurityNoise * 0.4);
      vec3 crystalCol = hsv2rgb(vec3(hue, 0.5, 0.9));
      float ring = 1.0 - abs(snoise(vec3(p * 0.3, u_time * 0.05)) * u_growthRings);
      crystalCol = mix(crystalCol, crystalCol * 0.8, ring);
      float facet = max(max(abs(p_fract.x), abs(p_fract.y)), abs(p_fract.z));
      float facetMask = 1.0 - smoothstep(0.4, 0.45, facet);
      vec3 facetN = faceforward(vec3(1.0, 0.0, 0.0), V, N);
      float spec = pow(max(0.0, dot(N, normalize(V + KL))), u_specHard);
      spec *= facetMask * 0.8;
      vec3 glow = crystalCol * u_internalGlow;
      col = crystalCol * (diff * 0.5 + 0.5) + spec * 1.2 + glow;
      alpha = 1.0;
    `
        },
        {
            id: "geode",
            name: "Geode Crystal",
            category: "Candy & Crystal",
            kind: "shader",
            swatch: "#DA70D6",
            flags: {},
            params: [{
                    name: 'Crystal Density',
                    default: 0.8,
                    min: 0.0,
                    max: 1.0
                },
                {
                    name: 'Facet Sharpness',
                    default: 0.7,
                    min: 0.0,
                    max: 1.0
                },
                {
                    name: 'Color Shift',
                    default: 0.5,
                    min: 0.0,
                    max: 1.0
                },
                {
                    name: 'Cavity Depth',
                    default: 0.6,
                    min: 0.0,
                    max: 1.0
                },
                {
                    name: 'Glow',
                    default: 0.2,
                    min: 0.0,
                    max: 0.5
                },
            ],
            fragUniforms: `
      uniform float u_crystalDensity;
      uniform float u_facetSharpness;
      uniform float u_colorShift;
      uniform float u_cavityDepth;
      uniform float u_glow;
    `,
            fragBody: `
      vec2 uv = vUv;
      vec2 center = vec2(0.5, 0.5);
      float dist = length(uv - center) * 2.0;
      float cavity = smoothstep(0.2, 0.5, dist) * u_cavityDepth;
      float angle = atan(uv.y - center.y, uv.x - center.x);
      float crystals = abs(sin(angle * 8.0)) * u_crystalDensity;
      crystals += abs(sin(dist * 15.0)) * 0.3;
      crystals = pow(crystals, u_facetSharpness * 3.0);
      float pattern = crystals * (1.0 - cavity * 0.7);
      vec3 col = hsv2rgb(vec3(fract(angle / 6.28318 * u_colorShift), 0.8, 0.9));
      col *= pattern;
      col += u_glow * vec3(0.7, 0.2, 0.7) * (1.0 - dist);
      gl_FragColor = vec4(col, 1.0);
    `
        }
    ],
    "Maps & Cartography": [{
        id: "topographical",
        name: "Topographical",
        category: "Maps & Cartography",
        kind: "shader",
        swatch: ["#228b22", "#8b4513"],
        transparent: false,
        additive: false,
        depthWrite: true,
        params: [{
                key: "lowColor",
                label: "Low Elevation Color",
                type: "color",
                default: "#006400"
            },
            {
                key: "highColor",
                label: "High Elevation Color",
                type: "color",
                default: "#d2b48c"
            },
            {
                key: "contourSpacing",
                label: "Contour Spacing",
                type: "float",
                default: 0.15,
                min: 0.05,
                max: 0.5
            },
            {
                key: "relief",
                label: "Relief Intensity",
                type: "float",
                default: 0.5,
                min: 0.0,
                max: 1.0
            },
            {
                key: "waterColor",
                label: "Water Color",
                type: "color",
                default: "#1e90ff"
            },
            {
                key: "terrainScale",
                label: "Terrain Scale",
                type: "float",
                default: 2.5,
                min: 1.0,
                max: 8.0
            }
        ],
        fragUniforms: `
      uniform vec3 u_lowColor;
      uniform vec3 u_highColor;
      uniform float u_contourSpacing;
      uniform float u_relief;
      uniform vec3 u_waterColor;
      uniform float u_terrainScale;
    `,
        fragBody: `
      vec2 uv = vWorldPos.xy * u_terrainScale;
      float height = fbm(vec3(uv, 0.5)) * 0.7 + snoise(vec3(uv * 2.0, 0.9)) * 0.3;
      height = height * 0.5 + 0.5;
      float contour = abs(fract(height / u_contourSpacing) - 0.5) * 2.0;
      contour = 1.0 - smoothstep(0.0, 0.03, contour);
      vec3 land = mix(u_lowColor, u_highColor, height);
      float water = step(height, 0.35);
      vec3 colTerrain = mix(land, u_waterColor, water);
      colTerrain = mix(colTerrain, vec3(0.0), contour * 0.6);
      float reliefShade = (diff * 0.5 + 0.5) * (0.8 + 0.2 * u_relief);
      col = colTerrain * reliefShade;
      alpha = 1.0;
    `
    }],
    "Stone & Mineral": [{
        id: "marble",
        name: "Marble",
        category: "Stone & Mineral",
        kind: "shader",
        swatch: ["#f5f5dc", "#2f4f4f"],
        transparent: false,
        additive: false,
        depthWrite: true,
        params: [{
                key: "stoneColor",
                label: "Stone Color",
                type: "color",
                default: "#f0ead6"
            },
            {
                key: "veinColor",
                label: "Vein Color",
                type: "color",
                default: "#3b3b3b"
            },
            {
                key: "veinScale",
                label: "Vein Scale",
                type: "float",
                default: 2.0,
                min: 0.5,
                max: 5.0
            },
            {
                key: "sharpness",
                label: "Vein Sharpness",
                type: "float",
                default: 0.5,
                min: 0.1,
                max: 1.0
            },
            {
                key: "polish",
                label: "Polish",
                type: "float",
                default: 0.8,
                min: 0.0,
                max: 1.5
            },
            {
                key: "turbulence",
                label: "Turbulence",
                type: "float",
                default: 0.6,
                min: 0.0,
                max: 1.0
            }
        ],
        fragUniforms: `
      uniform vec3 u_stoneColor;
      uniform vec3 u_veinColor;
      uniform float u_veinScale;
      uniform float u_sharpness;
      uniform float u_polish;
      uniform float u_turbulence;
    `,
        fragBody: `
      vec3 p = vWorldPos * u_veinScale;
      float n = fbm(vec3(p.xy, 0.3) + vec3(snoise(vec3(p.xy * u_turbulence, 0.7)), 0.0, 0.0));
      n = abs(n * 2.0 - 1.0);
      n = 1.0 - pow(n, u_sharpness * 3.0);
      vec3 marbleCol = mix(u_stoneColor, u_veinColor, n);
      float spec = pow(max(0.0, dot(N, normalize(V + KL))), 60.0) * u_polish;
      col = marbleCol * (diff * 0.6 + 0.5) + spec * 0.7;
      alpha = 1.0;
    `
    }],
    "Destruction & Stress": [{
        id: "cracked_glass",
        name: "Cracked Glass",
        category: "Destruction & Stress",
        kind: "shader",
        swatch: ["#aaddff", "#ffffff"],
        transparent: true,
        additive: false,
        depthWrite: false,
        params: [{
                key: "glassTint",
                label: "Glass Tint",
                type: "color",
                default: "#cceeff"
            },
            {
                key: "crackDensity",
                label: "Crack Density",
                type: "float",
                default: 1.5,
                min: 0.5,
                max: 4.0
            },
            {
                key: "crackWidth",
                label: "Crack Width",
                type: "float",
                default: 0.03,
                min: 0.01,
                max: 0.1
            },
            {
                key: "refractionStr",
                label: "Refraction Strength",
                type: "float",
                default: 0.15,
                min: 0.0,
                max: 0.4
            },
            {
                key: "stressIntensity",
                label: "Stress Intensity",
                type: "float",
                default: 0.5,
                min: 0.0,
                max: 1.0
            },
            {
                key: "shine",
                label: "Shine",
                type: "float",
                default: 1.0,
                min: 0.0,
                max: 2.0
            }
        ],
        fragUniforms: `
      uniform vec3 u_glassTint;
      uniform float u_crackDensity;
      uniform float u_crackWidth;
      uniform float u_refractionStr;
      uniform float u_stressIntensity;
      uniform float u_shine;
    `,
        fragBody: `
      vec2 uv = vWorldPos.xy * u_crackDensity;
      float t = u_time * 0.05;
      float crack = 0.0;
      vec2 seed = vec2(snoise(vec3(floor(uv), 0.0)), snoise(vec3(floor(uv), 1.0)));
      vec2 f = fract(uv) - 0.5;
      float dist = length(f - seed * 0.5);
      crack = 1.0 - smoothstep(u_crackWidth, u_crackWidth + 0.02, dist);
      float stress = snoise(vec3(uv * 0.5, t)) * u_stressIntensity;
      crack += stress * 0.3;
      vec3 refractN = N + vec3(snoise(vec3(vWorldPos.xy * 10.0, t)), snoise(vec3(vWorldPos.xy * 10.0, t + 1.0)), 0.0) * u_refractionStr;
      refractN = normalize(refractN);
      float spec = pow(max(0.0, dot(refractN, normalize(V + KL))), 100.0) * u_shine;
      vec3 glassCol = u_glassTint * (0.7 + 0.3 * fres);
      float alphaCrack = clamp(crack, 0.0, 1.0);
      col = mix(glassCol, vec3(1.0), spec);
      alpha = mix(0.15, 0.95, alphaCrack);
    `
    }],
    "Natural Terrain": [{
        id: "sand",
        name: "Sand",
        category: "Natural Terrain",
        kind: "shader",
        swatch: ["#c2b280", "#8b7355"],
        transparent: false,
        additive: false,
        depthWrite: true,
        params: [{
                key: "sandColor",
                label: "Sand Color",
                type: "color",
                default: "#e6c280"
            },
            {
                key: "shadowColor",
                label: "Shadow Color",
                type: "color",
                default: "#8b7355"
            },
            {
                key: "grainSize",
                label: "Grain Size",
                type: "float",
                default: 10.0,
                min: 2.0,
                max: 30.0
            },
            {
                key: "duneScale",
                label: "Dune Scale",
                type: "float",
                default: 1.5,
                min: 0.5,
                max: 5.0
            },
            {
                key: "clumpiness",
                label: "Clumpiness",
                type: "float",
                default: 0.3,
                min: 0.0,
                max: 1.0
            },
            {
                key: "specular",
                label: "Specular",
                type: "float",
                default: 0.2,
                min: 0.0,
                max: 0.8
            }
        ],
        fragUniforms: `
      uniform vec3 u_sandColor;
      uniform vec3 u_shadowColor;
      uniform float u_grainSize;
      uniform float u_duneScale;
      uniform float u_clumpiness;
      uniform float u_specular;
    `,
        fragBody: `
      vec2 uv = vWorldPos.xy * u_duneScale;
      float dune = fbm(vec3(uv, 0.3)) * 0.7 + snoise(vec3(uv * 0.5, 0.8)) * 0.3;
      vec2 grainUV = vWorldPos.xy * u_grainSize;
      float grain = snoise(vec3(grainUV, 0.5)) * 0.5 + 0.5;
      grain = grain * 0.8 + 0.2 * snoise(vec3(grainUV * 2.0, 0.9));
      float clump = snoise(vec3(grainUV * 0.2, 0.7));
      grain = mix(grain, 0.5, clump * u_clumpiness);
      vec3 sandCol = mix(u_shadowColor, u_sandColor, grain);
      sandCol = mix(sandCol, u_sandColor, dune * 0.4);
      float spec = pow(max(0.0, dot(N, normalize(V + KL))), 30.0) * u_specular;
      col = sandCol * (diff * 0.8 + 0.3) + spec * u_sandColor * 0.2;
      alpha = 1.0;
    `
    }],
    "Heraldry & Symbols": [{
        id: "emblem",
        name: "Emblem",
        category: "Heraldry & Symbols",
        kind: "shader",
        swatch: ["#800020", "#ffd700"],
        transparent: false,
        additive: false,
        depthWrite: true,
        params: [{
                key: "fabricColor",
                label: "Fabric Color",
                type: "color",
                default: "#800020"
            },
            {
                key: "threadColor",
                label: "Thread Color",
                type: "color",
                default: "#ffd700"
            },
            {
                key: "patternScale",
                label: "Pattern Scale",
                type: "float",
                default: 3.0,
                min: 1.0,
                max: 8.0
            },
            {
                key: "embossHeight",
                label: "Emboss Height",
                type: "float",
                default: 0.4,
                min: 0.1,
                max: 1.0
            },
            {
                key: "metallic",
                label: "Metallic Shine",
                type: "float",
                default: 0.8,
                min: 0.0,
                max: 1.5
            },
            {
                key: "threadWidth",
                label: "Thread Width",
                type: "float",
                default: 0.3,
                min: 0.1,
                max: 0.8
            }
        ],
        fragUniforms: `
      uniform vec3 u_fabricColor;
      uniform vec3 u_threadColor;
      uniform float u_patternScale;
      uniform float u_embossHeight;
      uniform float u_metallic;
      uniform float u_threadWidth;
    `,
        fragBody: `
      vec2 uv = vWorldPos.xy * u_patternScale;
      float pattern = fbm(vec3(uv, 0.2));
      pattern = pattern * 0.6 + snoise(vec3(uv * 2.5, 0.7)) * 0.4;
      float filigree = sin(pattern * 10.0) * 0.5 + 0.5;
      filigree = smoothstep(u_threadWidth - 0.05, u_threadWidth, filigree);
      float emboss = pattern * 2.0 - 1.0;
      vec3 normalEmboss = normalize(N + vec3(emboss * u_embossHeight, emboss * u_embossHeight * 0.5, 0.0));
      float fabricShade = diff * 0.7 + 0.4;
      vec3 fabric = u_fabricColor * fabricShade;
      float spec = pow(max(0.0, dot(normalEmboss, normalize(V + KL))), 30.0) * u_metallic;
      col = mix(fabric, u_threadColor, filigree) + spec * u_threadColor * 0.7;
      alpha = 1.0;
    `
    }],
    "Fluid Metals": [{
        id: "liquid_metal",
        name: "Liquid Metal",
        category: "Fluid Metals",
        kind: "shader",
        swatch: "#C0C0C0",
        flags: {
            isGravityAware: true
        },
        params: [{
                name: 'Flow Speed',
                default: 1.0,
                min: 0.0,
                max: 5.0
            },
            {
                name: 'Viscosity',
                default: 0.3,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Metallic Tint',
                default: 0.5,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Distortion',
                default: 0.7,
                min: 0.0,
                max: 2.0
            },
            {
                name: 'Contrast',
                default: 1.2,
                min: 0.5,
                max: 2.0
            },
        ],
        fragUniforms: `
      uniform float u_flowSpeed;
      uniform float u_viscosity;
      uniform float u_metallicTint;
      uniform float u_distortion;
      uniform float u_contrast;
    `,
        fragBody: `
      vec2 uv = vUv;
      vec3 gravity = normalize(uGravity);
      vec2 flowDir = gravity.xy * u_flowSpeed * 0.1;

      float n = 0.0;
      float amp = 1.0;
      float freq = 4.0;
      vec2 p = uv * 5.0;
      for (int i = 0; i < 5; i++) {
        n += amp * abs(snoise(vec3(p + flowDir * uTime * (1.0 + float(i) * 0.2), uTime * 0.1)));
        p *= 2.0;
        amp *= 0.5;
      }
      n = smoothstep(0.2, 0.8, n);

      vec3 metalBase = mix(vec3(0.7, 0.7, 0.8), vec3(0.9, 0.8, 0.6), u_metallicTint);
      float spec = pow(max(0.0, n - 0.5) * 2.0, 3.0);
      vec3 col = metalBase * (n * 0.8 + 0.2) + spec * vec3(1.0);
      col = mix(col, col * (1.0 + sin(uv.x * 10.0) * 0.2), u_distortion);
      col = mix(vec3(0.5), col, u_contrast);
      gl_FragColor = vec4(col, 1.0);
    `
    }],
    "Materials & Textile": [{
        id: "leather",
        name: "Leather",
        category: "Materials & Textile",
        kind: "shader",
        swatch: "#8B4513",
        flags: {},
        params: [{
                name: 'Grain Size',
                default: 0.5,
                min: 0.1,
                max: 2.0
            },
            {
                name: 'Wrinkle Depth',
                default: 0.6,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Base Color',
                default: 0.0,
                min: 0.0,
                max: 1.0,
                isColor: true
            },
            {
                name: 'Aging',
                default: 0.3,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Gloss',
                default: 0.2,
                min: 0.0,
                max: 1.0
            },
        ],
        fragUniforms: `
      uniform float u_grainSize;
      uniform float u_wrinkleDepth;
      uniform vec3 u_baseColor;
      uniform float u_aging;
      uniform float u_gloss;
    `,
        fragBody: `
      vec2 uv = vUv;
      float grain = snoise(vec3(uv * 20.0 * u_grainSize, 0.0)) * 0.5 + 0.5;
      float wrinkle = abs(snoise(vec3(uv * 8.0 + uTime * 0.05, uTime * 0.02)));
      wrinkle = smoothstep(0.4, 1.0, wrinkle) * u_wrinkleDepth;

      float ageNoise = snoise(vec3(uv * 30.0, 1.0));
      float age = ageNoise * u_aging;

      vec3 leatherCol = mix(u_baseColor, u_baseColor * 0.7, grain * 0.8 + wrinkle * 0.6);
      leatherCol += age * vec3(0.6, 0.5, 0.4);
      leatherCol = clamp(leatherCol, 0.0, 1.0);

      float spec = pow(max(0.0, grain * 1.2 - 0.2), 8.0) * u_gloss;
      vec3 finalCol = leatherCol + spec;
      gl_FragColor = vec4(finalCol, 1.0);
    `
    }],
    "Wood & Natural Materials": [{
        id: "wood_grain",
        name: "Wood Grain",
        category: "Wood & Natural Materials",
        kind: "shader",
        swatch: "#8B5A2B",
        flags: {},
        params: [{
                name: 'Grain Scale',
                default: 1.0,
                min: 0.2,
                max: 3.0
            },
            {
                name: 'Knot Density',
                default: 0.3,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Ring Contrast',
                default: 1.5,
                min: 0.5,
                max: 3.0
            },
            {
                name: 'Wood Tint',
                default: 0.5,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Gloss',
                default: 0.15,
                min: 0.0,
                max: 0.5
            },
        ],
        fragUniforms: `
      uniform float u_grainScale;
      uniform float u_knotDensity;
      uniform float u_ringContrast;
      uniform float u_woodTint;
      uniform float u_gloss;
    `,
        fragBody: `
      vec2 uv = vUv * u_grainScale;
      vec2 grain = vec2(uv.x, uv.y + sin(uv.x * 6.28318) * 0.1);
      float rings = abs(sin(grain.y * 20.0 + grain.x * 5.0)) * 0.5 + 0.5;
      rings = pow(rings, u_ringContrast);

      float knot = 1.0;
      for (int i = 0; i < 3; i++) {
        vec2 knotPos = vec2(
          snoise(vec3(float(i) * 1.3, 0.0, 0.5)) * 0.8 + 0.5,
          snoise(vec3(float(i) * 2.7, 1.0, 0.3)) * 0.8 + 0.5
        );
        float d = length(grain - knotPos) * 8.0;
        float knotShape = 1.0 - smoothstep(0.2, 0.6, d) * 0.9;
        knot = min(knot, knotShape);
      }

      float wood = mix(rings, rings * 0.8 + knot * 0.5, u_knotDensity);
      vec3 baseCol = mix(vec3(0.6, 0.4, 0.2), vec3(0.8, 0.6, 0.3), u_woodTint);
      vec3 col = baseCol * (0.5 + wood * 0.6);
      float gloss = pow(rings * 0.8, 3.0) * u_gloss;
      col += gloss;
      gl_FragColor = vec4(col, 1.0);
    `
    }],
    "Industrial": [{
        id: "carbon_fiber",
        name: "Carbon Fiber",
        category: "Industrial",
        kind: "shader",
        swatch: "#2F4F4F",
        flags: {},
        params: [{
                name: 'Weave Scale',
                default: 2.0,
                min: 0.5,
                max: 5.0
            },
            {
                name: 'Anisotropy',
                default: 0.8,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Fiber Width',
                default: 0.6,
                min: 0.1,
                max: 1.0
            },
            {
                name: 'Highlight',
                default: 0.5,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Darkness',
                default: 0.3,
                min: 0.0,
                max: 0.8
            },
        ],
        fragUniforms: `
      uniform float u_weaveScale;
      uniform float u_anisotropy;
      uniform float u_fiberWidth;
      uniform float u_highlight;
      uniform float u_darkness;
    `,
        fragBody: `
      vec2 uv = vUv * u_weaveScale;
      float weave = abs(sin(uv.x * 3.14159 * 2.0)) * 0.5 + 0.5;
      float fiber = abs(sin(uv.y * 12.0)) > (1.0 - u_fiberWidth) ? 0.2 : 0.8;
      float pattern = weave * 0.7 + fiber * 0.5;
      float anisotropy = mix(0.0, (uv.x - floor(uv.x)) * 0.5, u_anisotropy);
      float dark = pattern * (1.0 - u_darkness) + u_darkness;
      float highlight = pow(pattern, 2.0) * u_highlight;
      float finalVal = dark + highlight + anisotropy;
      vec3 col = vec3(finalVal * 0.8, finalVal * 0.85, finalVal * 0.9);
      gl_FragColor = vec4(col, 1.0);
    `
    }],
    "Geometric Patterns": [{
        id: "honeycomb",
        name: "Honeycomb",
        category: "Geometric Patterns",
        kind: "shader",
        swatch: "#DAA520",
        flags: {},
        params: [{
                name: 'Cell Size',
                default: 1.5,
                min: 0.5,
                max: 3.0
            },
            {
                name: 'Wall Thickness',
                default: 0.4,
                min: 0.1,
                max: 1.0
            },
            {
                name: 'Depth Variation',
                default: 0.6,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Warm Tone',
                default: 0.7,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Highlight',
                default: 0.3,
                min: 0.0,
                max: 1.0
            },
        ],
        fragUniforms: `
      uniform float u_cellSize;
      uniform float u_wallThickness;
      uniform float u_depthVariation;
      uniform float u_warmTone;
      uniform float u_highlight;
    `,
        fragBody: `
      vec2 uv = vUv * u_cellSize;
      vec2 q = vec2(uv.x * 1.7320508, uv.y);
      vec2 id = round(q);
      vec2 gv = q - id;
      float angle = atan(gv.y, gv.x) + 3.14159;
      float sector = floor(angle / (3.14159 / 3.0));
      float dist = length(gv);
      float hexDist = 0.5 / cos(mod(angle + 3.14159 / 6.0, 3.14159 / 3.0) - 3.14159 / 6.0);
      float edge = smoothstep(hexDist * 0.95, hexDist, dist);
      float depth = snoise(vec3(id * 0.7, 0.0)) * u_depthVariation;
      float wall = 1.0 - edge * u_wallThickness;
      wall += depth * 0.3;
      float highlight = pow(max(0.0, 1.0 - dist * 2.0), 3.0) * u_highlight;
      vec3 col = mix(vec3(0.85, 0.65, 0.2), vec3(0.95, 0.8, 0.4), u_warmTone) * (wall * 0.7 + 0.3);
      col += highlight * vec3(1.0, 0.9, 0.6);
      gl_FragColor = vec4(col, 1.0);
    `
    }],
    "Mathematical Art": [{
            id: "moire_pattern",
            name: "Moiré Interference",
            category: "Mathematical Art",
            kind: "shader",
            swatch: "#000000",
            flags: {},
            params: [{
                    name: 'Scale A',
                    default: 1.5,
                    min: 0.5,
                    max: 3.0
                },
                {
                    name: 'Scale B',
                    default: 1.0,
                    min: 0.5,
                    max: 3.0
                },
                {
                    name: 'Angle Offset',
                    default: 0.5,
                    min: 0.0,
                    max: 1.0
                },
                {
                    name: 'Contrast',
                    default: 1.0,
                    min: 0.5,
                    max: 2.0
                },
                {
                    name: 'Color Shift',
                    default: 0.5,
                    min: 0.0,
                    max: 1.0
                },
            ],
            fragUniforms: `
      uniform float u_scaleA;
      uniform float u_scaleB;
      uniform float u_angleOffset;
      uniform float u_contrast;
      uniform float u_colorShift;
    `,
            fragBody: `
      vec2 uv = vUv;
      float angle = u_angleOffset * 3.14159;
      vec2 uv1 = vec2(uv.x * cos(angle) - uv.y * sin(angle), uv.x * sin(angle) + uv.y * cos(angle)) * 10.0 * u_scaleA;
      vec2 uv2 = uv * 10.0 * u_scaleB;
      float pattern1 = sin(uv1.x) * sin(uv1.y);
      float pattern2 = sin(uv2.x) * sin(uv2.y);
      float moire = abs(pattern1 - pattern2);
      moire = pow(moire, u_contrast);
      vec3 col = hsv2rgb(vec3(moire * 0.5 + u_colorShift * 0.7, 0.8, 0.9));
      gl_FragColor = vec4(col, 1.0);
    `
        },
        {
            id: "distorted_grid",
            name: "Distorted Grid",
            category: "Mathematical Art",
            kind: "shader",
            swatch: "#FFFFFF",
            flags: {},
            params: [{
                    name: 'Frequency',
                    default: 1.0,
                    min: 0.5,
                    max: 4.0
                },
                {
                    name: 'Distortion Amount',
                    default: 0.8,
                    min: 0.0,
                    max: 2.0
                },
                {
                    name: 'Line Thickness',
                    default: 0.5,
                    min: 0.1,
                    max: 1.0
                },
                {
                    name: 'Color1',
                    default: 0.0,
                    min: 0.0,
                    max: 1.0,
                    isColor: true
                },
                {
                    name: 'Color2',
                    default: 0.6,
                    min: 0.0,
                    max: 1.0,
                    isColor: true
                },
            ],
            fragUniforms: `
      uniform float u_frequency;
      uniform float u_distortionAmount;
      uniform float u_lineThickness;
      uniform vec3 u_color1;
      uniform vec3 u_color2;
    `,
            fragBody: `
      vec2 uv = vUv * u_frequency;
      float distort = snoise(vec3(uv * 1.5 + uTime * 0.2, 0.5)) * u_distortionAmount;
      float gridX = abs(sin(uv.x * 3.14159 * 2.0 + distort)) > (1.0 - u_lineThickness) ? 0.0 : 1.0;
      float gridY = abs(sin(uv.y * 3.14159 * 2.0 + distort)) > (1.0 - u_lineThickness) ? 0.0 : 1.0;
      float grid = 1.0 - gridX * gridY;
      vec3 col = mix(u_color1, u_color2, grid);
      gl_FragColor = vec4(col, 1.0);
    `
        }
    ],
    "Digital Degradation": [{
        id: "glitch_art",
        name: "Glitch Art",
        category: "Digital Degradation",
        kind: "shader",
        swatch: "#FF00FF",
        flags: {},
        params: [{
                name: 'Block Size',
                default: 0.05,
                min: 0.01,
                max: 0.2
            },
            {
                name: 'Shift Amount',
                default: 0.5,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Tearing',
                default: 0.3,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Color Split',
                default: 1.0,
                min: 0.0,
                max: 2.0
            },
            {
                name: 'Noise Level',
                default: 0.2,
                min: 0.0,
                max: 1.0
            },
        ],
        fragUniforms: `
      uniform float u_blockSize;
      uniform float u_shiftAmount;
      uniform float u_tearing;
      uniform float u_colorSplit;
      uniform float u_noiseLevel;
    `,
        fragBody: `
      vec2 uv = vUv;
      float blockY = floor(uv.y / u_blockSize) * u_blockSize;
      float rand = hash(blockY);
      float shift = (rand - 0.5) * u_shiftAmount * step(u_tearing, hash(blockY + 0.1));
      float colorShift = (rand - 0.5) * u_colorSplit * step(u_noiseLevel, hash(blockY + 0.2));
      vec2 glitchUV = uv;
      glitchUV.x += shift;
      float r = sin(glitchUV.x * 50.0) * 0.5 + 0.5;
      float g = sin((glitchUV.x + colorShift) * 50.0) * 0.5 + 0.5;
      float b = sin((glitchUV.x - colorShift) * 50.0) * 0.5 + 0.5;
      float noise = hash(vec2(floor(uv.y * 40.0), floor(uv.x * 40.0))) * u_noiseLevel;
      vec3 col = vec3(r, g, b) + noise;
      col = clamp(col, 0.0, 1.0);
      gl_FragColor = vec4(col, 1.0);
    `
    }],
    "Organics & Sea": [{
        id: "sand_dollar",
        name: "Sand Dollar",
        category: "Organics & Sea",
        kind: "shader",
        swatch: "#F5DEB3",
        flags: {},
        params: [{
                name: 'Size',
                default: 1.0,
                min: 0.5,
                max: 2.0
            },
            {
                name: 'Petal Count',
                default: 5,
                min: 3,
                max: 8,
                step: 1
            },
            {
                name: 'Ridge Depth',
                default: 0.7,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Texture Noise',
                default: 0.4,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Color Warmth',
                default: 0.6,
                min: 0.0,
                max: 1.0
            },
        ],
        fragUniforms: `
      uniform float u_size;
      uniform int u_petalCount;
      uniform float u_ridgeDepth;
      uniform float u_textureNoise;
      uniform float u_colorWarmth;
    `,
        fragBody: `
      vec2 uv = vUv - 0.5;
      float r = length(uv) * u_size;
      float a = atan(uv.y, uv.x);
      float petalCount = float(u_petalCount);
      float petal = sin(a * petalCount) * 0.5 + 0.5;
      float ridge = sin(r * 15.0) * 0.5 + 0.5;
      float shape = petal * ridge;
      shape = smoothstep(0.3, 0.7, shape) * u_ridgeDepth + (1.0 - u_ridgeDepth);
      float noise = snoise(vec3(uv * 5.0, 0.0)) * u_textureNoise;
      float pattern = min(1.0, shape + noise);
      vec3 col = mix(vec3(0.96, 0.87, 0.70), vec3(0.94, 0.81, 0.65), u_colorWarmth) * pattern;
      float edge = 1.0 - smoothstep(0.45, 0.5, r);
      col *= edge;
      gl_FragColor = vec4(col, 1.0);
    `
    }],
    "Patterns & Textiles": [{
        id: "camouflage",
        name: "Camouflage",
        category: "Patterns & Textiles",
        kind: "shader",
        swatch: "#556B2F",
        flags: {},
        params: [{
                name: 'Blob Size',
                default: 1.0,
                min: 0.5,
                max: 3.0
            },
            {
                name: 'Complexity',
                default: 0.5,
                min: 0.1,
                max: 1.0
            },
            {
                name: 'Color1',
                default: 0.0,
                min: 0.0,
                max: 1.0,
                isColor: true
            },
            {
                name: 'Color2',
                default: 0.3,
                min: 0.0,
                max: 1.0,
                isColor: true
            },
            {
                name: 'Color3',
                default: 0.6,
                min: 0.0,
                max: 1.0,
                isColor: true
            },
        ],
        fragUniforms: `
      uniform float u_blobSize;
      uniform float u_complexity;
      uniform vec3 u_color1;
      uniform vec3 u_color2;
      uniform vec3 u_color3;
    `,
        fragBody: `
      vec2 uv = vUv * u_blobSize;
      float n1 = snoise(vec3(uv * 3.0, 0.0));
      float n2 = snoise(vec3(uv * 5.0 + 10.0, 1.0));
      float blob = n1 * 0.6 + n2 * 0.4;
      blob = smoothstep(0.2, 0.8, blob) * u_complexity;
      blob += snoise(vec3(uv * 8.0, 2.0)) * 0.3;
      float t = clamp((blob + 0.5) * 1.5, 0.0, 1.0);
      vec3 col = mix(mix(u_color1, u_color2, t), u_color3, pow(t, 2.0));
      gl_FragColor = vec4(col, 1.0);
    `
    }],
    "Ancient & Painted": [{
        id: "cave_painting",
        name: "Cave Painting",
        category: "Ancient & Painted",
        kind: "shader",
        swatch: "#A0522D",
        flags: {},
        params: [{
                name: 'Rock Roughness',
                default: 0.7,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Pigment Density',
                default: 0.5,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Ochre Hue',
                default: 0.15,
                min: 0.0,
                max: 0.5
            },
            {
                name: 'Crack Depth',
                default: 0.4,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Age',
                default: 0.6,
                min: 0.0,
                max: 1.0
            },
        ],
        fragUniforms: `
      uniform float u_rockRoughness;
      uniform float u_pigmentDensity;
      uniform float u_ochreHue;
      uniform float u_crackDepth;
      uniform float u_age;
    `,
        fragBody: `
      vec2 uv = vUv;
      float rock = snoise(vec3(uv * 10.0, 0.0)) * u_rockRoughness;
      rock += snoise(vec3(uv * 25.0, 1.0)) * 0.3;
      rock = rock * 0.5 + 0.5;

      float pigment = snoise(vec3(uv * 15.0 + 5.0, 2.0)) * u_pigmentDensity;
      pigment = smoothstep(0.2, 0.8, pigment);

      vec3 ochre = hsv2rgb(vec3(u_ochreHue, 0.7, 0.6));
      vec3 baseCol = mix(vec3(0.6, 0.55, 0.45), ochre, pigment);

      float crack = abs(snoise(vec3(uv * 25.0, 3.0)));
      crack = smoothstep(0.5 - u_crackDepth * 0.3, 0.5, crack);

      float age = snoise(vec3(uv * 5.0, 4.0)) * u_age;
      vec3 col = baseCol * (1.0 - crack * 0.7) * (1.0 - age * 0.5);
      col = clamp(col, 0.0, 1.0);
      gl_FragColor = vec4(col, 1.0);
    `
    }],
    "Neon & Fluorescent": [{
        id: "neon_lights",
        name: "Neon Lights",
        category: "Neon & Fluorescent",
        kind: "shader",
        swatch: "#FF1493",
        flags: {
            isAdditive: true
        },
        params: [{
                name: 'Tube Spacing',
                default: 1.0,
                min: 0.5,
                max: 3.0
            },
            {
                name: 'Glow Intensity',
                default: 0.8,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Speed',
                default: 1.0,
                min: 0.0,
                max: 2.0
            },
            {
                name: 'Color Variation',
                default: 0.6,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Blur Width',
                default: 0.3,
                min: 0.05,
                max: 0.8
            },
        ],
        fragUniforms: `
      uniform float u_tubeSpacing;
      uniform float u_glowIntensity;
      uniform float u_speed;
      uniform float u_colorVariation;
      uniform float u_blurWidth;
    `,
        fragBody: `
      vec2 uv = vUv;
      float y = uv.y;
      float tube = abs(sin(y * 15.0 * u_tubeSpacing + uTime * u_speed)) < u_blurWidth ? 1.0 : 0.0;
      float glow = exp(-abs(sin(y * 15.0 * u_tubeSpacing + uTime * u_speed)) * 5.0 / u_blurWidth) * u_glowIntensity;
      float hue = fract(y * u_colorVariation + uTime * 0.1);
      vec3 col = hsv2rgb(vec3(hue, 1.0, 1.0)) * (tube + glow);
      gl_FragColor = vec4(col, col.r + col.g + col.b > 0.0 ? 1.0 : 0.0);
    `
    }],
    "Denim & Fabric": [{
        id: "acid_wash",
        name: "Acid Wash Denim",
        category: "Denim & Fabric",
        kind: "shader",
        swatch: "#4B6E9C",
        flags: {},
        params: [{
                name: 'Wash Intensity',
                default: 0.7,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Fiber Detail',
                default: 0.5,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Contrast',
                default: 1.2,
                min: 0.5,
                max: 2.0
            },
            {
                name: 'Blue Hue',
                default: 0.6,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Fade Amount',
                default: 0.4,
                min: 0.0,
                max: 1.0
            },
        ],
        fragUniforms: `
      uniform float u_washIntensity;
      uniform float u_fiberDetail;
      uniform float u_contrast;
      uniform float u_blueHue;
      uniform float u_fadeAmount;
    `,
        fragBody: `
      vec2 uv = vUv;
      float noise = snoise(vec3(uv * 20.0, 0.0)) * 0.5 + 0.5;
      float fiber = snoise(vec3(uv * 80.0 + 5.0, 1.0)) * u_fiberDetail;
      float wash = snoise(vec3(uv * 8.0 + 3.0, 2.0)) * u_washIntensity;
      wash = smoothstep(0.3, 0.7, wash);
      float pattern = mix(noise, wash, 0.7) + fiber * 0.2;
      pattern = mix(pattern, pattern * 1.5, u_contrast - 1.0);
      float fade = snoise(vec3(uv * 12.0, 3.0)) * u_fadeAmount;
      vec3 baseBlue = hsv2rgb(vec3(u_blueHue, 0.8, 0.7));
      vec3 white = vec3(0.95);
      vec3 col = mix(baseBlue, white, pattern * fade);
      gl_FragColor = vec4(col, 1.0);
    `
    }],
    "Astro & Planetary": [{
        id: "asteroid",
        name: "Asteroid Surface",
        category: "Astro & Planetary",
        kind: "shader",
        swatch: "#696969",
        flags: {},
        params: [{
                name: 'Rock Density',
                default: 0.8,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Crater Size',
                default: 1.0,
                min: 0.5,
                max: 2.0
            },
            {
                name: 'Regolith Noise',
                default: 0.6,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Gray Tint',
                default: 0.5,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Roughness',
                default: 0.7,
                min: 0.0,
                max: 1.0
            },
        ],
        fragUniforms: `
      uniform float u_rockDensity;
      uniform float u_craterSize;
      uniform float u_regolithNoise;
      uniform float u_grayTint;
      uniform float u_roughness;
    `,
        fragBody: `
      vec2 uv = vUv;
      float rock = snoise(vec3(uv * 10.0, 0.0)) * u_rockDensity;
      rock += snoise(vec3(uv * 25.0, 1.0)) * 0.3;
      rock = rock * 0.5 + 0.5;

      float crater = 1.0;
      for (int i = 0; i < 4; i++) {
        vec2 center = vec2(
          hash(vec2(uv.x + float(i) * 0.4, float(i))),
          hash(vec2(uv.y - float(i), float(i) * 2.0))
        ) * 0.8 + 0.1;
        float dist = length(uv - center) * u_craterSize;
        float ring = abs(dist - 0.15) < 0.02 ? 0.2 : 0.0;
        crater = min(crater, 1.0 - ring);
      }

      float regolith = snoise(vec3(uv * 40.0, 2.0)) * u_regolithNoise;
      regolith = regolith * 0.5 + 0.5;
      float surface = (rock * 0.6 + regolith * 0.4) * crater;
      surface = mix(surface, surface * 0.8 + 0.2, u_roughness);
      vec3 col = mix(vec3(0.5, 0.5, 0.5), vec3(0.7, 0.7, 0.7), u_grayTint) * surface;
      col += regolith * 0.1;
      gl_FragColor = vec4(col, 1.0);
    `
    }],
    "Glass & Transmission": [{
        id: "frosted_glass",
        name: "Frosted Glass",
        category: "Glass & Transmission",
        kind: "shader",
        swatch: "#E0FFFF",
        flags: {
            isGlass: true
        },
        params: [{
                name: 'Frost Roughness',
                default: 0.7,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Scatter Strength',
                default: 0.5,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Tint',
                default: 0.0,
                min: 0.0,
                max: 1.0,
                isColor: true
            },
            {
                name: 'Speckle Size',
                default: 0.3,
                min: 0.1,
                max: 1.0
            },
            {
                name: 'Opacity',
                default: 0.6,
                min: 0.0,
                max: 1.0
            },
        ],
        fragUniforms: `
      uniform float u_frostRoughness;
      uniform float u_scatterStrength;
      uniform vec3 u_tint;
      uniform float u_speckleSize;
      uniform float u_opacity;
    `,
        fragBody: `
      vec2 uv = vUv;
      float noise = snoise(vec3(uv * 50.0 * u_speckleSize, 0.0)) * u_frostRoughness;
      noise += snoise(vec3(uv * 30.0, 1.0)) * 0.3;
      float scatter = snoise(vec3(uv * 20.0, 2.0)) * u_scatterStrength;
      float alpha = mix(0.3, 0.9, u_opacity) * (noise * 0.7 + 0.3);
      vec3 col = mix(vec3(0.9, 0.95, 1.0), u_tint, 0.3) + scatter * 0.2;
      gl_FragColor = vec4(col, alpha);
    `
    }],
    "Underwater Life": [{
        id: "jellyfish",
        name: "Jellyfish",
        category: "Underwater Life",
        kind: "shader",
        swatch: "#E6E6FA",
        flags: {
            isAdditive: true
        },
        params: [{
                name: 'Tentacle Count',
                default: 8,
                min: 4,
                max: 16,
                step: 1
            },
            {
                name: 'Waviness',
                default: 0.6,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Translucency',
                default: 0.7,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Color Cycle',
                default: 0.5,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Glow',
                default: 0.4,
                min: 0.0,
                max: 1.0
            },
        ],
        fragUniforms: `
      uniform int u_tentacleCount;
      uniform float u_waviness;
      uniform float u_translucency;
      uniform float u_colorCycle;
      uniform float u_glow;
    `,
        fragBody: `
      vec2 uv = vUv - 0.5;
      float r = length(uv);
      float a = atan(uv.y, uv.x);
      float tentacleCount = float(u_tentacleCount);
      float tentacle = 1.0 - abs(sin(a * tentacleCount + uTime * 0.5) * 0.5 + 0.5);
      tentacle = smoothstep(0.0, 0.1, tentacle);
      float wave = sin(a * 4.0 + r * 20.0 - uTime * 3.0 * u_waviness) * 0.5 + 0.5;
      float opacity = tentacle * wave * u_translucency;
      float bell = smoothstep(0.5, 0.4, r) * 0.6;
      float edge = smoothstep(0.0, 0.1, r) * smoothstep(0.45, 0.5, r);
      float jelly = mix(opacity, bell, 0.6) * edge;
      jelly += u_glow * exp(-r * 5.0);
      vec3 col = hsv2rgb(vec3(fract(a / 6.28318 + uTime * 0.1 * u_colorCycle), 0.6, 1.0));
      col *= jelly;
      gl_FragColor = vec4(col, jelly);
    `
    }],
};

// Flat array (same order as categories above)
const SHADER_PRESETS = [{
        id: "frosted",
        name: "Frosted glass",
        category: "Realistic",
        kind: "physical",
        swatch: ['#eaf4f7', '#b9cdd4'],
        params: [{
                key: 'color',
                label: 'Tint',
                type: 'color',
                default: '#eef6f8',
                apply: (m, v) => m.color.set(v)
            },
            {
                key: 'roughness',
                label: 'Frost',
                type: 'range',
                min: 0.1,
                max: 1,
                step: 0.01,
                default: 0.55,
                apply: (m, v) => {
                    m.roughness = v;
                }
            },
        ],
        make: "() => new THREE.MeshPhysicalMaterial({ transmission: 1, thickness: 0.9, roughness: 0.55, metalness: 0, ior: 1.4, transparent: true, side: THREE.DoubleSide, envMapIntensity: 1 })"
    },
    {
        id: "chrome",
        name: "Chrome",
        category: "Realistic",
        kind: "physical",
        swatch: ['#f4f6fa', '#9aa2ad'],
        params: [{
                key: 'color',
                label: 'Tint',
                type: 'color',
                default: '#f4f6fa',
                apply: (m, v) => m.color.set(v)
            },
            {
                key: 'roughness',
                label: 'Polish',
                type: 'range',
                min: 0,
                max: 0.5,
                step: 0.01,
                default: 0.03,
                apply: (m, v) => {
                    m.roughness = v;
                }
            },
        ],
        make: "() => new THREE.MeshPhysicalMaterial({ metalness: 1, roughness: 0.03, color: 0xf4f6fa, envMapIntensity: 1.2, side: THREE.DoubleSide })"
    },
    {
        id: "brushed",
        name: "Brushed steel",
        category: "Realistic",
        kind: "physical",
        swatch: ['#c6cad0', '#7f858d'],
        params: [{
                key: 'color',
                label: 'Tint',
                type: 'color',
                default: '#b8bcc4',
                apply: (m, v) => m.color.set(v)
            },
            {
                key: 'roughness',
                label: 'Grain',
                type: 'range',
                min: 0.1,
                max: 0.8,
                step: 0.01,
                default: 0.38,
                apply: (m, v) => {
                    m.roughness = v;
                }
            },
        ],
        make: "() => new THREE.MeshPhysicalMaterial({ metalness: 1, roughness: 0.38, color: 0xb8bcc4, envMapIntensity: 1, side: THREE.DoubleSide })"
    },
    {
        id: "gold",
        name: "Gold",
        category: "Realistic",
        kind: "physical",
        swatch: ['#ffe08a', '#c8912f'],
        params: [{
                key: 'color',
                label: 'Metal',
                type: 'color',
                default: '#ffce5a',
                apply: (m, v) => m.color.set(v)
            },
            {
                key: 'roughness',
                label: 'Polish',
                type: 'range',
                min: 0,
                max: 0.7,
                step: 0.01,
                default: 0.18,
                apply: (m, v) => {
                    m.roughness = v;
                }
            },
        ],
        make: "() => new THREE.MeshPhysicalMaterial({ metalness: 1, roughness: 0.18, color: 0xffce5a, envMapIntensity: 1.2, side: THREE.DoubleSide })"
    },
    {
        id: "copper",
        name: "Copper",
        category: "Realistic",
        kind: "physical",
        swatch: ['#f0a878', '#a8582e'],
        params: [{
                key: 'color',
                label: 'Metal',
                type: 'color',
                default: '#d98b5a',
                apply: (m, v) => m.color.set(v)
            },
            {
                key: 'roughness',
                label: 'Polish',
                type: 'range',
                min: 0,
                max: 0.7,
                step: 0.01,
                default: 0.28,
                apply: (m, v) => {
                    m.roughness = v;
                }
            },
        ],
        make: "() => new THREE.MeshPhysicalMaterial({ metalness: 1, roughness: 0.28, color: 0xd98b5a, envMapIntensity: 1.1, side: THREE.DoubleSide })"
    },
    {
        id: "ceramic",
        name: "Glazed ceramic",
        category: "Realistic",
        kind: "physical",
        swatch: ['#f2f4f7', '#c3ccd4'],
        params: [{
                key: 'color',
                label: 'Glaze',
                type: 'color',
                default: '#eef1f4',
                apply: (m, v) => m.color.set(v)
            },
            {
                key: 'roughness',
                label: 'Matte',
                type: 'range',
                min: 0.05,
                max: 0.9,
                step: 0.01,
                default: 0.28,
                apply: (m, v) => {
                    m.roughness = v;
                }
            },
        ],
        make: "() => new THREE.MeshPhysicalMaterial({ metalness: 0, roughness: 0.28, clearcoat: 1, clearcoatRoughness: 0.08, color: 0xeef1f4, envMapIntensity: 1, side: THREE.DoubleSide })"
    },
    {
        id: "pearl",
        name: "Pearl / car paint",
        category: "Realistic",
        kind: "physical",
        swatch: ['#f7d7ee', '#8fb6e0'],
        params: [{
                key: 'color',
                label: 'Base',
                type: 'color',
                default: '#c05a9a',
                apply: (m, v) => m.color.set(v)
            },
            {
                key: 'iridescence',
                label: 'Shift',
                type: 'range',
                min: 0,
                max: 1,
                step: 0.01,
                default: 1,
                apply: (m, v) => {
                    m.iridescence = v;
                }
            },
        ],
        make: "() => new THREE.MeshPhysicalMaterial({ metalness: 0.6, roughness: 0.25, clearcoat: 1, clearcoatRoughness: 0.05, iridescence: 1, iridescenceIOR: 1.6, color: 0xc05a9a, envMapIntensity: 1.1, side: THREE.DoubleSide })"
    },
    {
        id: "velvet",
        name: "Velvet",
        category: "Realistic",
        kind: "physical",
        swatch: ['#7a2740', '#3a1020'],
        params: [{
                key: 'color',
                label: 'Cloth',
                type: 'color',
                default: '#5a1f2a',
                apply: (m, v) => m.color.set(v)
            },
            {
                key: 'sheen',
                label: 'Sheen',
                type: 'color',
                default: '#d98a9a',
                apply: (m, v) => m.sheenColor.set(v)
            },
        ],
        make: "() => new THREE.MeshPhysicalMaterial({ metalness: 0, roughness: 0.9, sheen: 1, sheenRoughness: 0.5, sheenColor: new THREE.Color(0xd98a9a), color: 0x5a1f2a, envMapIntensity: 0.6, side: THREE.DoubleSide })"
    },
    {
        id: "clay",
        name: "Matte clay",
        category: "Realistic",
        kind: "physical",
        swatch: ['#d1a184', '#8a5a3e'],
        params: [{
            key: 'color',
            label: 'Clay',
            type: 'color',
            default: '#c88f6a',
            apply: (m, v) => m.color.set(v)
        }, ],
        make: "() => new THREE.MeshPhysicalMaterial({ metalness: 0, roughness: 1, color: 0xc88f6a, envMapIntensity: 0.5, side: THREE.DoubleSide })"
    },
    {
        id: "gelatinouscube",
        name: "Gelatinous Cube",
        category: "Liquids & potions",
        kind: "shader",
        swatch: ['#8affb0', '#0a4a2a'],
        gravity: true,
        transparent: true,
        additive: true,
        depthWrite: false,
        fragBody: `\`
        float t = u_time * u_speed; float wobble = sin(rad * 4.0 + t * 2.0) * 0.2; float gel = fbm(vObjPos * 3.0 + t * 0.3 + wobble); float opacity = smoothstep(0.3, 0.7, gel) * 0.9; col = hsv2rgb(vec3(0.3 + gel * 0.2, 0.8, 1.0)) * opacity * 1.5; alpha = opacity;
      \``,
        params: [{
            key: 'speed',
            label: 'Wiggle',
            type: 'range',
            min: 0,
            max: 3,
            step: 0.05,
            default: 1.2
        }]
    },
    {
        id: "heartbeat",
        name: "Heartbeat",
        category: "Organic & fleshy",
        kind: "shader",
        swatch: ['#ff4a5e', '#3a0510'],
        params: [{
                key: 'base',
                label: 'Flesh',
                type: 'color',
                default: '#40141c'
            },
            {
                key: 'color',
                label: 'Pulse',
                type: 'color',
                default: '#ff3a52'
            },
            {
                key: 'scale',
                label: 'Veins',
                type: 'range',
                min: 1,
                max: 10,
                step: 0.1,
                default: 4
            },
            {
                key: 'speed',
                label: 'BPM',
                type: 'range',
                min: 0.2,
                max: 3,
                step: 0.05,
                default: 1
            },
        ],
        fragUniforms: "uniform vec3 u_base; uniform vec3 u_color; uniform float u_scale;",
        fragBody: `
      float tb = fract(u_time * u_speed * 0.85);
      float beat = exp(-pow((tb - 0.10) * 14.0, 2.0)) + 0.55 * exp(-pow((tb - 0.32) * 14.0, 2.0));
      float vein = pow(1.0 - abs(snoise(vObjPos * u_scale)), 7.0);
      col = u_base * (0.25 + 0.4 * diff);
      col += u_color * vein * (0.4 + beat * 2.2);
      col += u_color * pow(fres, 2.2) * (0.3 + beat * 1.4);
    `
    },
    {
        id: "matcap",
        name: "Sculpt matcap",
        category: "Stylized & toon",
        kind: "shader",
        swatch: ['#b7bcc4', '#5c626b'],
        params: [{
                key: 'color',
                label: 'Body',
                type: 'color',
                default: '#8a8f98'
            },
            {
                key: 'spec',
                label: 'Highlight',
                type: 'color',
                default: '#ffffff'
            },
        ],
        fragUniforms: "uniform vec3 u_color; uniform vec3 u_spec;",
        fragBody: `
      vec3 vn = normalize(vViewNormal);
      vec2 muv = vn.xy*0.5 + 0.5;
      float d = distance(muv, vec2(0.68, 0.72));
      float spec = smoothstep(0.45, 0.0, d);
      float amb = 0.25 + 0.7*muv.y;
      col = u_color * amb + spec * u_spec * 1.2;
      col *= (0.6 + 0.4*(1.0 - pow(fres, 3.0)));
    `
    },
    {
        id: "iridescent",
        name: "Iridescent",
        category: "Stylized & toon",
        kind: "shader",
        swatch: ['#7affd0', '#b06be6'],
        params: [{
                key: 'color',
                label: 'Base',
                type: 'color',
                default: '#20232a'
            },
            {
                key: 'bands',
                label: 'Bands',
                type: 'range',
                min: 0.5,
                max: 4,
                step: 0.05,
                default: 1.6
            },
            {
                key: 'intensity',
                label: 'Sheen',
                type: 'range',
                min: 0,
                max: 1.5,
                step: 0.02,
                default: 1
            },
            {
                key: 'speed',
                label: 'Drift',
                type: 'range',
                min: 0,
                max: 3,
                step: 0.05,
                default: 1
            },
        ],
        fragUniforms: "uniform vec3 u_color; uniform float u_bands; uniform float u_intensity;",
        fragBody: `
      float fp = pow(fres, 1.4);
      float hue = fract(u_time*0.03*u_speed + fp*u_bands + N.y*0.25 + vWorldPos.y*0.1);
      vec3 rain = hsv2rgb(vec3(hue, 0.85, 1.0));
      col = mix(u_color*diff, rain, clamp(fp*u_intensity, 0.0, 1.0));
      col += pow(fp, 4.0)*0.5;
    `
    },
    {
        id: "normals",
        name: "Normals",
        category: "Stylized & toon",
        kind: "shader",
        swatch: ['#8a8fff', '#5affa0'],
        params: [],
        fragUniforms: "",
        fragBody: `
      col = normalize(vWorldNormal)*0.5 + 0.5;
    `
    },
    {
        id: "topo",
        name: "Topographic",
        category: "Stylized & toon",
        kind: "shader",
        swatch: ['#e8dcc4', '#5a4a2a'],
        params: [{
                key: 'base',
                label: 'Paper',
                type: 'color',
                default: '#efe6d2'
            },
            {
                key: 'color',
                label: 'Ink',
                type: 'color',
                default: '#4a3a20'
            },
            {
                key: 'scale',
                label: 'Terrain',
                type: 'range',
                min: 0.5,
                max: 8,
                step: 0.1,
                default: 2.5
            },
            {
                key: 'bands',
                label: 'Contours',
                type: 'range',
                min: 3,
                max: 30,
                step: 1,
                default: 12
            },
            {
                key: 'speed',
                label: 'Shift',
                type: 'range',
                min: 0,
                max: 3,
                step: 0.05,
                default: 0.5
            },
        ],
        fragUniforms: "uniform vec3 u_base; uniform vec3 u_color; uniform float u_scale; uniform float u_bands;",
        fragBody: `
      float f = fbm(vObjPos * u_scale + vec3(0.0, u_time * u_speed * 0.06, 0.0));
      float c = fract(f * u_bands);
      float line = smoothstep(0.10, 0.02, min(c, 1.0 - c));
      col = mix(u_base * (0.55 + 0.45 * diff), u_color, line);
      col += u_color * pow(fres, 3.0) * 0.15;
    `
    },
    {
        id: "voxelize",
        name: "Voxelizer",
        category: "Stylized & toon",
        kind: "shader",
        swatch: ['#ff5500', '#0000ff'],
        gravity: true,
        fragBody: `\`
        float voxelSize = 0.1;
        vec3 voxel = floor(vObjPos / voxelSize) * voxelSize;
        float noise = snoise(voxel * 3.0 + u_time * u_speed);
        float edge = step(0.5, fbm(voxel * 10.0));
        vec3 c1 = hsv2rgb(vec3(noise * 0.5 + 0.2, 1.0, 1.0));
        vec3 c2 = hsv2rgb(vec3(noise * 0.5 + 0.7, 1.0, 1.0));
        col = mix(c1, c2, edge) * (0.5 + diff * 0.5);
        col += vec3(1.0) * pow(fres, 5.0) * 1.5;
      \``
    },
    {
        id: "nanoswarm",
        name: "Nano-swarm",
        category: "Sci-fi & cyberpunk",
        kind: "shader",
        swatch: ['#ff5500', '#111111'],
        params: [{
            key: 'speed',
            label: 'Move',
            type: 'range',
            min: 0,
            max: 4,
            step: 0.05,
            default: 2
        }, ],
        fragBody: `
      float t = u_time * u_speed;
      float s1 = step(0.85, snoise(vObjPos * 10.0 + vec3(t, 0.0, 0.0)));
      float s2 = step(0.85, snoise(vObjPos * 12.0 - vec3(0.0, t, 0.0)));
      float s3 = step(0.85, snoise(vObjPos * 14.0 + vec3(0.0, 0.0, t)));
      col = vec3(0.1) * diff;
      col += vec3(1.0, 0.3, 0.0) * (s1 + s2 + s3);
      col += vec3(1.0, 0.5, 0.0) * pow(fres, 3.0) * 1.5;
    `
    },
    {
        id: "fiberoptic",
        name: "Fiber Optic",
        category: "Sci-fi & cyberpunk",
        kind: "shader",
        swatch: ['#00ffcc', '#ff00ff'],
        gravity: true,
        transparent: true,
        additive: true,
        depthWrite: false,
        fragBody: `\`
        float t = u_time * u_speed;
        float hNorm = (h - u_levMin) / (u_levMax - u_levMin);
        float s = sin(h * 30.0 + t * 5.0 + rad * 10.0) * 0.5 + 0.5;
        float bright = smoothstep(0.85, 1.0, s);
        vec3 c1 = hsv2rgb(vec3(hNorm * 0.8, 1.0, 1.0));
        vec3 c2 = hsv2rgb(vec3(hNorm * 0.8 + 0.5, 1.0, 1.0));
        col = mix(c1, c2, bright) * bright * 3.0;
        col += vec3(1.0) * pow(fres, 3.0) * 1.5;
        alpha = bright * 0.9;
      \``
    },
    {
        id: "echolocation",
        name: "Echolocation",
        category: "Sci-fi & cyberpunk",
        kind: "shader",
        swatch: ['#00ff00', '#000000'],
        gravity: true,
        transparent: true,
        additive: true,
        depthWrite: false,
        fragBody: `\`
        float t = u_time * u_speed;
        float dist = rad;
        float wave = sin(dist * 30.0 - t * 8.0) * 0.5 + 0.5;
        float ring = smoothstep(0.45, 0.6, wave) * smoothstep(0.9, 0.8, dist);
        col = vec3(0.2, 1.0, 0.2) * ring * 2.0;
        col += vec3(0.8) * pow(fres, 4.0) * 0.5;
        alpha = ring * 0.8;
      \``
    },
    {
        id: "liquidcrystal",
        name: "Liquid Crystal",
        category: "Sci-fi & cyberpunk",
        kind: "shader",
        swatch: ['#ff80c0', '#80ff80'],
        gravity: true,
        transparent: true,
        depthWrite: false,
        fragBody: `\`
        float t = u_time * u_speed;
        float hNorm = (h - u_levMin) / (u_levMax - u_levMin);
        float n = fbm(vObjPos * 3.0 + t * 0.5);
        float swirl = sin(rad * 5.0 + h * 10.0 + t * 2.0 + n * 5.0) * 0.5 + 0.5;
        vec3 c1 = hsv2rgb(vec3(hNorm * 0.7 + n * 0.2, 1.0, 1.0));
        vec3 c2 = hsv2rgb(vec3(hNorm * 0.7 + 0.5 + n * 0.2, 1.0, 1.0));
        col = mix(c1, c2, swirl) * (0.4 + diff * 0.6);
        col += vec3(1.0) * pow(fres, 3.0) * 1.2;
        alpha = 0.7 + swirl * 0.3;
      \``
    },
    {
        id: "neon_circuits",
        name: "Neon Circuits",
        category: "Sci-fi & cyberpunk",
        kind: "shader",
        swatch: ["#0d0221", "#00ffff"],
        transparent: false,
        additive: false,
        depthWrite: true,
        params: [{
                key: "circuitColor",
                label: "Circuit Color",
                type: "color",
                default: "#00ffff"
            },
            {
                key: "baseDark",
                label: "Dark Base",
                type: "color",
                default: "#050510"
            },
            {
                key: "density",
                label: "Density",
                type: "float",
                default: 0.5,
                min: 0.1,
                max: 2.0
            },
            {
                key: "pulseSpeed",
                label: "Pulse Speed",
                type: "float",
                default: 1.0,
                min: 0.1,
                max: 5.0
            },
            {
                key: "glowFalloff",
                label: "Glow Falloff",
                type: "float",
                default: 1.0,
                min: 0.5,
                max: 3.0
            },
            {
                key: "sparkle",
                label: "Sparkle",
                type: "float",
                default: 0.5,
                min: 0.0,
                max: 1.0
            }
        ],
        fragUniforms: `
      uniform vec3 u_circuitColor;
      uniform vec3 u_baseDark;
      uniform float u_density;
      uniform float u_pulseSpeed;
      uniform float u_glowFalloff;
      uniform float u_sparkle;
    `,
        fragBody: `
      vec3 pos = vWorldPos * 0.5;
      vec2 uv = pos.xy * u_density;

      float trace = 0.0;
      float node = 0.0;
      float t = u_time * u_pulseSpeed;

      for (int i = 0; i < 4; i++) {
        float fi = float(i) + 1.0;
        vec2 offset = vec2(snoise(vec3(fi * 13.7, 0.0, 0.0)), snoise(vec3(fi * 17.3, 0.0, 0.0))) * 2.0;
        vec2 p = uv + offset + vec2(snoise(vec3(uv, fi * 23.1 + t * 0.1)), snoise(vec3(uv, fi * 29.7 + t * 0.13)));
        float d = abs(sin(p.x * 3.14159) * cos(p.y * 3.14159));
        d = 1.0 - smoothstep(0.0, 0.05, d) * smoothstep(0.15, 0.12, d);
        trace += d * 0.25;
        float nd = length(fract(p * 2.0) - 0.5);
        node += (1.0 - smoothstep(0.05, 0.15, nd)) * (0.5 + 0.5 * sin(t * 5.0 + fi));
      }
      trace = clamp(trace, 0.0, 1.0);
      node = clamp(node, 0.0, 1.0) * u_sparkle;

      vec3 glow = u_circuitColor * (trace * 1.5 + node * 2.0) * u_glowFalloff;
      float diffuseLit = diff * 0.2 + 0.8;
      col = mix(u_baseDark, glow, trace + node) * diffuseLit;
      col += glow * 0.2;
      alpha = 1.0;
    `
    },
    {
        id: "hologram",
        name: "Hologram",
        category: "Sci-fi & cyberpunk",
        kind: "shader",
        swatch: ["#00ffff", "#ff00ff"],
        transparent: true,
        additive: true,
        depthWrite: false,
        params: [{
                key: "baseTint",
                label: "Base Tint",
                type: "color",
                default: "#88ccff"
            },
            {
                key: "shimmerColor",
                label: "Shimmer Color",
                type: "color",
                default: "#ffffff"
            },
            {
                key: "scanlineCnt",
                label: "Scanline Count",
                type: "float",
                default: 30.0,
                min: 5.0,
                max: 80.0
            },
            {
                key: "interferenceScale",
                label: "Interference Scale",
                type: "float",
                default: 10.0,
                min: 1.0,
                max: 30.0
            },
            {
                key: "opacity",
                label: "Opacity",
                type: "float",
                default: 0.6,
                min: 0.1,
                max: 1.0
            },
            {
                key: "flickerSpd",
                label: "Flicker Speed",
                type: "float",
                default: 1.0,
                min: 0.1,
                max: 5.0
            }
        ],
        fragUniforms: `
      uniform vec3 u_baseTint;
      uniform vec3 u_shimmerColor;
      uniform float u_scanlineCnt;
      uniform float u_interferenceScale;
      uniform float u_opacity;
      uniform float u_flickerSpd;
    `,
        fragBody: `
      vec2 uv = vWorldPos.xy * u_interferenceScale;
      float t = u_time * u_flickerSpd;
      float scanline = sin(vWorldPos.y * u_scanlineCnt * 3.14159) * 0.5 + 0.5;
      scanline = scanline * 0.7 + 0.3;
      float interference = sin(uv.x * 8.0 + t) * cos(uv.y * 6.0 - t * 0.7);
      vec3 holoCol = u_baseTint * scanline;
      holoCol += u_shimmerColor * interference * 0.3;
      float rOffset = snoise(vec3(vWorldPos.xy, t)) * 0.02;
      col = holoCol * 0.8;
      col += holoCol * 0.2 * snoise(vec3(vWorldPos.xy * 4.0, t * 0.5));
      alpha = u_opacity * (0.6 + 0.4 * scanline) * clamp(interference + 0.5, 0.0, 1.0);
    `
    },
    {
        id: "glitch",
        name: "Glitch / corrupt",
        category: "Glitch & digital",
        kind: "shader",
        swatch: ['#ff2a6a', '#0a2a3a'],
        gravity: true,
        params: [{
                key: 'color',
                label: 'Base',
                type: 'color',
                default: '#1a7a8a'
            },
            {
                key: 'rows',
                label: 'Rows',
                type: 'range',
                min: 8,
                max: 80,
                step: 1,
                default: 32
            },
            {
                key: 'speed',
                label: 'Corrupt',
                type: 'range',
                min: 0,
                max: 3,
                step: 0.05,
                default: 1
            },
        ],
        fragUniforms: "uniform vec3 u_color; uniform float u_rows;\\nfloat hashf(float x){ return fract(sin(x * 127.1) * 43758.5453); }",
        fragBody: `\`
      float tq = floor(u_time * u_speed * 9.0);
      float row = floor(hn * u_rows);
      float rr = hashf(row * 1.37 + tq);
      vec3 base = u_color * (0.35 + 0.6 * diff);
      float rim = pow(fres, 2.0);
      base += vec3(rim * 0.8, 0.0, 0.0) * hashf(tq + 3.0) + vec3(0.0, 0.0, rim * 0.8) * hashf(tq + 7.0);
      if (rr > 0.86) base = vec3(1.0) - base;
      if (hashf(row + tq + 11.0) > 0.93) base *= 0.15;
      col = base * (0.85 + 0.15 * sin(hn * 500.0 + u_time * 20.0));
    \``
    },
    {
        id: "coderain",
        name: "Code rain",
        category: "Glitch & digital",
        kind: "shader",
        swatch: ['#3aff6a', '#020a04'],
        gravity: true,
        params: [{
                key: 'color',
                label: 'Glyphs',
                type: 'color',
                default: '#2aff5e'
            },
            {
                key: 'cols',
                label: 'Columns',
                type: 'range',
                min: 8,
                max: 80,
                step: 1,
                default: 36
            },
            {
                key: 'speed',
                label: 'Rain',
                type: 'range',
                min: 0,
                max: 3,
                step: 0.05,
                default: 1
            },
        ],
        fragUniforms: "uniform vec3 u_color; uniform float u_cols;\\nfloat hashf(float x){ return fract(sin(x * 127.1) * 43758.5453); }",
        fragBody: `\`
      float colId = floor((ang / 6.2831 + 0.5) * u_cols);
      float sp = 0.35 + hashf(colId) * 1.1;
      float v = fract(hn * (0.8 + hashf(colId + 5.0) * 0.6) + u_time * u_speed * sp * 0.35);
      float trail = pow(v, 5.0);
      float cell = floor(hn * 30.0);
      float glyph = 0.35 + 0.65 * step(0.3, hashf(colId * 7.3 + cell * 1.7 + floor(u_time * u_speed * 6.0) * step(0.93, hashf(cell + colId))));
      col = vec3(0.01, 0.03, 0.015);
      col += u_color * trail * glyph * 1.8 + vec3(0.85, 1.0, 0.9) * smoothstep(0.97, 1.0, v) * glyph;
      col += u_color * pow(fres, 2.5) * 0.25;
    \``
    },
    {
        id: "voidrift",
        name: "Void rift",
        category: "Glitch & digital",
        kind: "shader",
        swatch: ['#ff00ff', '#000000'],
        params: [{
            key: 'speed',
            label: 'Tear',
            type: 'range',
            min: 0,
            max: 3,
            step: 0.05,
            default: 1
        }, ],
        fragBody: `\`
      float t = u_time * u_speed;
      float rift = abs(sin(ang * 2.0 + hn * 5.0 + t * 2.0));
      float edge = smoothstep(0.0, 0.05, rift);
      vec3 worldCol = normalize(vWorldPos) * 0.5 + 0.5;
      vec3 voidCol = vec3(0.0);
      col = mix(worldCol * diff, voidCol, edge);
      col += vec3(1.0, 0.0, 1.0) * smoothstep(0.05, 0.0, rift) * 6.0; // Blinding neon rift edges
      col += vec3(0.0, 1.0, 1.0) * smoothstep(0.02, 0.0, rift) * 4.0;
    \``
    },
    {
        id: "missingtex",
        name: "Error 404",
        category: "Glitch & digital",
        kind: "shader",
        swatch: ['#ff00ff', '#000000'],
        params: [{
            key: 'speed',
            label: 'Glitch',
            type: 'range',
            min: 0,
            max: 4,
            step: 0.05,
            default: 1.5
        }, ],
        fragBody: `\`
      float t = u_time * u_speed;
      vec3 p = vObjPos * 5.0;
      p += vec3(fbm(p + t), fbm(p - t), fbm(p + t * 0.5)) * 2.0;
      float checker = mod(floor(p.x) + floor(p.y) + floor(p.z), 2.0);
      vec3 c1 = vec3(0.0, 0.0, 0.0);
      vec3 c2 = vec3(1.0, 0.0, 1.0);
      col = mix(c1, c2, checker) * (0.5 + diff * 0.5);
      col += vec3(1.0) * pow(fres, 4.0) * 2.0;
    \``
    },
    {
        id: "timeslicer",
        name: "Time Slicer",
        category: "Glitch & digital",
        kind: "shader",
        swatch: ['#ff0000', '#00ff00'],
        gravity: true,
        fragBody: `\`
        float t = u_time * u_speed;
        float slice = sin(h * 15.0 - t * 5.0) * 0.5 + 0.5;
        float thickness = abs(fract(h * 15.0 - t * 5.0) - 0.5) * 2.0;
        float sliceEdge = smoothstep(0.8, 0.9, thickness) * smoothstep(0.95, 0.9, thickness);
        float glow = exp(-thickness * 10.0);
        vec3 c1 = vec3(1.0, 0.2, 0.1) * glow;
        vec3 c2 = vec3(0.1, 1.0, 0.2) * sliceEdge;
        col = (c1 + c2) * 1.5 * (0.5 + diff * 0.5);
        col += vec3(1.0) * pow(fres, 3.0) * 1.2;
      \``
    },
    {
        id: "sonar",
        name: "Sonar ping",
        category: "Cosmic & radiant",
        kind: "shader",
        swatch: ['#3aff8a', '#03251a'],
        transparent: true,
        additive: true,
        depthWrite: false,
        gravity: true,
        params: [{
                key: 'color',
                label: 'Ping',
                type: 'color',
                default: '#3aff8a'
            },
            {
                key: 'speed',
                label: 'Sweep',
                type: 'range',
                min: 0,
                max: 3,
                step: 0.05,
                default: 1
            },
        ],
        fragUniforms: "uniform vec3 u_color;",
        fragBody: `\`
      float ping = fract(length(vObjPos) / E * 0.85 - u_time * u_speed * 0.45);
      float ring = pow(1.0 - ping, 9.0);
      float grid = pow(0.5 + 0.5 * sin(hn * 60.0), 12.0) * 0.15 + pow(0.5 + 0.5 * sin(ang * 24.0), 12.0) * 0.12;
      col = u_color * (0.04 + ring * 1.8) + u_color * grid + u_color * pow(fres, 2.0) * 0.35;
      alpha = clamp(0.22 + ring * 0.78, 0.0, 1.0);
    \``
    },
    {
        id: "radioactive",
        name: "Radioactive",
        category: "Cosmic & radiant",
        kind: "shader",
        swatch: ['#b6ff2a', '#1a2a05'],
        params: [{
                key: 'base',
                label: 'Sludge',
                type: 'color',
                default: '#1e2a10'
            },
            {
                key: 'color',
                label: 'Isotope',
                type: 'color',
                default: '#a8ff1e'
            },
            {
                key: 'scale',
                label: 'Cracks',
                type: 'range',
                min: 1,
                max: 12,
                step: 0.1,
                default: 5
            },
            {
                key: 'speed',
                label: 'Decay',
                type: 'range',
                min: 0,
                max: 3,
                step: 0.05,
                default: 1
            },
        ],
        fragUniforms: "uniform vec3 u_base; uniform vec3 u_color; uniform float u_scale;",
        fragBody: `
      float crack = pow(1.0 - abs(snoise(vObjPos * u_scale)), 9.0);
      float flick = 0.75 + 0.25 * sin(u_time * 13.0 * u_speed + snoise(vObjPos * 2.0 + u_time * 0.5) * 4.0);
      col = u_base * (0.3 + 0.5 * diff);
      col += u_color * crack * flick * 2.4;
      col += u_color * pow(fres, 2.5) * flick * 0.9;
    `
    },
    {
        id: "portal",
        name: "Portal vortex",
        category: "Cosmic & radiant",
        kind: "shader",
        swatch: ['#8a3aff', '#ff8a3a'],
        gravity: true,
        params: [{
                key: 'color',
                label: 'Void',
                type: 'color',
                default: '#180830'
            },
            {
                key: 'hue',
                label: 'Hue',
                type: 'range',
                min: 0,
                max: 1,
                step: 0.01,
                default: 0.75
            },
            {
                key: 'arms',
                label: 'Arms',
                type: 'range',
                min: 1,
                max: 8,
                step: 1,
                default: 3
            },
            {
                key: 'speed',
                label: 'Spin',
                type: 'range',
                min: 0,
                max: 3,
                step: 0.05,
                default: 1
            },
        ],
        fragUniforms: "uniform vec3 u_color; uniform float u_hue; uniform float u_arms;",
        fragBody: `\`
      float sw = sin(ang * u_arms + rad * 16.0 - u_time * u_speed * 3.2 + fbm(vObjPos * 2.0) * 3.0);
      float sp = pow(0.5 + 0.5 * sw, 3.0);
      vec3 rain = hsv2rgb(vec3(fract(u_hue + rad * 0.35 - u_time * u_speed * 0.05), 0.85, 1.0));
      col = mix(u_color, rain * 2.0, sp);
      col += rain * pow(fres, 2.0) * 0.8;
    \``
    },
    {
        id: "soulfire",
        name: "Soul fire",
        category: "Cosmic & radiant",
        kind: "shader",
        swatch: ['#5ad2ff', '#0a1a4a'],
        transparent: true,
        additive: true,
        depthWrite: false,
        gravity: true,
        params: [{
                key: 'color',
                label: 'Flame',
                type: 'color',
                default: '#3ab8ff'
            },
            {
                key: 'scale',
                label: 'Licks',
                type: 'range',
                min: 1,
                max: 10,
                step: 0.1,
                default: 4
            },
            {
                key: 'speed',
                label: 'Burn',
                type: 'range',
                min: 0,
                max: 3,
                step: 0.05,
                default: 1
            },
        ],
        fragUniforms: "uniform vec3 u_color; uniform float u_scale;",
        fragBody: `\`
      float n = fbm(vObjPos * u_scale - upA * (u_time * u_speed * 1.8));
      float flame = pow(clamp(n * 0.5 + 0.5, 0.0, 1.0), 2.2) * (0.35 + 0.85 * hn);
      vec3 fc = mix(u_color * 0.4, mix(u_color, vec3(1.0), 0.75), flame);
      col = fc * (flame * 2.4 + pow(fres, 1.6) * 1.2);
      alpha = clamp(flame * 1.3 + pow(fres, 1.8) * 0.7, 0.0, 1.0);
    \``
    },
    {
        id: "star",
        name: "Living star",
        category: "Cosmic & radiant",
        kind: "shader",
        swatch: ['#ffdf6a', '#c83205'],
        displaceParam: true,
        params: [{
                key: 'heat',
                label: 'Heat',
                type: 'range',
                min: 0.5,
                max: 2.5,
                step: 0.05,
                default: 1.2
            },
            {
                key: 'scale',
                label: 'Granules',
                type: 'range',
                min: 1,
                max: 8,
                step: 0.1,
                default: 3
            },
            {
                key: 'displace',
                label: 'Boil',
                type: 'range',
                min: 0,
                max: 0.2,
                step: 0.005,
                default: 0.04
            },
            {
                key: 'speed',
                label: 'Churn',
                type: 'range',
                min: 0,
                max: 3,
                step: 0.05,
                default: 1
            },
        ],
        fragUniforms: "uniform float u_heat; uniform float u_scale;",
        fragBody: `
      float g1 = fbm(vObjPos * u_scale + vec3(u_time * u_speed * 0.10));
      float g2 = fbm(vObjPos * u_scale * 2.7 - vec3(u_time * u_speed * 0.07));
      float gr = clamp(0.5 + 0.5 * (g1 * 0.65 + g2 * 0.35), 0.0, 1.0);
      col = mix(vec3(0.45, 0.05, 0.0), vec3(0.98, 0.45, 0.05), smoothstep(0.15, 0.55, gr));
      col = mix(col, vec3(1.0, 0.96, 0.82) * u_heat, smoothstep(0.55, 0.9, gr));
      col += vec3(1.0, 0.55, 0.1) * pow(fres, 2.0) * 1.6;
    `
    },
    {
        id: "blackhole",
        name: "Black hole",
        category: "Cosmic & radiant",
        kind: "shader",
        swatch: ['#ff8a3a', '#000008'],
        gravity: true,
        params: [{
                key: 'core',
                label: 'Horizon',
                type: 'range',
                min: 0,
                max: 0.8,
                step: 0.01,
                default: 0.35
            },
            {
                key: 'heat',
                label: 'Accretion',
                type: 'range',
                min: 0.3,
                max: 2.5,
                step: 0.05,
                default: 1.2
            },
            {
                key: 'speed',
                label: 'Spin',
                type: 'range',
                min: 0,
                max: 3,
                step: 0.05,
                default: 1
            },
        ],
        fragUniforms: "uniform float u_core; uniform float u_heat;",
        fragBody: `\`
      float disc = smoothstep(u_core, u_core + 0.18, fres);
      float swirl = sin(ang * 3.0 - pow(fres, 2.0) * 14.0 - u_time * u_speed * 2.6 + fbm(vObjPos * 3.0) * 2.0);
      vec3 hot = mix(vec3(1.0, 0.55, 0.1), vec3(0.4, 0.6, 1.0), 0.5 + 0.5 * swirl);
      col = hot * pow(fres, 3.5) * (1.6 + swirl * 0.4) * u_heat * disc;
    \``
    },
    {
        id: "aurora",
        name: "Aurora",
        category: "Cosmic & radiant",
        kind: "shader",
        swatch: ["#0b0b2b", "#00ff88"],
        transparent: false,
        additive: false,
        depthWrite: true,
        params: [{
                key: "auroraColor1",
                label: "Aurora Color 1",
                type: "color",
                default: "#00ff88"
            },
            {
                key: "auroraColor2",
                label: "Aurora Color 2",
                type: "color",
                default: "#0044ff"
            },
            {
                key: "waveScale",
                label: "Wave Scale",
                type: "float",
                default: 0.8,
                min: 0.1,
                max: 2.0
            },
            {
                key: "speed",
                label: "Speed",
                type: "float",
                default: 1.0,
                min: 0.1,
                max: 3.0
            },
            {
                key: "altitude",
                label: "Altitude",
                type: "float",
                default: 0.5,
                min: 0.0,
                max: 1.0
            },
            {
                key: "rayCount",
                label: "Ray Count",
                type: "float",
                default: 10.0,
                min: 2.0,
                max: 30.0
            }
        ],
        fragUniforms: `
      uniform vec3 u_auroraColor1;
      uniform vec3 u_auroraColor2;
      uniform float u_waveScale;
      uniform float u_speed;
      uniform float u_altitude;
      uniform float u_rayCount;
    `,
        fragBody: `
      vec2 uv = vWorldPos.xy * u_waveScale;
      float t = u_time * u_speed;
      float y = vWorldPos.y;
      float altMask = smoothstep(u_altitude - 0.3, u_altitude, y) * (1.0 - smoothstep(u_altitude + 0.3, u_altitude + 0.5, y));
      float wave = sin(uv.x * 2.0 + t * 0.5 + snoise(vec3(uv * 0.3, t * 0.2)) * 5.0) * 0.5 + 0.5;
      wave += sin(uv.x * 3.5 - t * 0.7 + snoise(vec3(uv * 0.25, t * 0.7)) * 3.0) * 0.3;
      float rays = 0.0;
      for (int i = 0; i < int(u_rayCount); i++) {
        float fi = float(i);
        float phase = fi * 0.73 + t * 0.13;
        float rx = sin(phase + y * 8.0) * 0.5 + 0.5;
        rays += 1.0 - smoothstep(0.0, 0.03, abs(uv.x - rx));
      }
      rays = clamp(rays / u_rayCount * 5.0, 0.0, 1.0);
      float intensity = wave * rays * altMask;
      vec3 auroraCol = mix(u_auroraColor1, u_auroraColor2, wave) * intensity;
      vec3 base = vec3(0.01, 0.01, 0.08);
      col = base + auroraCol * 1.5;
      alpha = 1.0;
    `
    },
    {
        id: "magnetar",
        name: "Magnetar core",
        category: "Cosmic & radiant",
        kind: "shader",
        swatch: ['#0055ff', '#000022'],
        transparent: true,
        additive: true,
        depthWrite: false,
        displaceParam: true,
        params: [{
                key: 'speed',
                label: 'Pulse',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.05,
                default: 1.5
            },
            {
                key: 'displace',
                label: 'Distort',
                type: 'range',
                min: 0,
                max: 0.2,
                step: 0.005,
                default: 0.08
            },
        ],
        fragBody: `
      float t = u_time * u_speed;
      float field = sin(vObjPos.x * 6.0 + t) + cos(vObjPos.y * 6.0 - t) + sin(vObjPos.z * 6.0 + t * 0.5);
      field = abs(field);
      float core = smoothstep(1.2, 2.5, field);
      vec3 c1 = vec3(0.0, 0.1, 0.5);
      vec3 c2 = vec3(0.5, 0.8, 1.0);
      col = mix(c1, c2, pow(field, 3.0)) * diff;
      col += vec3(1.0) * core * 4.0; // Blinding magnetic arcs
      col += vec3(0.2, 0.6, 1.0) * pow(fres, 2.0) * 2.5;
      alpha = clamp(0.4 + core, 0.0, 1.0);
    `
    },
    {
        id: "galaxy",
        name: "Galaxy",
        category: "Cosmic & radiant",
        kind: "shader",
        swatch: ["#110022", "#ff88cc"],
        transparent: false,
        additive: false,
        depthWrite: true,
        params: [{
                key: "coreColor",
                label: "Core Color",
                type: "color",
                default: "#ffffff"
            },
            {
                key: "dustColor",
                label: "Dust Color",
                type: "color",
                default: "#2a0a4a"
            },
            {
                key: "armCount",
                label: "Arm Count",
                type: "float",
                default: 3.0,
                min: 1.0,
                max: 6.0
            },
            {
                key: "tightness",
                label: "Spiral Tightness",
                type: "float",
                default: 1.5,
                min: 0.5,
                max: 3.0
            },
            {
                key: "starDensity",
                label: "Star Density",
                type: "float",
                default: 0.6,
                min: 0.1,
                max: 1.0
            },
            {
                key: "rotSpeed",
                label: "Rotation Speed",
                type: "float",
                default: 0.2,
                min: 0.0,
                max: 1.0
            }
        ],
        fragUniforms: `
      uniform vec3 u_coreColor;
      uniform vec3 u_dustColor;
      uniform float u_armCount;
      uniform float u_tightness;
      uniform float u_starDensity;
      uniform float u_rotSpeed;
    `,
        fragBody: `
      vec2 uv = vWorldPos.xy * 0.8;
      float r = length(uv);
      float a = atan(uv.y, uv.x);
      float t = u_time * u_rotSpeed;
      float spiral = sin(a * u_armCount - r * u_tightness * 6.0 + t);
      spiral = smoothstep(-0.1, 0.3, spiral);
      float arms = spiral * (1.0 - r * 0.8);
      float dust = fbm(vec3(uv * 2.0, t * 0.1)) * 0.7;
      dust *= arms;
      float core = 1.0 - smoothstep(0.0, 0.25, r);
      core = pow(core, 1.5);
      vec3 colArm = mix(u_dustColor, u_coreColor, arms);
      col = colArm * (diff * 0.5 + 0.5);
      col += u_coreColor * core * 0.9;
      float stars = snoise(vec3(uv * 20.0, 1.0)) * 0.5 + 0.5;
      stars = smoothstep(0.97, 1.0, stars) * u_starDensity;
      col += stars * u_coreColor * 0.8;
      alpha = 1.0;
    `
    },
    {
        id: "vortex",
        name: "Vortex",
        category: "Cosmic & radiant",
        kind: "shader",
        swatch: ["#0b0b2b", "#ff5500"],
        transparent: false,
        additive: false,
        depthWrite: true,
        params: [{
                key: "vortexColor",
                label: "Vortex Color",
                type: "color",
                default: "#ff5500"
            },
            {
                key: "coreGlow",
                label: "Core Glow",
                type: "float",
                default: 0.8,
                min: 0.1,
                max: 2.0
            },
            {
                key: "armCount",
                label: "Arm Count",
                type: "float",
                default: 4.0,
                min: 1.0,
                max: 8.0
            },
            {
                key: "twistSpd",
                label: "Twist Speed",
                type: "float",
                default: 1.0,
                min: 0.1,
                max: 3.0
            },
            {
                key: "streakLen",
                label: "Streak Length",
                type: "float",
                default: 0.5,
                min: 0.1,
                max: 1.0
            },
            {
                key: "backgroundDark",
                label: "Background Darkness",
                type: "float",
                default: 0.9,
                min: 0.5,
                max: 1.0
            }
        ],
        fragUniforms: `
      uniform vec3 u_vortexColor;
      uniform float u_coreGlow;
      uniform float u_armCount;
      uniform float u_twistSpd;
      uniform float u_streakLen;
      uniform float u_backgroundDark;
    `,
        fragBody: `
      vec2 uv = vWorldPos.xy * 1.2;
      float r = length(uv);
      float a = atan(uv.y, uv.x);
      float t = u_time * u_twistSpd;
      float angleShift = a + log(r + 0.01) * 3.0 * u_armCount + t;
      float arms = sin(angleShift) * 0.5 + 0.5;
      arms *= smoothstep(0.8, 0.0, r);
      float streaks = 0.0;
      for (int i = 0; i < 4; i++) {
        float seed = float(i) * 1.7;
        vec2 dir = vec2(cos(seed), sin(seed));
        float proj = dot(uv, dir) * 3.0 + t;
        streaks += 1.0 - smoothstep(0.0, u_streakLen, abs(fract(proj) - 0.5));
      }
      streaks = clamp(streaks * 0.5, 0.0, 1.0);
      float core = exp(-r * 5.0) * u_coreGlow;
      vec3 colVortex = u_vortexColor * (arms * 0.7 + streaks * 0.3 + core);
      vec3 bg = vec3(0.0, 0.0, 0.02) * u_backgroundDark;
      col = bg + colVortex;
      alpha = 1.0;
    `
    },
    {
        id: "neonpulse",
        name: "Neon Pulse",
        category: "Plasma & energy",
        kind: "shader",
        swatch: ['#ff00ff', '#00ffff'],
        gravity: true,
        fragBody: `\`
        float t = u_time * u_speed;
        float pulse = sin(rad * 8.0 - t * 4.0) * 0.5 + 0.5;
        float ring = smoothstep(0.3, 0.4, pulse) * smoothstep(0.6, 0.5, pulse);
        vec3 c1 = hsv2rgb(vec3(fract(h * 1.5 + t * 0.1), 1.0, 1.0));
        vec3 c2 = hsv2rgb(vec3(fract(h * 1.5 + 0.5 + t * 0.1), 1.0, 1.0));
        col = mix(c1, c2, ring) * (0.5 + diff * 0.5);
        col += vec3(1.0) * pow(fres, 3.0) * 2.0;
      \``
    },
    {
        id: "plasma_globe",
        name: "Plasma Globe",
        category: "Plasma & energy",
        kind: "shader",
        swatch: "#9400D3",
        flags: {
            isAdditive: true
        },
        params: [{
                name: 'Speed',
                default: 1.0,
                min: 0.0,
                max: 3.0
            },
            {
                name: 'Intensity',
                default: 0.7,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Ray Count',
                default: 3,
                min: 1,
                max: 8,
                step: 1
            },
            {
                name: 'Color Scheme',
                default: 0.5,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Distortion',
                default: 0.4,
                min: 0.0,
                max: 1.0
            },
        ],
        fragUniforms: `
      uniform float u_speed;
      uniform float u_intensity;
      uniform int u_rayCount;
      uniform float u_colorScheme;
      uniform float u_distortion;
    `,
        fragBody: `
      vec2 uv = vUv - 0.5;
      float r = length(uv);
      float a = atan(uv.y, uv.x);
      float angleShift = a + uTime * u_speed * 0.2;
      float rayCount = float(u_rayCount);
      float ray = sin(angleShift * rayCount) * 0.5 + 0.5;
      ray = smoothstep(0.2, 0.8, ray);

      float dist = 1.0 - r;
      float arc = sin((r * 10.0 - uTime * u_speed) * 2.0) * 0.5 + 0.5;
      arc = smoothstep(0.4, 0.6, arc);

      float centerGlow = exp(-r * 5.0) * 0.8;
      float plasma = (ray * arc * u_intensity + centerGlow) * step(0.0, dist);

      vec3 col = mix(
        hsv2rgb(vec3(fract(a / 6.28318 + u_colorScheme * 0.7), 0.9, 1.0)),
        vec3(0.8, 0.6, 1.0),
        0.5
      );
      col *= plasma;
      gl_FragColor = vec4(col, plasma);
    `
    },
    {
        id: "electric_arc",
        name: "Electric Arc",
        category: "Plasma & energy",
        kind: "shader",
        swatch: "#00FFFF",
        flags: {
            isAdditive: true
        },
        params: [{
                name: 'Arc Density',
                default: 0.7,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Branching',
                default: 0.5,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Glow',
                default: 0.8,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Color',
                default: 0.5,
                min: 0.0,
                max: 1.0,
                isColor: true
            },
            {
                name: 'Flicker',
                default: 0.3,
                min: 0.0,
                max: 1.0
            },
        ],
        fragUniforms: `
      uniform float u_arcDensity;
      uniform float u_branching;
      uniform float u_glow;
      uniform float u_color;
      uniform float u_flicker;
    `,
        fragBody: `
      vec2 uv = vUv;
      float t = uTime * (1.0 + sin(uTime * 10.0) * u_flicker);
      float arc = 0.0;
      vec2 p = uv * 2.0 - 1.0;
      for (int i = 0; i < 5; i++) {
        float offset = float(i) * 0.3;
        float x = p.x;
        float y = p.y + sin(x * 8.0 + t + offset) * 0.2;
        float d = abs(y) - 0.02 * u_branching;
        d = 1.0 - smoothstep(0.0, 0.02, d);
        arc += d * u_arcDensity * 0.2;

        y = p.y + cos(x * 15.0 + t * 1.3) * 0.15;
        d = abs(y) - 0.01;
        arc += (1.0 - smoothstep(0.0, 0.02, d)) * 0.1 * u_branching;
      }
      arc = clamp(arc, 0.0, 1.0);

      float glow = exp(-length(p) * 3.0) * u_glow;
      vec3 col = hsv2rgb(vec3(u_color, 0.9, 1.0)) * (arc + glow);
      gl_FragColor = vec4(col, arc + glow > 0.0 ? 1.0 : 0.0);
    `
    },
    {
        id: "magmacore",
        name: "Magma core",
        category: "Elemental & heat",
        kind: "shader",
        swatch: ['#ff6a1e', '#141012'],
        params: [{
                key: 'scale',
                label: 'Cracks',
                type: 'range',
                min: 1,
                max: 12,
                step: 0.1,
                default: 4
            },
            {
                key: 'speed',
                label: 'Pulse',
                type: 'range',
                min: 0,
                max: 3,
                step: 0.05,
                default: 1
            },
        ],
        fragUniforms: "uniform float u_scale;",
        fragBody: `
      float crack = pow(1.0 - abs(snoise(vObjPos * u_scale)), 12.0);
      float pulse = 0.55 + 0.45 * sin(u_time * u_speed * 2.2 + snoise(vObjPos) * 3.0);
      col = vec3(0.06, 0.05, 0.055) * (0.6 + 0.8 * diff);
      col += mix(vec3(1.0, 0.25, 0.02), vec3(1.0, 0.8, 0.3), crack) * crack * pulse * 2.6;
    `
    },
    {
        id: "sandstorm",
        name: "Sandstorm",
        category: "Elemental & heat",
        kind: "shader",
        swatch: ['#d4a036', '#3a2a0a'],
        gravity: true,
        transparent: true,
        additive: true,
        depthWrite: false,
        fragBody: `\n float t = u_time * u_speed; float grains = 0.0; for(int i=0; i<4; i++){ float fi = float(i); grains += step(0.82, snoise(vObjPos * (8.0+fi*3.0) + vec3(fi*t*1.7, -fi*t*0.9, fi))); } col = vec3(0.8,0.5,0.2) * grains * 2.0; alpha = grains * 0.85;\n`,
        params: [{
            key: 'speed',
            label: 'Wind',
            type: 'range',
            min: 0,
            max: 4,
            step: 0.05,
            default: 2.2
        }, ]
    },
    {
        id: "molten",
        name: "Molten",
        category: "Elemental & heat",
        kind: "shader",
        swatch: ["#ff4500", "#ffd700"],
        transparent: false,
        additive: false,
        depthWrite: true,
        params: [{
                key: "hotColor",
                label: "Hot Color",
                type: "color",
                default: "#ff6600"
            },
            {
                key: "coolColor",
                label: "Cool Color",
                type: "color",
                default: "#330000"
            },
            {
                key: "temperature",
                label: "Temperature",
                type: "float",
                default: 0.8,
                min: 0.0,
                max: 1.5
            },
            {
                key: "flowSpeed",
                label: "Flow Speed",
                type: "float",
                default: 1.0,
                min: 0.1,
                max: 5.0
            },
            {
                key: "bumpStrength",
                label: "Bump Strength",
                type: "float",
                default: 0.5,
                min: 0.0,
                max: 1.0
            },
            {
                key: "glowFalloff",
                label: "Glow Falloff",
                type: "float",
                default: 0.6,
                min: 0.1,
                max: 2.0
            }
        ],
        fragUniforms: `
      uniform vec3 u_hotColor;
      uniform vec3 u_coolColor;
      uniform float u_temperature;
      uniform float u_flowSpeed;
      uniform float u_bumpStrength;
      uniform float u_glowFalloff;
    `,
        fragBody: `
      vec2 uv = vWorldPos.xy * 2.5;
      float t = u_time * u_flowSpeed;
      float displ = fbm(vec3(uv + vec2(0.0, t * 0.7), 0.5)) * u_bumpStrength;
      displ += snoise(vec3(uv * 3.0 + vec2(0.0, t * 1.2), 0.9)) * u_bumpStrength * 0.4;
      float tempMap = u_temperature * (0.6 + 0.4 * displ);
      tempMap = clamp(tempMap, 0.0, 1.0);
      vec3 blackbody = mix(vec3(0.6, 0.2, 0.05), vec3(1.0, 0.85, 0.1), tempMap);
      if (tempMap > 0.7) blackbody = mix(blackbody, vec3(1.0, 0.95, 0.6), (tempMap - 0.7) * 5.0);
      vec3 lava = mix(u_coolColor, u_hotColor * blackbody, tempMap);
      float spec = pow(max(0.0, dot(N, normalize(V + KL))), 30.0);
      spec *= smoothstep(0.3, 0.7, tempMap);
      vec3 glow = u_hotColor * displ * u_glowFalloff;
      col = lava * (diff * 0.6 + 0.5) + spec * 0.8 + glow * 0.3;
      alpha = 1.0;
    `
    },
    {
        id: "flame",
        name: "Flame",
        category: "Elemental & heat",
        kind: "shader",
        swatch: ["#ff4500", "#ffd700"],
        transparent: false,
        additive: false,
        depthWrite: true,
        params: [{
                key: "baseColor",
                label: "Base Color",
                type: "color",
                default: "#ff8c00"
            },
            {
                key: "tipColor",
                label: "Tip Color",
                type: "color",
                default: "#ffff00"
            },
            {
                key: "turbulence",
                label: "Turbulence",
                type: "float",
                default: 1.0,
                min: 0.2,
                max: 2.0
            },
            {
                key: "speed",
                label: "Speed",
                type: "float",
                default: 1.0,
                min: 0.1,
                max: 4.0
            },
            {
                key: "soot",
                label: "Soot",
                type: "float",
                default: 0.15,
                min: 0.0,
                max: 0.5
            },
            {
                key: "brightness",
                label: "Brightness",
                type: "float",
                default: 1.2,
                min: 0.5,
                max: 2.0
            }
        ],
        fragUniforms: `
      uniform vec3 u_baseColor;
      uniform vec3 u_tipColor;
      uniform float u_turbulence;
      uniform float u_speed;
      uniform float u_soot;
      uniform float u_brightness;
    `,
        fragBody: `
      vec2 uv = vWorldPos.xy * 2.0;
      float t = u_time * u_speed;
      float y = vWorldPos.y * 0.8 + 0.5;
      float noise = fbm(vec3(uv * 1.5 + vec2(0.0, t * 0.8), 0.5)) * u_turbulence;
      noise += snoise(vec3(uv * 3.0 + vec2(0.0, t * 1.3), 0.9)) * u_turbulence * 0.5;
      float shape = (1.0 - y) * (1.0 + noise * 0.5);
      shape = clamp(shape, 0.0, 1.0);
      vec3 flameCol = mix(u_baseColor, u_tipColor, y * shape);
      float sootLayer = smoothstep(0.1, 0.3, noise) * u_soot * (1.0 - y);
      flameCol = mix(flameCol, vec3(0.02, 0.02, 0.02), sootLayer);
      float intensity = shape * u_brightness;
      col = flameCol * intensity * (diff * 0.5 + 0.6);
      alpha = 1.0;
    `
    },
    {
        id: "pearlescent",
        name: "Nacre / Pearl",
        category: "Structural color",
        kind: "shader",
        swatch: ['#ffe8f0', '#c8a0d0'],
        gravity: true,
        params: [{
                key: 'baseTone',
                label: 'Base Tone',
                type: 'color',
                default: '#fae8f0'
            },
            {
                key: 'iridStr',
                label: 'Iridescence',
                type: 'range',
                min: 0,
                max: 1.5,
                step: 0.02,
                default: 1.0
            },
            {
                key: 'layers',
                label: 'Layer Count',
                type: 'range',
                min: 2,
                max: 10,
                step: 1,
                default: 5
            },
            {
                key: 'gloss',
                label: 'Gloss',
                type: 'range',
                min: 0,
                max: 1,
                step: 0.01,
                default: 0.7
            },
            {
                key: 'speed',
                label: 'Shimmer',
                type: 'range',
                min: 0,
                max: 3,
                step: 0.05,
                default: 0.5
            },
        ],
        fragUniforms: "uniform vec3 u_baseTone; uniform float u_iridStr; uniform float u_layers; uniform float u_gloss;",
        fragBody: `\`
      // Nacre: layered aragonite plates cause thin-film interference
      // Layer count controls how many "virtual layers" contribute
      float t = u_time * u_speed;
      vec3 iriAccum = vec3(0.0);
      float weightSum = 0.0;
      for (int i = 0; i < 10; i++) {
        if (float(i) >= u_layers) break;
        float fi = float(i);
        float depth = fi * 0.12;
        float phase = fres * 12.0 + depth * 8.0 + fi * 1.8;
        float w = exp(-fi * 0.35);
        vec3 layerCol = hsv2rgb(vec3(
          fract(0.08 + fres * 0.22 + fi * 0.03 + t * 0.02),
          0.45,
          0.55 + 0.45 * (0.5 + 0.5 * sin(phase))
        ));
        iriAccum += layerCol * w;
        weightSum += w;
      }
      vec3 iriCol = iriAccum / weightSum;
      // Soft diffuse base with pearlescent sheen
      vec3 base = u_baseTone * (0.4 + diff * 0.55);
      col = mix(base, iriCol, u_iridStr * 0.7);
      // High-gloss specular
      float spec = pow(max(dot(reflect(-V, N), KL), 0.0), 60.0);
      col += vec3(1.0) * spec * u_gloss * 1.8;
      col += iriCol * pow(fres, 2.5) * u_iridStr * 0.7;
    \``
    },
    {
        id: "oil_spill",
        name: "Oil Spill",
        category: "Structural color",
        kind: "shader",
        swatch: ["#0a0a0a", "#ff00cc"],
        transparent: false,
        additive: false,
        depthWrite: true,
        params: [{
                key: "hueShift",
                label: "Hue Shift",
                type: "float",
                default: 0.0,
                min: 0.0,
                max: 1.0
            },
            {
                key: "iridescence",
                label: "Iridescence",
                type: "float",
                default: 1.0,
                min: 0.0,
                max: 2.0
            },
            {
                key: "noiseScale",
                label: "Noise Scale",
                type: "float",
                default: 3.0,
                min: 1.0,
                max: 10.0
            },
            {
                key: "flowSpeed",
                label: "Flow Speed",
                type: "float",
                default: 1.0,
                min: 0.1,
                max: 5.0
            },
            {
                key: "gloss",
                label: "Gloss",
                type: "float",
                default: 0.8,
                min: 0.0,
                max: 2.0
            }
        ],
        fragUniforms: `
      uniform float u_hueShift;
      uniform float u_iridescence;
      uniform float u_noiseScale;
      uniform float u_flowSpeed;
      uniform float u_gloss;
    `,
        fragBody: `
      vec2 uv = vWorldPos.xy * u_noiseScale;
      uv.y += u_time * u_flowSpeed;
      float n = fbm(vec3(uv, 0.5));
      n = n * 0.5 + 0.5;
      float thickness = pow(n, 0.8) * u_iridescence;
      float hue = fract(thickness + u_hueShift + length(vObjPos) * 0.2);
      vec3 oilCol = hsv2rgb(vec3(hue, 0.9, 0.7));
      float spec = pow(max(0.0, dot(N, normalize(V + KL))), 60.0);
      spec *= u_gloss;
      vec3 base = vec3(0.02, 0.01, 0.04);
      col = mix(base, oilCol, thickness * 0.8 + 0.2) + spec * oilCol * 0.7;
      alpha = 1.0;
    `
    },
    {
        id: "corrosion",
        name: "Rusted Iron",
        category: "Aged & weathered",
        kind: "shader",
        swatch: ['#8a3a1a', '#3a3a3a'],
        gravity: true,
        params: [{
                key: 'rust',
                label: 'Rust Color',
                type: 'color',
                default: '#b8451e'
            },
            {
                key: 'iron',
                label: 'Bare Iron',
                type: 'color',
                default: '#4a4a52'
            },
            {
                key: 'decay',
                label: 'Decay',
                type: 'range',
                min: 0,
                max: 1,
                step: 0.01,
                default: 0.65
            },
            {
                key: 'flakeSz',
                label: 'Flake Size',
                type: 'range',
                min: 1,
                max: 10,
                step: 0.5,
                default: 4
            },
            {
                key: 'rough',
                label: 'Roughness',
                type: 'range',
                min: 0,
                max: 1,
                step: 0.01,
                default: 0.6
            },
            {
                key: 'speed',
                label: 'Creep',
                type: 'range',
                min: 0,
                max: 2,
                step: 0.05,
                default: 0.2
            },
        ],
        fragUniforms: "uniform vec3 u_rust; uniform vec3 u_iron; uniform float u_decay; uniform float u_flakeSz; uniform float u_rough;",
        fragBody: `\`
      float t = u_time * u_speed;
      // Multi-octave rust map: large patches + fine granularity
      float rustLarge = fbm(vObjPos * u_flakeSz * 0.7 + vec3(t * 0.1, 0.0, 0.0));
      float rustFine  = fbm(vObjPos * u_flakeSz * 2.5 + vec3(0.0, t * 0.15, 0.0));
      float edgeRust  = pow(fres, 1.8); // rust starts at edges
      float rustMap   = clamp(rustLarge * 0.6 + rustFine * 0.3 + edgeRust * 0.2, 0.0, 1.0);
      float rustMask  = smoothstep(0.35, 0.55 + (1.0 - u_decay) * 0.3, rustMap);
      // Flaking: sharp cracks where rust meets iron
      float flakeCrack = pow(abs(snoise(vObjPos * u_flakeSz * 3.0)), 12.0);
      float flake = flakeCrack * rustMask * u_decay;
      // Iron has specular highlights; rust is matte
      float ironSpec = pow(max(dot(reflect(-V, N), KL), 0.0), 30.0) * (1.0 - u_rough);
      vec3 ironCol = u_iron * (0.3 + diff * 0.6) + vec3(1.0) * ironSpec * 0.7;
      vec3 rustCol = u_rust * (0.35 + diff * 0.45);
      // Rust gets darker / crustier in crevices
      rustCol *= 0.7 + 0.3 * (1.0 - rustFine);
      col = mix(ironCol, rustCol, rustMask);
      // Bright orange edge where fresh rust flakes are exposing
      col += u_rust * 1.3 * flake * 0.6;
      col += u_rust * pow(fres, 3.5) * 0.2;
    \``
    },
    {
        id: "cracked_earth",
        name: "Cracked Earth",
        category: "Aged & weathered",
        kind: "shader",
        swatch: ["#8b5a2b", "#3e2723"],
        transparent: false,
        additive: false,
        depthWrite: true,
        params: [{
                key: "dryColor",
                label: "Dry Color",
                type: "color",
                default: "#c8a96e"
            },
            {
                key: "crackColor",
                label: "Crack Color",
                type: "color",
                default: "#2d1c15"
            },
            {
                key: "crackWidth",
                label: "Crack Width",
                type: "float",
                default: 0.3,
                min: 0.1,
                max: 1.0
            },
            {
                key: "crackDensity",
                label: "Crack Density",
                type: "float",
                default: 2.0,
                min: 0.5,
                max: 5.0
            },
            {
                key: "dustAmount",
                label: "Dust Amount",
                type: "float",
                default: 0.3,
                min: 0.0,
                max: 1.0
            },
            {
                key: "age",
                label: "Age",
                type: "float",
                default: 0.7,
                min: 0.0,
                max: 1.0
            }
        ],
        fragUniforms: `
      uniform vec3 u_dryColor;
      uniform vec3 u_crackColor;
      uniform float u_crackWidth;
      uniform float u_crackDensity;
      uniform float u_dustAmount;
      uniform float u_age;
    `,
        fragBody: `
      vec3 pos = vWorldPos * u_crackDensity;
      float n = fbm(vec3(pos.xy * 1.8, 0.3));
      float ridge = fbm(vec3(pos.xy * 3.2, 1.7));
      float crackMask = smoothstep(u_crackWidth, u_crackWidth + 0.05, abs(ridge - 0.5) * 2.0);
      crackMask *= 1.0 - smoothstep(0.4, 0.6, n);
      float dustNoise = snoise(vec3(pos.xy * 8.0, u_time * 0.05)) * 0.5 + 0.5;
      float dust = dustNoise * u_dustAmount * (1.0 - crackMask);
      vec3 soil = mix(u_dryColor, u_crackColor, crackMask);
      soil = mix(soil, u_dryColor * 1.1, dust * u_age);
      vec3 finalCol = soil * (diff * 0.7 + 0.4);
      col = finalCol;
      alpha = 1.0;
    `
    },
    {
        id: "overload",
        name: "Sensory overload",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#ff00ff', '#00ffff'],
        params: [{
            key: 'speed',
            label: 'Trip',
            type: 'range',
            min: 0,
            max: 4,
            step: 0.05,
            default: 1.5
        }, ],
        fragBody: `
      float t = u_time * u_speed;
      float n1 = fbm(vObjPos * 4.0 + t * 0.5);
      float n2 = fbm(vObjPos * 6.0 - t * 0.8);
      float n3 = fbm(vObjPos * 3.0 + vec3(t * 1.2, -t * 0.5, 0.0));
      vec3 c1 = hsv2rgb(vec3(n1 * 0.5 + t * 0.1, 1.0, 1.0));
      vec3 c2 = hsv2rgb(vec3(n2 * 0.5 + 0.5, 1.0, 1.0));
      col = mix(c1, c2, n3) * (0.5 + diff * 0.8);
      col += vec3(1.0) * pow(fres, 4.0) * 3.0;
    `
    },
    {
        id: "moireheadache",
        name: "Moiré Madness",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#000000', '#ffffff'],
        params: [{
            key: 'speed',
            label: 'Spin',
            type: 'range',
            min: 0,
            max: 4,
            step: 0.05,
            default: 1.5
        }],
        fragBody: `
        float t = u_time * u_speed;
        float x = vObjPos.x * 20.0;
        float y = vObjPos.y * 20.0;
        float m1 = sin(x * 2.0 + t) * sin(y * 2.0 - t);
        float m2 = sin(x * 1.5 - t * 1.3) * sin(y * 1.5 + t * 1.3);
        float m = abs(m1 - m2);
        col = vec3(m) * diff * 2.0;
        col += vec3(1.0) * pow(fres, 5.0) * 1.0;
      `
    },
    {
        id: "brainmelting",
        name: "Brain Melting",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#ffaa00', '#aa00ff'],
        params: [{
            key: 'speed',
            label: 'Trip',
            type: 'range',
            min: 0,
            max: 4,
            step: 0.05,
            default: 2.0
        }],
        fragBody: `
        float t = u_time * u_speed;
        float n1 = fbm(vObjPos * 4.0 + t * 0.7);
        float n2 = fbm(vObjPos * 5.0 - t * 0.9);
        float n3 = fbm(vObjPos * 3.0 + vec3(t * 1.1, -t * 0.6, 0.0));
        float melt = sin(vObjPos.y * 10.0 + n1 * 8.0 + t) * 0.5 + 0.5;
        vec3 hot = vec3(1.0, 0.6, 0.1) * (1.0 - melt) * 2.0;
        vec3 cold = vec3(0.2, 0.1, 1.0) * melt * 2.0;
        col = mix(hot, cold, n3) * diff;
        col += vec3(1.0) * pow(fres, 4.0) * 1.8;
      `
    },
    {
        id: "cellularnoise",
        name: "Cellular Chaos",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#ff0088', '#00ff88'],
        gravity: true,
        fragBody: `\`
        float t = u_time * u_speed;
        vec3 p = vObjPos * 4.0;
        vec3 cell = floor(p) + 0.5;
        vec3 subs = p - cell;
        float dist = length(subs) * 0.8;
        float n = snoise(cell + t * 0.2);
        float brightness = 1.0 - smoothstep(0.0, 0.5, dist);
        vec3 c1 = hsv2rgb(vec3(n * 0.8, 1.0, 1.0));
        vec3 c2 = vec3(1.0);
        col = mix(vec3(0.05), c1, brightness * 0.8) * diff;
        col += c2 * pow(fres, 4.0) * 1.5;
      \``
    },
    {
        id: "neonwireframe",
        name: "Neon Wireframe",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ["#000000", "#ff0055"],
        gravity: false,
        fragBody: "float t = u_time * u_speed; float edge = abs(1.0 - 2.0 * fbm(vObjPos * 20.0 + t * 0.5)); edge = smoothstep(0.7,0.9,edge); vec3 wire = vec3(1.0, 0.0, 0.5) * edge * 3.0; col = wire * diff; col += vec3(1.0) * pow(fres,5.0)*0.5;",
        params: [{
            "key": "speed",
            "label": "Wire",
            "type": "range",
            "min": 0,
            "max": 4,
            "step": 0.05,
            "default": 1.0
        }]
    },
    {
        id: "psychedelicfractal",
        name: "Psychedelic Fractal",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ["#ff00ff", "#ffff00"],
        gravity: false,
        params: [{
            "key": "speed",
            "label": "Trip",
            "type": "range",
            "min": 0,
            "max": 4,
            "step": 0.05,
            "default": 2.5
        }],
        fragBody: "float t = u_time * u_speed; float z = 1.0; for(int i=0; i<3; i++){ z = fbm(vObjPos * z * 2.0 + t * 0.2) * 2.0; } vec3 c = hsv2rgb(vec3(z*0.8+t*0.1, 1.0, 1.0)); col = c * diff; col += vec3(1.0)*pow(fres,4.0)*2.0;"
    },
    {
        id: "nebulaecho",
        name: "Nebula Echo",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#ff80c0', '#200840'],
        gravity: true,
        fragBody: `\n float t = u_time * u_speed; float cloud = fbm(vObjPos*2.5 + t*0.15); float echo = sin(cloud*12.0)*0.5+0.5; vec3 col1 = hsv2rgb(vec3(0.8+cloud*0.2, 0.7, 0.5)); vec3 col2 = hsv2rgb(vec3(0.6+cloud*0.3, 0.8, 0.8)); col = mix(col1, col2, echo) * diff; col += vec3(1.0,0.7,1.0)*pow(fres,3.0)*0.9;\n`,
        params: [{
            key: 'speed',
            label: 'Drift',
            type: 'range',
            min: 0,
            max: 3,
            step: 0.05,
            default: 1.0
        }, ]
    },
    {
        id: "electricarc",
        name: "Electric Arc",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#ffffff', '#0022ff'],
        gravity: true,
        transparent: true,
        additive: true,
        depthWrite: false,
        fragBody: `\`
        float t = u_time * u_speed; float arc = 0.0; for(int i=0; i<3; i++){ float fi = float(i); arc += pow(max(0.0, 1.0 - abs(sin(rad*(7.0+fi*3.0)+t*2.0 + fi*1.2)*0.5+0.5 - 0.7)*15.0), 3.0); } arc = clamp(arc,0.0,1.0); col = vec3(0.8,0.9,1.0) * arc * 4.0; col += vec3(1.0)*pow(fres,5.0)*0.8; alpha = arc*0.95;
      \``,
        params: [{
            key: 'speed',
            label: 'Zap',
            type: 'range',
            min: 0,
            max: 4,
            step: 0.05,
            default: 2.5
        }]
    },
    {
        id: "flowymercury",
        name: "Flow Mercury",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#c0c0d0', '#3a3a4a'],
        gravity: true,
        fragBody: `\n float t = u_time * u_speed; float flow = fbm(vObjPos*3.0 + t*0.4); float streak = abs(sin(ang*4.0 + flow*7.0 + t)*0.5+0.5); col = mix(vec3(0.2,0.22,0.3), vec3(0.8,0.82,0.9), streak) * diff; col += vec3(1.0)*pow(fres,3.0)*0.7;\n`,
        params: [{
            key: 'speed',
            label: 'Flow',
            type: 'range',
            min: 0,
            max: 3,
            step: 0.05,
            default: 1.3
        }, ]
    },
    {
        id: "candyfloss",
        name: "Candy Floss",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#ffaacc', '#ffffff'],
        gravity: false,
        fragBody: "float t = u_time * u_speed; float fluff = fbm(vObjPos*5.0 + t*0.6); float thickness = smoothstep(0.3, 0.8, fluff); vec3 cotton = mix(vec3(1.0,0.7,0.8), vec3(1.0,0.9,0.95), thickness); col = cotton * diff; col += vec3(1.0)*pow(fres,2.0)*1.2;",
        params: [{
            key: 'speed',
            label: 'Spin',
            type: 'range',
            min: 0,
            max: 3,
            step: 0.05,
            default: 0.8
        }, ]
    },
    {
        id: "glitchgrid",
        name: "Glitch Grid",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#ff00ff', '#000000'],
        gravity: true,
        transparent: true,
        additive: true,
        depthWrite: false,
        fragBody: `\n float t = u_time * u_speed; vec3 grid = fract(vObjPos*6.0); float gx = smoothstep(0.08,0.1,grid.x); float gy = smoothstep(0.08,0.1,grid.y); float gz = smoothstep(0.08,0.1,grid.z); float g = max(max(gx,gy),gz); float glitch = step(0.92, snoise(vObjPos*3.0 + t*10.0)); g = max(g, glitch); col = vec3(1.0,0.2,1.0) * g * 3.0; alpha = g*0.9;\n`,
        params: [{
            key: 'speed',
            label: 'Scan',
            type: 'range',
            min: 0,
            max: 4,
            step: 0.05,
            default: 1.8
        }, ]
    },
    {
        id: "sparkleswirl",
        name: "Sparkle Swirl",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#ffdd55', '#550066'],
        gravity: true,
        fragBody: `\n float t = u_time * u_speed; float a = ang + t*2.0 + rad*8.0; float spark = pow(max(sin(a*6.0)*0.5+0.5, 0.0), 20.0); vec3 base = hsv2rgb(vec3(0.15+rad*0.2, 0.9, 0.7)); col = base * diff + vec3(1.0,0.9,0.3)*spark*5.0; col += vec3(1.0)*pow(fres,4.0)*0.5;\n`,
        params: [{
            key: 'speed',
            label: 'Spin',
            type: 'range',
            min: 0,
            max: 4,
            step: 0.05,
            default: 1.5
        }, ]
    },
    {
        id: "bubblecolumn",
        name: "Bubble Column",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#80d0ff', '#0a2a4a'],
        gravity: true,
        transparent: true,
        additive: true,
        depthWrite: false,
        fragBody: `\n float t = u_time * u_speed; float rise = fract(h*4.0 - t*0.9); float bubble = step(0.85, snoise(vObjPos*10.0 + vec3(0.0,t*2.5,0.0))); bubble *= smoothstep(0.0, 0.2, rise)*smoothstep(0.8,0.6,rise); col = vec3(0.3,0.8,1.0) * bubble * 2.5; col += vec3(1.0)*pow(fres,3.0)*0.4; alpha = bubble*0.9;\n`,
        params: [{
            key: 'speed',
            label: 'Speed',
            type: 'range',
            min: 0,
            max: 4,
            step: 0.05,
            default: 1.6
        }, ]
    },
    {
        id: "hologlitch",
        name: "Holo-Glitch",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#ffaa00', '#0055ff'],
        gravity: true,
        fragBody: `\n float t = u_time * u_speed; float holo = fbm(vObjPos*4.0 + t*0.5); float g = step(0.65, holo) * smoothstep(0.3,0.8,holo); vec3 c1 = hsv2rgb(vec3(0.6+holo*0.2,1.0,1.0)); vec3 c2 = vec3(0.9); col = mix(c1*0.3, c2, g) * diff; col += vec3(1.0)*pow(fres,4.0)*0.8;\n`,
        params: [{
            key: 'speed',
            label: 'Shift',
            type: 'range',
            min: 0,
            max: 4,
            step: 0.05,
            default: 1.0
        }, ]
    },
    {
        id: "rainbowwave",
        name: "Rainbow Wave",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#ff0066', '#00ffcc'],
        gravity: true,
        fragBody: `\`
        float t = u_time * u_speed; float wave = sin(rad*6.0 + h*8.0 - t*4.0)*0.5+0.5; col = hsv2rgb(vec3(fract(h*2.0+t*0.1), 1.0, 1.0)) * wave * 2.0 * diff; col += vec3(1.0)*pow(fres,3.0)*1.2;
      \``,
        params: [{
            key: 'speed',
            label: 'Speed',
            type: 'range',
            min: 0,
            max: 4,
            step: 0.05,
            default: 2.0
        }]
    },
    {
        id: "octarine",
        name: "Octarine",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#7a00ff', '#ff0088'],
        gravity: true,
        fragBody: `\n float t = u_time * u_speed; float n = fbm(vObjPos*5.0 + t*0.4); float magic = sin(n*12.0)*0.5+0.5; vec3 c = hsv2rgb(vec3(0.8+n*0.2, 0.9, 1.0)); col = c * magic * 2.0 * diff; col += vec3(0.8,0.2,1.0)*pow(fres,4.0)*0.6;\n`,
        params: []
    },
    {
        id: "shatteredglass",
        name: "Shattered Glass",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#88ccff', '#1a2a3a'],
        gravity: true,
        fragBody: `\n float t = u_time * u_speed; vec3 p = vObjPos*12.0; vec3 f1 = floor(p); float crack = abs(snoise(f1 + t*0.1)); float shard = step(0.7, crack); col = mix(vec3(0.1,0.2,0.3), vec3(0.6,0.8,1.0), shard) * diff; col += vec3(1.0)*pow(fres,5.0)*0.9;\n`,
        params: [{
            key: 'speed',
            label: 'Stress',
            type: 'range',
            min: 0,
            max: 3,
            step: 0.05,
            default: 1.0
        }, ]
    },
    {
        id: "phaseghost",
        name: "Phase Ghost",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#d0f0ff', '#4a6a8a'],
        gravity: true,
        transparent: true,
        additive: true,
        depthWrite: false,
        fragBody: `\`
        float t = u_time * u_speed; float phase = sin(t*3.0 + rad*10.0)*0.5+0.5; float v = fbm(vObjPos*2.0 + t*0.7); col = vec3(0.8,0.9,1.0) * v * phase * 2.0; alpha = v*phase*0.85;
      \``,
        params: [{
            key: 'speed',
            label: 'Phase',
            type: 'range',
            min: 0,
            max: 4,
            step: 0.05,
            default: 1.5
        }]
    },
    {
        id: "liquidmetal",
        name: "Liquid Metal",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#b0b0c0', '#1a1a2a'],
        gravity: true,
        fragBody: `\n float t = u_time * u_speed; float flow = fbm(vObjPos*2.5 + t*0.3); float spec = pow(max(dot(reflect(-V, N), KL), 0.0), 30.0); col = mix(vec3(0.1,0.12,0.18), vec3(0.8,0.82,0.9), flow) * diff; col += vec3(1.0)*spec*1.8;\n`,
        params: [{
            key: 'speed',
            label: 'Flow',
            type: 'range',
            min: 0,
            max: 3,
            step: 0.05,
            default: 1.0
        }, ]
    },
    {
        id: "inkdrop",
        name: "Ink Drop",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#000000', '#ffffff'],
        gravity: true,
        fragBody: `\n float t = u_time * u_speed; float n = fbm(vObjPos*3.0 + t*0.2); float ink = smoothstep(0.4, 0.7, n); col = vec3(1.0-ink) * diff; col += vec3(0.1)*pow(fres,2.0);\n`,
        params: []
    },
    {
        id: "shockwave",
        name: "Shockwave",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#ffaa00', '#550000'],
        gravity: true,
        transparent: true,
        additive: true,
        depthWrite: false,
        fragBody: `\n float t = u_time * u_speed; float wave = sin(rad*15.0 - t*6.0)*0.5+0.5; wave = smoothstep(0.2,0.6,wave) * exp(-rad*2.0); col = vec3(1.0,0.6,0.1) * wave * 3.0; alpha = wave*0.9;\n`,
        params: [{
            key: 'speed',
            label: 'Boom',
            type: 'range',
            min: 0,
            max: 4,
            step: 0.05,
            default: 2.0
        }, ]
    },
    {
        id: "tvstatic",
        name: "TV Static",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#888888', '#000000'],
        gravity: false,
        fragBody: "float t = u_time * u_speed; float noise = snoise(vObjPos*30.0 + t*50.0); float mono = noise*0.5+0.5; col = vec3(mono) * diff; col += vec3(1.0)*pow(fres,2.0)*0.2;",
        params: [{
            key: 'speed',
            label: 'Noise',
            type: 'range',
            min: 0,
            max: 4,
            step: 0.05,
            default: 1.0
        }, ]
    },
    {
        id: "motherofflowers",
        name: "Mother of Flowers",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#ff88bb', '#2288ff'],
        gravity: true,
        fragBody: `\n float t = u_time * u_speed; float petal = sin(ang*5.0 + rad*12.0 + t)*0.5+0.5; petal = pow(petal, 3.0); vec3 c = hsv2rgb(vec3(0.8+petal*0.2, 0.8, 0.9)); col = c * petal * 2.0 * diff; col += vec3(1.0,0.6,0.8)*pow(fres,3.0)*0.7;\n`,
        params: [{
            key: 'speed',
            label: 'Bloom',
            type: 'range',
            min: 0,
            max: 3,
            step: 0.05,
            default: 1.0
        }, ]
    },
    {
        id: "quantumfoam",
        name: "Quantum Foam",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#00ffff', '#ff00ff'],
        gravity: true,
        transparent: true,
        additive: true,
        depthWrite: false,
        fragBody: ` float t = u_time * u_speed; float n = fbm(vObjPos * 8.0 + t * 0.3); float bubbles = step(0.6, n) * n; vec3 c1 = hsv2rgb(vec3(fract(h*2.0 + t*0.2), 1.0, 1.0)); vec3 c2 = vec3(1.0); col = mix(c1*0.3, c2, bubbles) * diff; col += vec3(1.0,0.7,1.0) * pow(fres,4.0)*1.5; alpha = bubbles*0.9; `,
        params: [{
            key: 'speed',
            label: 'Brew',
            type: 'range',
            min: 0,
            max: 4,
            step: 0.05,
            default: 1.2
        }]
    },
    {
        id: "plasmavortex",
        name: "Plasma Vortex",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#ffaa00', '#00ffff'],
        gravity: true,
        transparent: true,
        additive: true,
        depthWrite: false,
        fragBody: ` float t = u_time * u_speed; float r = rad; float a = ang + t*4.0 + r*15.0; float vortex = sin(a*5.0)*0.5+0.5; float rim = smoothstep(0.7,1.0,r); vec3 c = hsv2rgb(vec3(0.1+r*0.2+t*0.1,1.0,1.0)) * vortex; col = c * (0.3+diff*0.7); col += vec3(1.0,0.5,0.2)*pow(rim,2.0)*2.0; alpha = vortex*0.8+rim; `,
        params: [{
            key: 'speed',
            label: 'Spin',
            type: 'range',
            min: 0,
            max: 4,
            step: 0.05,
            default: 2.0
        }]
    },
    {
        id: "datacorruption",
        name: "Data Corruption",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#00ff00', '#000000'],
        gravity: true,
        transparent: true,
        additive: true,
        depthWrite: false,
        fragBody: ` float t = u_time * u_speed; float block = floor(h*20.0+t*2.0)*0.05; float glitch = step(0.9, snoise(vec3(block, t*5.0, 0.0))); float g2 = step(0.95, fbm(vObjPos*30.0+t*10.0)); float bright = max(glitch,g2); col = vec3(0.2,1.0,0.2) * bright * 2.0; alpha = bright*0.9; `,
        params: []
    },
    {
        id: "oilslick",
        name: "Oil Slick",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#ff00ff', '#00ff88'],
        gravity: true,
        transparent: true,
        depthWrite: false,
        fragBody: ` float t = u_time * u_speed; float film = fbm(vObjPos*2.0 + t*0.3)*0.7; vec3 iri = hsv2rgb(vec3(fract(h*5.0+film), 1.0, 1.0)); col = iri * (0.5+diff*0.5); col += vec3(1.0)*pow(fres,3.0)*1.2; alpha = 0.7+film*0.3; `,
        params: [{
            key: 'speed',
            label: 'Flow',
            type: 'range',
            min: 0,
            max: 4,
            step: 0.05,
            default: 1.0
        }]
    },
    {
        id: "cellularautomata",
        name: "Cellular Automata",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#ff8800', '#ff0088'],
        gravity: true,
        fragBody: ` float t = u_time * u_speed; vec3 cell = floor(vObjPos*5.0) / 5.0; float n = snoise(cell + t*0.2); float state = step(0.3, n) * step(0.7, n); vec3 c1 = hsv2rgb(vec3(0.05, 1.0, 1.0)); vec3 c2 = hsv2rgb(vec3(0.8, 1.0, 1.0)); col = mix(c1, c2, state) * diff; col += vec3(1.0)*pow(fres,4.0)*0.8; `,
        params: [{
            key: 'speed',
            label: 'Evolve',
            type: 'range',
            min: 0,
            max: 4,
            step: 0.05,
            default: 1.8
        }]
    },
    {
        id: "magneticflux",
        name: "Magnetic Flux",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#00ff00', '#0000ff'],
        gravity: true,
        transparent: true,
        additive: true,
        depthWrite: false,
        fragBody: ` float t = u_time * u_speed; float field = sin(rad*10.0 - t*3.0 + h*5.0)*0.5+0.5; float lines = abs(field - floor(field*5.0 + 0.5)/5.0)*10.0; lines = smoothstep(0.8,1.0,lines); col = vec3(0.2,1.0,0.2) * lines * 2.0; col += vec3(0.2,0.5,1.0) * pow(fres,4.0)*1.5; alpha = lines*0.9; `,
        params: [{
            key: 'speed',
            label: 'Flux',
            type: 'range',
            min: 0,
            max: 4,
            step: 0.05,
            default: 1.5
        }]
    },
    {
        id: "spectralmelt",
        name: "Spectral Melt",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#ff0000', '#0000ff'],
        gravity: true,
        fragBody: ` float t = u_time * u_speed; float melt = sin(vObjPos.y*10.0 + t*2.0 + fbm(vObjPos*2.0)*5.0)*0.5+0.5; vec3 hot = vec3(1.0,0.3,0.1) * (1.0-melt) * 2.0; vec3 cold = vec3(0.1,0.2,1.0) * melt * 2.0; col = mix(hot, cold, melt) * diff; col += vec3(1.0)*pow(fres,3.0)*1.5; `,
        params: []
    },
    {
        id: "timedilated",
        name: "Time Dilated",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#00ffff', '#ff00ff'],
        gravity: true,
        transparent: true,
        additive: true,
        depthWrite: false,
        fragBody: ` float t = u_time * u_speed; float distort = sin(t*2.0)*0.5+0.5; float hh = h + distort*0.2*sin(rad*10.0); float stripe = sin(hh*20.0 - t*8.0)*0.5+0.5; stripe = smoothstep(0.3,0.7,stripe); vec3 c1 = hsv2rgb(vec3(0.6+distort*0.3,1.0,1.0)); vec3 c2 = vec3(1.0); col = mix(c1, c2, stripe) * diff; col += vec3(1.0)*pow(fres,4.0)*1.2; alpha = stripe*0.9; `,
        params: [{
            key: 'speed',
            label: 'Warp',
            type: 'range',
            min: 0,
            max: 4,
            step: 0.05,
            default: 2.0
        }]
    },
    {
        id: "holographicnoise",
        name: "Holographic Noise",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#ff8800', '#00ff88'],
        gravity: true,
        fragBody: ` float t = u_time * u_speed; float n = fbm(vObjPos*6.0 + t*0.5); float hologram = sin(n*15.0)*0.5+0.5; vec3 c = hsv2rgb(vec3(fract(h*2.0+t*0.1), 1.0, 1.0)); col = c * hologram * 2.0 * diff; col += vec3(1.0)*pow(fres,4.0)*0.8; `,
        params: [{
            key: 'speed',
            label: 'Shift',
            type: 'range',
            min: 0,
            max: 4,
            step: 0.05,
            default: 1.3
        }]
    },
    {
        id: "crackedenergy",
        name: "Cracked Energy",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#4400ff', '#ffaa00'],
        gravity: true,
        transparent: true,
        additive: true,
        depthWrite: false,
        fragBody: ` float t = u_time * u_speed; float crack = max(0.0, 1.0 - abs(sin(rad*10.0 + t*2.0)*0.5+0.5 - 0.8)*10.0); crack += max(0.0, 1.0 - abs(sin(rad*7.0 - t*1.5 + h*3.0)*0.5+0.5 - 0.8)*10.0); crack = clamp(crack, 0.0, 1.0); col = vec3(0.6,0.2,1.0) * crack * 2.0; col += vec3(1.0,0.5,0.2)*pow(fres,3.0)*1.5; alpha = crack*0.9; `,
        params: [{
            key: 'speed',
            label: 'Charge',
            type: 'range',
            min: 0,
            max: 4,
            step: 0.05,
            default: 2.2
        }]
    },
    {
        id: "warpcore",
        name: "Warp Core",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#00ffff', '#ffffff'],
        gravity: true,
        transparent: true,
        additive: true,
        depthWrite: false,
        fragBody: ` float t = u_time * u_speed; float pulse = sin(rad*5.0 - t*3.0)*0.5+0.5; float intensity = pow(pulse, 4.0) * 3.0; col = vec3(0.2,1.0,1.0) * intensity; col += vec3(1.0)*pow(fres,4.0)*1.5; alpha = intensity*0.8; `,
        params: [{
            key: 'speed',
            label: 'Throttle',
            type: 'range',
            min: 0,
            max: 4,
            step: 0.05,
            default: 2.5
        }]
    },
    {
        id: "chromaticaberration",
        name: "Chromatic Aberration",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#ff0000', '#00ff00'],
        gravity: true,
        transparent: true,
        additive: true,
        depthWrite: false,
        fragBody: ` float t = u_time * u_speed; float hNorm = (h - u_levMin)/(u_levMax - u_levMin); vec3 colR = vec3(1.0,0.2,0.2) * step(0.7, fbm(vObjPos*10.0 + hNorm*0.1 + t*0.1)); vec3 colG = vec3(0.2,1.0,0.2) * step(0.7, fbm(vObjPos*10.0 - hNorm*0.1 + t*0.1)); vec3 colB = vec3(0.2,0.2,1.0) * step(0.7, fbm(vObjPos*10.0 + t*0.1)); col = (colR+colG+colB)*1.5; alpha = max(max(colR.r,colG.g),colB.b)*0.9; `,
        params: [{
            key: 'speed',
            label: 'Fringe',
            type: 'range',
            min: 0,
            max: 4,
            step: 0.05,
            default: 1.0
        }]
    },
    {
        id: "particlestorm",
        name: "Particle Storm",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#ff5500', '#111111'],
        gravity: true,
        transparent: true,
        additive: true,
        depthWrite: false,
        fragBody: ` float t = u_time * u_speed; float p = 0.0; for(int i=0; i<5; i++){ float fi = float(i); p += step(0.85, snoise(vObjPos* (10.0+fi*2.0) + vec3(fi*t*1.5, -fi*t*0.8, 0.0))); } col = vec3(1.0,0.4,0.1) * p * 2.0; alpha = p*0.8; `,
        params: [{
            key: 'speed',
            label: 'Storm',
            type: 'range',
            min: 0,
            max: 4,
            step: 0.05,
            default: 2.3
        }]
    },
    {
        id: "bubbleuniverse",
        name: "Bubble Universe",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#ff00cc', '#00ffcc'],
        gravity: true,
        transparent: true,
        additive: true,
        depthWrite: false,
        fragBody: ` float t = u_time * u_speed; float bubbles = step(0.5, snoise(vObjPos*5.0 + t*0.3)); bubbles *= smoothstep(0.3, 0.5, fbm(vObjPos*3.0)); col = hsv2rgb(vec3(fract(rad*2.0 + t*0.1), 1.0, 1.0)) * bubbles * 2.0; col += vec3(1.0)*pow(fres,4.0)*0.5; alpha = bubbles*0.9; `,
        params: [{
            key: 'speed',
            label: 'Fizz',
            type: 'range',
            min: 0,
            max: 4,
            step: 0.05,
            default: 1.0
        }]
    },
    {
        id: "mandala",
        name: "Mandala",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#ffaa00', '#aa00ff'],
        gravity: true,
        transparent: true,
        additive: true,
        depthWrite: false,
        fragBody: ` float t = u_time * u_speed; float r = rad; float a = ang + t; float symmetry = 6.0; float pattern = sin(a*symmetry + r*10.0)*0.5+0.5; pattern = smoothstep(0.5,0.7,pattern); vec3 c1 = hsv2rgb(vec3(0.1+r*0.2+t*0.05,1.0,1.0)); col = c1 * pattern * 2.0 * diff; col += vec3(1.0)*pow(fres,3.0)*0.6; alpha = pattern*0.9; `,
        params: [{
            key: 'speed',
            label: 'Rotate',
            type: 'range',
            min: 0,
            max: 4,
            step: 0.05,
            default: 1.0
        }]
    },
    {
        id: "vaporwave",
        name: "Vaporwave",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#ff80c0', '#80ff80'],
        gravity: true,
        fragBody: ` float t = u_time * u_speed; float wave = sin(rad*3.0 - t*2.0 + h*5.0)*0.5+0.5; vec3 pastel1 = vec3(1.0, 0.5, 0.7); vec3 pastel2 = vec3(0.5, 1.0, 0.7); col = mix(pastel1, pastel2, wave) * diff; col += vec3(1.0)*pow(fres,2.0)*1.0; `,
        params: [{
            key: 'speed',
            label: 'Chill',
            type: 'range',
            min: 0,
            max: 4,
            step: 0.05,
            default: 0.8
        }]
    },
    {
        id: "cyberpunkgrid",
        name: "Cyberpunk Grid",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#ff00ff', '#00ffff'],
        gravity: true,
        transparent: true,
        additive: true,
        depthWrite: false,
        fragBody: ` float t = u_time * u_speed; vec3 g = fract(vObjPos * 4.0); float grid = min(min(step(0.9,g.x), step(0.9,g.y)), step(0.9,g.z)); grid = smoothstep(0.0,0.1,grid); col = vec3(1.0,0.2,1.0) * grid * 2.0; col += vec3(0.2,1.0,1.0)*pow(fres,5.0)*1.2; alpha = grid*0.9; `,
        params: [{
            key: 'speed',
            label: 'Scan',
            type: 'range',
            min: 0,
            max: 4,
            step: 0.05,
            default: 1.5
        }]
    },
    {
        id: "radiatedglow",
        name: "Radiated Glow",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#00ff44', '#000000'],
        gravity: true,
        transparent: true,
        additive: true,
        depthWrite: false,
        fragBody: ` float t = u_time * u_speed; float glow = exp(-rad * 5.0) * sin(t * 2.0 + h*10.0) * 0.5 + 0.5; glow *= smoothstep(0.0,0.2,glow); col = vec3(0.2,1.0,0.4) * glow * 2.0; alpha = glow*0.8; `,
        params: [{
            key: 'speed',
            label: 'Pulse',
            type: 'range',
            min: 0,
            max: 4,
            step: 0.05,
            default: 1.7
        }]
    },
    {
        id: "wormhole",
        name: "Wormhole",
        category: "Psychedelic & mind-bending",
        kind: "shader",
        swatch: ['#4400ff', '#ff00aa'],
        gravity: true,
        transparent: true,
        additive: true,
        depthWrite: false,
        fragBody: `\`
        float r = rad;
        float a = ang + u_time * u_speed * 3.0 + r * 20.0;
        float tunnel = sin(a * 5.0) * 0.5 + 0.5;
        float rim = smoothstep(0.8, 1.0, r);
        vec3 col1 = hsv2rgb(vec3(0.7 + r * 0.2, 1.0, 1.0));
        vec3 col2 = hsv2rgb(vec3(0.9 + r * 0.3, 1.0, 0.8));
        col = mix(col1, col2, tunnel);
        col += vec3(1.0, 0.2, 0.5) * pow(rim, 2.0) * 2.0;
        alpha = clamp(tunnel * 0.8 + rim, 0.0, 1.0);
      \``
    },
    {
        id: "mercury",
        name: "Liquid metal",
        category: "Liquid & organic",
        kind: "shader",
        swatch: ['#eef2f8', '#5a636e'],
        params: [{
                key: 'tint',
                label: 'Tint',
                type: 'color',
                default: '#cdd4de'
            },
            {
                key: 'ripple',
                label: 'Ripple',
                type: 'range',
                min: 0,
                max: 0.6,
                step: 0.01,
                default: 0.25
            },
            {
                key: 'speed',
                label: 'Flow',
                type: 'range',
                min: 0,
                max: 3,
                step: 0.05,
                default: 1
            },
        ],
        fragUniforms: "uniform vec3 u_tint; uniform float u_ripple;",
        fragBody: `
      float w = fbm(vObjPos*8.0 + u_time*u_speed*0.5);
      float w2 = fbm(vObjPos*8.0 + 30.0 - u_time*u_speed*0.4);
      vec3 nn = normalize(N + u_ripple*vec3(w, (w+w2)*0.5, w2));
      vec3 r = reflect(-V, nn);
      float up = clamp(r.y*0.5+0.5, 0.0, 1.0);
      vec3 sky = mix(vec3(0.05,0.06,0.08), vec3(0.92,0.95,1.0), pow(up,1.5));
      float spec = pow(up, 8.0);
      col = sky * u_tint + spec*0.6;
      col += pow(1.0 - max(dot(nn,V),0.0), 3.0) * 0.2;
    `
    },
    {
        id: "jelly",
        name: "Jelly / goo",
        category: "Liquid & organic",
        kind: "shader",
        swatch: ['#ff8fc4', '#a83e78'],
        transparent: false,
        displaceParam: true,
        params: [{
                key: 'color',
                label: 'Jelly',
                type: 'color',
                default: '#ff7ab8'
            },
            {
                key: 'color2',
                label: 'Rim',
                type: 'color',
                default: '#fff3a0'
            },
            {
                key: 'displace',
                label: 'Wobble',
                type: 'range',
                min: 0,
                max: 0.3,
                step: 0.005,
                default: 0.12
            },
            {
                key: 'speed',
                label: 'Speed',
                type: 'range',
                min: 0,
                max: 3,
                step: 0.05,
                default: 1
            },
        ],
        fragUniforms: "uniform vec3 u_color; uniform vec3 u_color2;",
        fragBody: `
      vec3 sss = u_color * (0.55 + 0.6*diff);
      float rim = pow(1.0 - max(dot(N,V),0.0), 2.0);
      col = mix(sss, u_color2, rim*0.7);
      col += rim*0.3;
    `
    },
    {
        id: "lava",
        name: "Lava / magma",
        category: "Liquid & organic",
        kind: "shader",
        swatch: ['#ffd24a', '#8a1c05'],
        params: [{
                key: 'intensity',
                label: 'Heat',
                type: 'range',
                min: 0.5,
                max: 2.2,
                step: 0.05,
                default: 1.2
            },
            {
                key: 'scale',
                label: 'Scale',
                type: 'range',
                min: 1,
                max: 8,
                step: 0.1,
                default: 3
            },
            {
                key: 'speed',
                label: 'Flow',
                type: 'range',
                min: 0,
                max: 3,
                step: 0.05,
                default: 1
            },
        ],
        fragUniforms: "uniform float u_intensity; uniform float u_scale;",
        fragBody: `
      vec3 q = vObjPos*u_scale;
      float n = fbm(q + vec3(0.0, -u_time*u_speed*0.4, 0.0));
      float n2 = fbm(q*2.0 + vec3(u_time*u_speed*0.3, 0.0, 0.0));
      float h = (n*0.65 + n2*0.35)*0.5 + 0.5;
      vec3 cool = vec3(0.11,0.02,0.01);
      vec3 mid  = vec3(0.85,0.18,0.03);
      vec3 hot  = vec3(1.0,0.85,0.35);
      col = mix(cool, mid, smoothstep(0.35,0.6,h));
      col = mix(col, hot, smoothstep(0.62,0.86,h));
      col *= u_intensity;
    `
    },
    {
        id: "shield",
        name: "Energy shield",
        category: "Sci-fi FX",
        kind: "shader",
        swatch: ['#5effc8', '#1c9a70'],
        transparent: true,
        additive: true,
        depthWrite: false,
        params: [{
                key: 'color',
                label: 'Colour',
                type: 'color',
                default: '#5effc8'
            },
            {
                key: 'power',
                label: 'Edge',
                type: 'range',
                min: 0.8,
                max: 4,
                step: 0.05,
                default: 2
            },
            {
                key: 'opacity',
                label: 'Opacity',
                type: 'range',
                min: 0,
                max: 1,
                step: 0.01,
                default: 0.8
            },
            {
                key: 'speed',
                label: 'Pulse',
                type: 'range',
                min: 0,
                max: 3,
                step: 0.05,
                default: 1
            },
        ],
        fragUniforms: "uniform vec3 u_color; uniform float u_power; uniform float u_opacity;",
        fragBody: `
      float fp = pow(fres, u_power);
      float bands = 0.5 + 0.5*sin(vWorldPos.y*20.0 - u_time*3.0*u_speed);
      float pulse = 0.6 + 0.4*sin(u_time*2.0*u_speed);
      col = u_color * (fp*1.5 + 0.15) + u_color*bands*fp*0.4;
      alpha = clamp(fp*pulse, 0.0, 1.0) * u_opacity + 0.04;
    `
    },
    {
        id: "plasma",
        name: "Plasma / electric",
        category: "Sci-fi FX",
        kind: "shader",
        swatch: ['#c08bff', '#2a1258'],
        params: [{
                key: 'color',
                label: 'Core',
                type: 'color',
                default: '#2a1258'
            },
            {
                key: 'color2',
                label: 'Arc',
                type: 'color',
                default: '#c08bff'
            },
            {
                key: 'scale',
                label: 'Scale',
                type: 'range',
                min: 1,
                max: 8,
                step: 0.1,
                default: 3
            },
            {
                key: 'speed',
                label: 'Speed',
                type: 'range',
                min: 0,
                max: 3,
                step: 0.05,
                default: 1
            },
        ],
        fragUniforms: "uniform vec3 u_color; uniform vec3 u_color2; uniform float u_scale;",
        fragBody: `
      vec3 q = vObjPos*u_scale;
      float n = fbm(q*2.0 + u_time*u_speed*0.6);
      float v = abs(sin(n*6.2831 + u_time*u_speed*3.0));
      float bolt = pow(1.0 - v, 8.0);
      col = mix(u_color*0.15, u_color2*2.2, bolt) + bolt*0.6;
    `
    },
    {
        id: "xray",
        name: "X-ray",
        category: "Sci-fi FX",
        kind: "shader",
        swatch: ['#9adfff', '#155a80'],
        transparent: true,
        additive: true,
        depthWrite: false,
        params: [{
                key: 'color',
                label: 'Colour',
                type: 'color',
                default: '#7ad0ff'
            },
            {
                key: 'power',
                label: 'Falloff',
                type: 'range',
                min: 0.5,
                max: 3,
                step: 0.05,
                default: 1.4
            },
            {
                key: 'opacity',
                label: 'Opacity',
                type: 'range',
                min: 0,
                max: 1,
                step: 0.01,
                default: 0.7
            },
        ],
        fragUniforms: "uniform vec3 u_color; uniform float u_power; uniform float u_opacity;",
        fragBody: `
      float fp = pow(fres, u_power);
      col = u_color * fp * 2.2;
      alpha = clamp(fp, 0.0, 1.0) * u_opacity;
    `
    },
    {
        id: "neonwire",
        name: "Neon wireframe",
        category: "Sci-fi FX",
        kind: "shader",
        swatch: ['#5effd0', '#12403a'],
        transparent: true,
        additive: true,
        depthWrite: false,
        wireframe: true,
        params: [{
            key: 'color',
            label: 'Wire',
            type: 'color',
            default: '#5effd0'
        }, ],
        fragUniforms: "uniform vec3 u_color;",
        fragBody: `
      col = u_color * (1.2 + fres);
    `
    },
    {
        id: "dissolve",
        name: "Dissolve burn",
        category: "Sci-fi FX",
        kind: "shader",
        swatch: ['#ff6a2a', '#cfcfcf'],
        params: [{
                key: 'color',
                label: 'Surface',
                type: 'color',
                default: '#c9c9c9'
            },
            {
                key: 'glow',
                label: 'Ember',
                type: 'color',
                default: '#ff6a2a'
            },
            {
                key: 'scale',
                label: 'Grain',
                type: 'range',
                min: 1,
                max: 12,
                step: 0.1,
                default: 4
            },
            {
                key: 'edge',
                label: 'Reveal',
                type: 'range',
                min: 0,
                max: 1,
                step: 0.01,
                default: 0.35
            },
            {
                key: 'width',
                label: 'Ember width',
                type: 'range',
                min: 0.01,
                max: 0.3,
                step: 0.005,
                default: 0.08
            },
        ],
        fragUniforms: "uniform vec3 u_color; uniform vec3 u_glow; uniform float u_scale; uniform float u_edge; uniform float u_width;",
        fragBody: `
      float n = fbm(vObjPos*u_scale)*0.5 + 0.5;
      if (n < u_edge) discard;
      float e = smoothstep(u_edge, u_edge + u_width, n);
      col = mix(u_glow*2.6, u_color*diff, e);
    `
    },
    {
        id: "poison",
        name: "Poison vial",
        category: "Potions & liquids",
        kind: "shader",
        swatch: ['#7dff3a', '#123c05'],
        transparent: true,
        depthWrite: false,
        gravity: true,
        params: [{
                key: 'color',
                label: 'Venom',
                type: 'color',
                default: '#4fdc1e'
            },
            {
                key: 'glow',
                label: 'Fume',
                type: 'color',
                default: '#c8ff5a'
            },
            {
                key: 'fill',
                label: 'Fill',
                type: 'range',
                min: 0.02,
                max: 0.98,
                step: 0.01,
                default: 0.55
            },
            {
                key: 'speed',
                label: 'Boil',
                type: 'range',
                min: 0,
                max: 3,
                step: 0.05,
                default: 1
            },
        ],
        fragUniforms: "uniform vec3 u_color; uniform vec3 u_glow;",
        fragBody: `LIQ + \`
      if (dSurf > 0.0) {\` + GLASS_ABOVE + \`} else {
        float murk = fbm(vObjPos * 3.0 - upA * (u_time * u_speed * 0.4));
        vec3 body = mix(u_color * 0.14, u_color, clamp(0.35 + murk * 0.5, 0.0, 1.0));
        float bub = snoise(vObjPos * 7.0 - upA * (u_time * u_speed * 1.1));
        float dots = smoothstep(0.55, 0.72, bub);
        col = body * (0.6 + 0.5 * diff);
        col += u_glow * dots * 1.1;
        col += u_glow * menis * 1.8;
        col += u_glow * pow(fres, 2.5) * 0.6;
        alpha = 0.94;
      }
    \``
    },
    {
        id: "lovepotion",
        name: "Love potion",
        category: "Potions & liquids",
        kind: "shader",
        swatch: ['#ff9ad2', '#b0125f'],
        transparent: true,
        depthWrite: false,
        gravity: true,
        params: [{
                key: 'color',
                label: 'Blush',
                type: 'color',
                default: '#ff5aa8'
            },
            {
                key: 'color2',
                label: 'Swirl',
                type: 'color',
                default: '#ffc4e8'
            },
            {
                key: 'fill',
                label: 'Fill',
                type: 'range',
                min: 0.02,
                max: 0.98,
                step: 0.01,
                default: 0.7
            },
            {
                key: 'speed',
                label: 'Swirl',
                type: 'range',
                min: 0,
                max: 3,
                step: 0.05,
                default: 1
            },
        ],
        fragUniforms: "uniform vec3 u_color; uniform vec3 u_color2;",
        fragBody: `LIQ + \`
      if (dSurf > 0.0) {\` + GLASS_ABOVE + \`} else {
        float swirl = fbm(vObjPos * 2.5 + vec3(u_time * u_speed * 0.22));
        vec3 body = mix(u_color, u_color2, 0.5 + 0.5 * swirl) * (0.85 - depth * 0.45);
        float gl = pow(max(snoise(vObjPos * 26.0 + upA * (u_time * u_speed)), 0.0), 10.0);
        col = body * (0.7 + 0.4 * diff);
        col += vec3(1.0, 0.92, 0.97) * gl * 2.2;
        col += u_color2 * menis * 2.0;
        col += u_color2 * pow(fres, 3.0) * 0.5;
        alpha = 0.92;
      }
    \``
    },
    {
        id: "galaxyjar",
        name: "Bottled galaxy",
        category: "Potions & liquids",
        kind: "shader",
        swatch: ['#7a8dff', '#12083a'],
        transparent: true,
        depthWrite: false,
        gravity: true,
        params: [{
                key: 'color',
                label: 'Glass',
                type: 'color',
                default: '#8a9aff'
            },
            {
                key: 'fill',
                label: 'Fill',
                type: 'range',
                min: 0.02,
                max: 0.98,
                step: 0.01,
                default: 0.8
            },
            {
                key: 'scale',
                label: 'Nebula',
                type: 'range',
                min: 1,
                max: 8,
                step: 0.1,
                default: 3
            },
            {
                key: 'speed',
                label: 'Drift',
                type: 'range',
                min: 0,
                max: 3,
                step: 0.05,
                default: 1
            },
        ],
        fragUniforms: "uniform vec3 u_color; uniform float u_scale;",
        fragBody: `LIQ + \`
      if (dSurf > 0.0) {\` + GLASS_ABOVE + \`} else {
        float neb = fbm(vObjPos * u_scale + vec3(0.0, u_time * u_speed * 0.06, 0.0));
        vec3 nebc = hsv2rgb(vec3(fract(0.68 + neb * 0.22), 0.75, clamp(0.22 + neb * 0.6, 0.0, 1.0)));
        float star = pow(max(snoise(vObjPos * 40.0), 0.0), 14.0);
        col = nebc + vec3(star) * 2.2;
        col += nebc * menis * 2.5;
        col += vec3(0.6, 0.7, 1.0) * pow(fres, 3.0) * 0.4;
        alpha = 0.96;
      }
    \``
    },
    {
        id: "lavalamp",
        name: "Lava lamp",
        category: "Potions & liquids",
        kind: "shader",
        swatch: ['#ff7a3a', '#4a0a5e'],
        transparent: true,
        depthWrite: false,
        gravity: true,
        params: [{
                key: 'color',
                label: 'Fluid',
                type: 'color',
                default: '#5a1470'
            },
            {
                key: 'color2',
                label: 'Blob',
                type: 'color',
                default: '#ff6a2a'
            },
            {
                key: 'fill',
                label: 'Fill',
                type: 'range',
                min: 0.02,
                max: 0.98,
                step: 0.01,
                default: 0.9
            },
            {
                key: 'speed',
                label: 'Rise',
                type: 'range',
                min: 0,
                max: 3,
                step: 0.05,
                default: 1
            },
        ],
        fragUniforms: "uniform vec3 u_color; uniform vec3 u_color2;",
        fragBody: `LIQ + \`
      if (dSurf > 0.0) {\` + GLASS_ABOVE + \`} else {
        vec3 q = pPerp * (2.6 / E) + upA * (h * 1.6 / E - u_time * u_speed * 0.5);
        float blob = snoise(q + vec3(7.7)) * 0.6 + snoise(q * 2.1 - vec3(3.1)) * 0.4;
        float bm = smoothstep(0.16, 0.42, blob);
        vec3 body = mix(u_color * 0.6, u_color2 * 1.5, bm);
        col = body * (0.55 + 0.5 * diff);
        col += u_color2 * pow(depth, 1.7) * 0.8;
        col += u_color2 * menis * 1.4;
        col += u_color2 * pow(fres, 3.0) * 0.4;
        alpha = 0.95;
      }
    \``
    },
    {
        id: "melting",
        name: "Melting / drip",
        category: "Potions & liquids",
        kind: "shader",
        swatch: ['#ffd9a0', '#a85a1e'],
        gravity: true,
        params: [{
                key: 'color',
                label: 'Wax',
                type: 'color',
                default: '#f2b56a'
            },
            {
                key: 'sag',
                label: 'Melt',
                type: 'range',
                min: 0,
                max: 0.45,
                step: 0.005,
                default: 0.2
            },
            {
                key: 'speed',
                label: 'Ooze',
                type: 'range',
                min: 0,
                max: 3,
                step: 0.05,
                default: 0.6
            },
        ],
        fragUniforms: "uniform vec3 u_color;",
        fragBody: `\`
      float streak = fbm(pPerp * (6.0 / E) + upA * (h * 3.0 / E));
      vec3 base = u_color * (0.35 + 0.65 * diff);
      col = mix(base, u_color * 1.25, smoothstep(0.2, 0.8, streak) * 0.35);
      col += vec3(1.0) * pow(max(dot(reflect(-V, N), KL), 0.0), 24.0) * 0.7;
      col += u_color * pow(fres, 3.0) * 0.4;
    \``
    },
    {
        id: "acidbath",
        name: "Acid bath",
        category: "Potions & liquids",
        kind: "shader",
        swatch: ['#ccff00', '#1a2200'],
        transparent: true,
        depthWrite: false,
        gravity: true,
        params: [{
                key: 'color',
                label: 'Acid',
                type: 'color',
                default: '#a5ff00'
            },
            {
                key: 'fill',
                label: 'Fill',
                type: 'range',
                min: 0.02,
                max: 0.98,
                step: 0.01,
                default: 0.6
            },
            {
                key: 'speed',
                label: 'Boil',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.05,
                default: 1.5
            },
        ],
        fragUniforms: "uniform vec3 u_color;",
        fragBody: `LIQ + \`
      if (dSurf > 0.0) {\` + GLASS_ABOVE + \`} else {
        float t = u_time * u_speed;
        float boil = fbm(vObjPos * 10.0 + upA * (t * 3.0));
        vec3 body = mix(vec3(0.2, 0.4, 0.0), u_color, boil);
        col = body * (0.5 + diff * 0.8);
        col += vec3(1.0, 1.0, 0.5) * pow(boil, 6.0) * 5.0; // Blinding boiling glints
        col += u_color * menis * 4.0;
        col += u_color * pow(fres, 2.0) * 1.5;
        alpha = 0.92;
      }
    \``
    },
    {
        id: "cthulhublood",
        name: "Cthulhu blood",
        category: "Potions & liquids",
        kind: "shader",
        swatch: ['#6a00b0', '#1a0022'],
        transparent: true,
        depthWrite: false,
        gravity: true,
        params: [{
                key: 'color',
                label: 'Ichor',
                type: 'color',
                default: '#8000ff'
            },
            {
                key: 'fill',
                label: 'Fill',
                type: 'range',
                min: 0.02,
                max: 0.98,
                step: 0.01,
                default: 0.7
            },
            {
                key: 'speed',
                label: 'Throb',
                type: 'range',
                min: 0,
                max: 3,
                step: 0.05,
                default: 1
            },
        ],
        fragUniforms: "uniform vec3 u_color;",
        fragBody: `LIQ + \`
      if (dSurf > 0.0) {\` + GLASS_ABOVE + \`} else {
        float t = u_time * u_speed;
        float ripple = fbm(vObjPos * 15.0 + upA * (t * 1.5));
        vec3 body = mix(vec3(0.1, 0.0, 0.1), u_color, clamp(0.5 + ripple, 0.0, 1.0));
        col = body * (0.4 + diff * 0.6);
        col += vec3(1.0, 0.2, 0.8) * pow(fres, 3.0) * 1.2;
        col += u_color * menis * 3.0;
        alpha = 1.0;
      }
    \``
    },
    {
        id: "soapbubble",
        name: "Soap bubble",
        category: "Candy & crystal",
        kind: "physical",
        swatch: ['#d0f0ff', '#c39ae0'],
        params: [{
                key: 'iridescence',
                label: 'Film',
                type: 'range',
                min: 0,
                max: 1,
                step: 0.01,
                default: 1,
                apply: (m, v) => {
                    m.iridescence = v;
                }
            },
            {
                key: 'ior',
                label: 'Thin-ness',
                type: 'range',
                min: 1,
                max: 1.6,
                step: 0.01,
                default: 1.1,
                apply: (m, v) => {
                    m.ior = v;
                }
            },
        ],
        make: `() => new THREE.MeshPhysicalMaterial({
            transmission: 1,
            thickness: 0.05,
            roughness: 0.02,
            metalness: 0,
            ior: 1.1,
            iridescence: 1,
            iridescenceIOR: 1.3,
            transparent: true,
            side: THREE.DoubleSide,
            envMapIntensity: 1.3
        })`
    },
    {
        id: "lollipop",
        name: "Lollipop swirl",
        category: "Candy & crystal",
        kind: "shader",
        swatch: ['#ff4a4a', '#fff3e0'],
        gravity: true,
        params: [{
                key: 'color',
                label: 'Stripe A',
                type: 'color',
                default: '#ff3a3a'
            },
            {
                key: 'color2',
                label: 'Stripe B',
                type: 'color',
                default: '#fff2e2'
            },
            {
                key: 'arms',
                label: 'Stripes',
                type: 'range',
                min: 2,
                max: 12,
                step: 1,
                default: 4
            },
            {
                key: 'twist',
                label: 'Twist',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.1,
                default: 1.4
            },
        ],
        fragUniforms: "uniform vec3 u_color; uniform vec3 u_color2; uniform float u_arms; uniform float u_twist;",
        fragBody: `\`
      float band = sin(ang * u_arms + hn * u_twist * 6.2831);
      vec3 base = mix(u_color, u_color2, smoothstep(-0.2, 0.2, band));
      col = base * (0.35 + 0.75 * diff);
      col += vec3(1.0) * pow(max(dot(reflect(-V, N), KL), 0.0), 60.0) * 1.2;
      col += base * pow(fres, 3.0) * 0.5;
    \``
    },
    {
        id: "disco",
        name: "Disco ball",
        category: "Candy & crystal",
        kind: "shader",
        swatch: ['#f0f4ff', '#5a5e8a'],
        params: [{
                key: 'facets',
                label: 'Facets',
                type: 'range',
                min: 4,
                max: 24,
                step: 1,
                default: 10
            },
            {
                key: 'speed',
                label: 'Party',
                type: 'range',
                min: 0,
                max: 3,
                step: 0.05,
                default: 1
            },
        ],
        fragUniforms: "uniform float u_facets;",
        fragBody: `
      vec3 qn = normalize((floor(N * u_facets) + 0.5) / u_facets);
      float gap = smoothstep(0.25, 0.55, length(N - qn) * u_facets * 0.5);
      vec3 r = reflect(-V, qn);
      float up = clamp(r.y * 0.5 + 0.5, 0.0, 1.0);
      vec3 sky = mix(vec3(0.06, 0.07, 0.1), vec3(1.0), pow(up, 3.0));
      float spark = pow(max(dot(r, KL), 0.0), 90.0) * 4.0;
      float hueSpark = pow(max(dot(r, normalize(vec3(-0.4, 0.7, 0.3))), 0.0), 80.0);
      col = sky * 0.8 + vec3(spark) + hsv2rgb(vec3(fract(u_time * 0.15 * u_speed), 0.7, 1.0)) * hueSpark * 2.0;
      col *= 1.0 - gap * 0.6;
    `
    },
    {
        id: "ice",
        name: "Glacier ice",
        category: "Candy & crystal",
        kind: "shader",
        swatch: ['#d8f4ff', '#1a5e8a'],
        params: [{
            key: 'scale',
            label: 'Cracks',
            type: 'range',
            min: 1,
            max: 12,
            step: 0.1,
            default: 5
        }, ],
        fragUniforms: "uniform float u_scale;",
        fragBody: `
      float crack = pow(1.0 - abs(snoise(vObjPos * u_scale)), 11.0);
      float glint = pow(max(snoise(vObjPos * 30.0), 0.0), 9.0);
      col = mix(vec3(0.10, 0.28, 0.42), vec3(0.85, 0.95, 1.0), clamp(0.35 + 0.35 * diff + pow(fres, 2.0) * 0.6, 0.0, 1.0));
      col += vec3(1.0) * crack * 0.5 + vec3(1.0) * glint * 1.4;
    `
    },
    {
        id: "gemstone",
        name: "Faceted gem",
        category: "Candy & crystal",
        kind: "shader",
        swatch: ['#4affd2', '#0a4a6e'],
        params: [{
                key: 'color',
                label: 'Stone',
                type: 'color',
                default: '#18c8a8'
            },
            {
                key: 'fire',
                label: 'Fire',
                type: 'range',
                min: 0,
                max: 1.5,
                step: 0.02,
                default: 0.8
            },
            {
                key: 'speed',
                label: 'Shimmer',
                type: 'range',
                min: 0,
                max: 3,
                step: 0.05,
                default: 1
            },
        ],
        fragUniforms: "uniform vec3 u_color; uniform float u_fire;",
        fragBody: `
      vec3 fN = normalize(cross(dFdx(vWorldPos), dFdy(vWorldPos)));
      if (dot(fN, V) < 0.0) fN = -fN;
      float fd = max(dot(fN, KL), 0.0);
      float fr = pow(1.0 - max(dot(fN, V), 0.0), 2.0);
      vec3 disp = hsv2rgb(vec3(fract(snoise(fN * 2.7) * 0.5 + u_time * u_speed * 0.02), 0.7, 1.0));
      float spec = pow(max(dot(reflect(-V, fN), KL), 0.0), 50.0);
      col = u_color * (0.25 + 0.75 * fd) + disp * fr * u_fire + vec3(spec) * 1.6;
    `
    },
    {
        id: "tesseract",
        name: "Quantum tesseract",
        category: "Candy & crystal",
        kind: "shader",
        swatch: ['#00ffff', '#000033'],
        transparent: true,
        depthWrite: false,
        additive: true,
        params: [{
            key: 'speed',
            label: 'Shift',
            type: 'range',
            min: 0,
            max: 3,
            step: 0.05,
            default: 1
        }, ],
        fragBody: `
      float t = u_time * u_speed;
      vec3 fN = normalize(floor(N * 4.0) / 4.0);
      float fd = max(dot(fN, KL), 0.0);
      float fr = pow(1.0 - max(dot(fN, V), 0.0), 3.0);
      float grid = smoothstep(0.9, 1.0, sin(vObjPos.x * 15.0 + t) * sin(vObjPos.y * 15.0 + t) * sin(vObjPos.z * 15.0 + t));
      vec3 base = hsv2rgb(vec3(fract(t * 0.05), 0.8, 0.1 + fd * 0.2));
      col = base + vec3(fr) * 0.8;
      col += vec3(1.0, 0.5, 1.0) * grid * 3.0; // Pulsing hyper-dimensional intersections
      alpha = clamp(0.2 + fr + grid, 0.0, 1.0);
    `
    },
    {
        id: "meatgrinder",
        name: "Raw meat",
        category: "Chaos & Eldritch",
        kind: "shader",
        swatch: ['#aa0000', '#330000'],
        params: [{
            key: 'speed',
            label: 'Pulse',
            type: 'range',
            min: 0,
            max: 3,
            step: 0.05,
            default: 1
        }, ],
        fragBody: `
      float t = u_time * u_speed;
      float sinew = fbm(vObjPos * 8.0 + t * 0.2);
      float grain = abs(snoise(vObjPos * 20.0));
      vec3 flesh = mix(vec3(0.3, 0.0, 0.0), vec3(0.8, 0.2, 0.2), sinew);
      col = flesh * (0.3 + diff * 0.7);
      col += vec3(1.0, 0.8, 0.8) * pow(grain, 8.0) * (0.5 + 0.5 * sin(t * 5.0)); // Pulsing fibers
      col += vec3(0.5, 0.0, 0.0) * pow(fres, 2.0) * 0.8;
    `
    },
    {
        id: "turing",
        name: "Turing Patterns",
        category: "Biomimetic & organic",
        kind: "shader",
        swatch: ['#ff6a9a', '#1a0a3a'],
        gravity: true,
        params: [{
                key: 'feed',
                label: 'Feed Rate',
                type: 'range',
                min: 0.01,
                max: 0.12,
                step: 0.001,
                default: 0.055
            },
            {
                key: 'kill',
                label: 'Kill Rate',
                type: 'range',
                min: 0.03,
                max: 0.18,
                step: 0.001,
                default: 0.062
            },
            {
                key: 'iter',
                label: 'Iterations',
                type: 'range',
                min: 1,
                max: 6,
                step: 1,
                default: 4
            },
            {
                key: 'scaleRd',
                label: 'Detail',
                type: 'range',
                min: 2,
                max: 15,
                step: 0.5,
                default: 6
            },
            {
                key: 'speed',
                label: 'Morph',
                type: 'range',
                min: 0,
                max: 2,
                step: 0.05,
                default: 0.5
            },
        ],
        fragUniforms: "uniform float u_feed; uniform float u_kill; uniform float u_iter; uniform float u_scaleRd;",
        fragBody: `\`
      // Reaction-diffusion (Gray-Scott style) approximated via iterative fbm
      // u_feed = F, u_kill = K — the classic Turing parameter space
      float t = u_time * u_speed;
      vec3 q = vObjPos * u_scaleRd;
      float u = fbm(q + vec3(t * 0.1, 0.0, 0.0));
      float v = fbm(q + vec3(0.0, 0.0, t * 0.1) + 3.7);
      // Crude iterative Laplacian-like blending
      for (int i = 0; i < 6; i++) {
        if (float(i) >= u_iter) break;
        float fi = float(i) * 0.15;
        float lapU = fbm(q * (1.0 + fi * 0.5) + vec3(t * 0.08 * fi, 0.0, 0.0));
        float lapV = fbm(q * (1.0 + fi * 0.5) + vec3(0.0, 0.0, t * 0.08 * fi) + 5.1);
        float uvv = u * v * v;
        u += (u_feed * (1.0 - u) - uvv + 0.012 * lapU) * 0.3;
        v += (uvv - (u_feed + u_kill) * v + 0.012 * lapV) * 0.3;
      }
      float spot = clamp(u * 0.7 + v * 0.3, 0.0, 1.0);
      float stripe = clamp(abs(u - v), 0.0, 1.0);
      float pattern = mix(spot, stripe, 0.45);
      // Map to rich organic palette
      vec3 lo = vec3(0.06, 0.01, 0.08);
      vec3 hiA = hsv2rgb(vec3(0.92, 0.9, 1.0));  // hot pink
      vec3 hiB = hsv2rgb(vec3(0.15, 0.9, 1.0));  // gold
      vec3 hi = mix(hiA, hiB, smoothstep(0.3, 0.7, pattern));
      col = mix(lo, hi, pattern) * (0.55 + diff * 0.6);
      col += hi * pow(fres, 2.5) * 0.9;
    \``
    },
    {
        id: "bioluminescent",
        name: "Bioluminescent Abyss",
        category: "Biomimetic & organic",
        kind: "shader",
        swatch: ['#001a2a', '#3affd8'],
        gravity: true,
        transparent: true,
        additive: true,
        depthWrite: false,
        params: [{
                key: 'baseDark',
                label: 'Abyss',
                type: 'color',
                default: '#00101e'
            },
            {
                key: 'glowA',
                label: 'Glow A',
                type: 'color',
                default: '#2effc8'
            },
            {
                key: 'glowB',
                label: 'Glow B',
                type: 'color',
                default: '#ff3a8a'
            },
            {
                key: 'density',
                label: 'Spot Density',
                type: 'range',
                min: 2,
                max: 18,
                step: 0.5,
                default: 7
            },
            {
                key: 'pulseSpd',
                label: 'Pulse Speed',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.05,
                default: 1.5
            },
            {
                key: 'flicker',
                label: 'Flicker',
                type: 'range',
                min: 0,
                max: 1,
                step: 0.01,
                default: 0.6
            },
        ],
        fragUniforms: "uniform vec3 u_baseDark; uniform vec3 u_glowA; uniform vec3 u_glowB; uniform float u_density; uniform float u_pulseSpd; uniform float u_flicker;",
        fragBody: `\`
      float t = u_time * u_pulseSpd;
      // Organic spot field — scattered bioluminescent colonies
      float n1 = snoise(vObjPos * u_density + vec3(0.0, t * 0.3, 0.0));
      float n2 = snoise(vObjPos * u_density * 1.7 + vec3(t * 0.2, 0.0, 0.0) + 3.7);
      float n3 = snoise(vObjPos * u_density * 2.3 + vec3(0.0, 0.0, t * 0.25) + 7.1);
      // Multi-scale spot detection
      float spot = max(max(
        smoothstep(0.72, 0.88, n1),
        smoothstep(0.75, 0.90, n2)),
        smoothstep(0.78, 0.92, n3)
      );
      // Each spot pulses independently via secondary noise
      float pulsePhase = snoise(vObjPos * 3.0 + vec3(t * 0.7));
      float pulse = 0.55 + 0.45 * sin(t * 2.5 + pulsePhase * 8.0);
      // Flicker: random dropouts
      float flick = 1.0 - u_flicker * step(0.88, snoise(vObjPos * 45.0 + t * 30.0));
      float glow = spot * pulse * flick;
      // Depth haze: spots fade in the depths
      float depthAtten = 1.0 - pow(clamp(-dSurf / E, 0.0, 1.0) * 0.6, 1.5);
      // Blend between two bioluminescent colors
      float colorMix = snoise(vObjPos * 2.0 + vec3(0.0, 0.0, t * 0.1));
      vec3 glowCol = mix(u_glowA, u_glowB, smoothstep(-0.3, 0.3, colorMix));
      // Compose: dark base + glowing spots
      col = u_baseDark * (0.15 + diff * 0.3);
      col += glowCol * glow * depthAtten * 3.5;
      col += glowCol * pow(fres, 3.5) * 0.6;
      alpha = clamp(0.15 + glow * depthAtten * 0.85, 0.0, 1.0);
    \``
    },
    {
        id: "vhsglitch",
        name: "VHS Tracking",
        category: "Synthwave & retro",
        kind: "shader",
        swatch: ['#ff55aa', '#111111'],
        gravity: true,
        params: [{
                key: 'trackErr',
                label: 'Tracking Error',
                type: 'range',
                min: 0,
                max: 1,
                step: 0.01,
                default: 0.5
            },
            {
                key: 'staticA',
                label: 'Static Amount',
                type: 'range',
                min: 0,
                max: 1,
                step: 0.01,
                default: 0.35
            },
            {
                key: 'colorBleed',
                label: 'Color Bleed',
                type: 'range',
                min: 0,
                max: 1,
                step: 0.01,
                default: 0.6
            },
            {
                key: 'scanlines',
                label: 'Scanlines',
                type: 'range',
                min: 0,
                max: 1,
                step: 0.01,
                default: 0.8
            },
            {
                key: 'speed',
                label: 'Jitter',
                type: 'range',
                min: 0,
                max: 4,
                step: 0.05,
                default: 1.8
            },
        ],
        fragUniforms: "uniform float u_trackErr; uniform float u_staticA; uniform float u_colorBleed; uniform float u_scanlines;",
        fragBody: `\`
      float t = u_time * u_speed;
      // VHS: scanlines, tracking tearing, chroma bleed, static
      float scanline = sin(hn * 400.0) * 0.5 + 0.5;
      float scanMask = 0.75 + 0.25 * scanline * u_scanlines;
      // Tracking: horizontal bands of corruption
      float track = snoise(vec3(0.0, hn * 8.0 + t * 0.9, t * 4.0));
      float tear = smoothstep(0.82, 0.95, track) * u_trackErr;
      // Color channel displacement (chroma bleed)
      float rOff = u_colorBleed * 0.025 * snoise(vec3(hn * 30.0, t * 12.0, 0.0));
      float bOff = u_colorBleed * 0.025 * snoise(vec3(hn * 30.0, t * 12.0, 3.3));
      // Sample "image" via fbm (simulates recorded content)
      vec3 pR = vObjPos + upA * rOff;
      vec3 pB = vObjPos + upA * bOff;
      float imgR = fbm(pR * 3.0 + vec3(t * 0.15, 0.0, 0.0));
      float imgG = fbm(vObjPos * 3.0 + vec3(0.0, t * 0.15, 0.0));
      float imgB = fbm(pB * 3.0 + vec3(0.0, 0.0, t * 0.15));
      vec3 img = vec3(imgR, imgG, imgB) * 0.5 + 0.5;
      // Static snow
      float snow = snoise(vObjPos * 60.0 + t * 55.0) * 0.5 + 0.5;
      snow = smoothstep(0.65, 0.8, snow) * u_staticA;
      // Color-shifted fringe on tears
      vec3 tearCol = vec3(1.0, 0.1, 0.3) * tear;
      // Compose
      col = img * scanMask * (0.5 + diff * 0.5);
      col += vec3(snow);
      col += tearCol;
      col += vec3(0.8, 0.2, 0.6) * pow(fres, 4.0) * 0.4;
    \``
    },
    {
        id: "woven",
        name: "Woven Fabric",
        category: "Textiles",
        kind: "shader",
        swatch: ["#5d4037", "#8d6e63"],
        transparent: false,
        additive: false,
        depthWrite: true,
        params: [{
                key: "thread1Color",
                label: "Thread Color 1",
                type: "color",
                default: "#a0522d"
            },
            {
                key: "thread2Color",
                label: "Thread Color 2",
                type: "color",
                default: "#deb887"
            },
            {
                key: "weaveScale",
                label: "Weave Scale",
                type: "float",
                default: 8.0,
                min: 2.0,
                max: 20.0
            },
            {
                key: "fuzziness",
                label: "Fuzziness",
                type: "float",
                default: 0.3,
                min: 0.0,
                max: 1.0
            },
            {
                key: "specular",
                label: "Specular",
                type: "float",
                default: 0.6,
                min: 0.0,
                max: 1.5
            },
            {
                key: "twist",
                label: "Twist",
                type: "float",
                default: 0.2,
                min: 0.0,
                max: 1.0
            }
        ],
        fragUniforms: `
      uniform vec3 u_thread1Color;
      uniform vec3 u_thread2Color;
      uniform float u_weaveScale;
      uniform float u_fuzziness;
      uniform float u_specular;
      uniform float u_twist;
    `,
        fragBody: `
      vec2 uv = vWorldPos.xy * u_weaveScale;
      float patternX = abs(sin(uv.x * 3.14159 + uv.y * u_twist)) * 0.5 + 0.5;
      float patternY = abs(sin(uv.y * 3.14159 + uv.x * u_twist)) * 0.5 + 0.5;
      float weave = (patternX * patternY) * 0.8 + 0.2;
      float fuzzy = fbm(vec3(uv * 0.5, 0.3)) * u_fuzziness;
      vec3 base = mix(u_thread1Color, u_thread2Color, weave);
      base += fuzzy * 0.15;
      float spec = pow(max(0.0, dot(N, normalize(V + KL))), 20.0) * u_specular;
      spec *= 0.5 + 0.5 * weave;
      col = base * (diff * 0.7 + 0.4) + spec * base * 0.5;
      alpha = 1.0;
    `
    },
    {
        id: "stained_glass",
        name: "Stained Glass",
        category: "Architectural",
        kind: "shader",
        swatch: ["#000000", "#ffcc00"],
        transparent: true,
        additive: false,
        depthWrite: false,
        params: [{
                key: "leadColor",
                label: "Lead Color",
                type: "color",
                default: "#222222"
            },
            {
                key: "glassOpacity",
                label: "Glass Opacity",
                type: "float",
                default: 0.85,
                min: 0.2,
                max: 1.0
            },
            {
                key: "panelScale",
                label: "Panel Scale",
                type: "float",
                default: 4.0,
                min: 1.0,
                max: 10.0
            },
            {
                key: "hueVar",
                label: "Hue Variation",
                type: "float",
                default: 0.6,
                min: 0.0,
                max: 1.0
            },
            {
                key: "brightness",
                label: "Brightness",
                type: "float",
                default: 1.2,
                min: 0.5,
                max: 2.0
            },
            {
                key: "specular",
                label: "Specular",
                type: "float",
                default: 0.5,
                min: 0.0,
                max: 1.0
            }
        ],
        fragUniforms: `
      uniform vec3 u_leadColor;
      uniform float u_glassOpacity;
      uniform float u_panelScale;
      uniform float u_hueVar;
      uniform float u_brightness;
      uniform float u_specular;
    `,
        fragBody: `
      vec2 st = vWorldPos.xy * u_panelScale;
      vec2 i_st = floor(st);
      vec2 f_st = fract(st);
      float minDist = 1.0;
      for (int m = -1; m <= 1; m++) {
        for (int n = -1; n <= 1; n++) {
          vec2 neighbor = vec2(float(m), float(n));
          vec2 point = vec2(
            snoise(vec3(i_st + neighbor, 0.0)),
            snoise(vec3(i_st + neighbor, 1.0))
          ) * 0.5 + 0.5;
          vec2 diff = neighbor + point - f_st;
          float d = dot(diff, diff);
          minDist = min(minDist, d);
        }
      }
      float cellIndex = snoise(vec3(i_st, 2.0)) * 0.5 + 0.5;
      float hue = fract(cellIndex * u_hueVar + u_time * 0.01);
      vec3 glassCol = hsv2rgb(vec3(hue, 0.6, 0.7 * u_brightness));
      float border = 1.0 - smoothstep(0.0, 0.08, sqrt(minDist));
      vec3 baseCol = mix(glassCol, u_leadColor, border);
      float spec = pow(max(0.0, dot(N, normalize(V + KL))), 40.0) * u_specular;
      col = baseCol * (diff * 0.7 + 0.4) + spec * glassCol * 0.4;
      alpha = mix(u_glassOpacity, 1.0, border);
    `
    },
    {
        id: "subsurface_scatter",
        name: "Subsurface Scatter",
        category: "Biomimetic & Organic",
        kind: "shader",
        swatch: ["#ffccaa", "#ff8866"],
        transparent: false,
        additive: false,
        depthWrite: true,
        params: [{
                key: "surfaceColor",
                label: "Surface Color",
                type: "color",
                default: "#f4a460"
            },
            {
                key: "subsurfaceColor",
                label: "Subsurface Color",
                type: "color",
                default: "#ff4500"
            },
            {
                key: "scatterDepth",
                label: "Scatter Depth",
                type: "float",
                default: 0.6,
                min: 0.1,
                max: 1.5
            },
            {
                key: "fuzziness",
                label: "Fuzziness",
                type: "float",
                default: 0.4,
                min: 0.0,
                max: 1.0
            },
            {
                key: "backlightStr",
                label: "Backlight Strength",
                type: "float",
                default: 0.7,
                min: 0.0,
                max: 2.0
            },
            {
                key: "radius",
                label: "Radius",
                type: "float",
                default: 0.5,
                min: 0.1,
                max: 1.0
            }
        ],
        fragUniforms: `
      uniform vec3 u_surfaceColor;
      uniform vec3 u_subsurfaceColor;
      uniform float u_scatterDepth;
      uniform float u_fuzziness;
      uniform float u_backlightStr;
      uniform float u_radius;
    `,
        fragBody: `
      float NoL = max(0.0, dot(N, KL));
      float backLight = max(0.0, dot(N, -KL));
      float thickness = fbm(vec3(vWorldPos * 3.0, 0.2)) * u_radius;
      thickness += snoise(vec3(vWorldPos * 8.0, 0.7)) * u_fuzziness * 0.3;
      float scatter = exp(-thickness * u_scatterDepth);
      vec3 sssCol = u_subsurfaceColor * scatter * backLight * u_backlightStr;
      vec3 surface = u_surfaceColor * (NoL * 0.7 + 0.3);
      vec3 spec = pow(max(0.0, dot(N, normalize(V + KL))), 20.0) * 0.25;
      col = surface + sssCol + spec;
      col = mix(col, u_surfaceColor, 0.1 * u_fuzziness);
      alpha = 1.0;
    `
    },
    {
        id: "moss",
        name: "Moss",
        category: "Biomimetic & Organic",
        kind: "shader",
        swatch: ["#2e4a22", "#1b2e0e"],
        transparent: false,
        additive: false,
        depthWrite: true,
        params: [{
                key: "mossColor",
                label: "Moss Color",
                type: "color",
                default: "#3a5f2d"
            },
            {
                key: "baseColor",
                label: "Base Color",
                type: "color",
                default: "#4a3b32"
            },
            {
                key: "density",
                label: "Density",
                type: "float",
                default: 0.7,
                min: 0.0,
                max: 1.0
            },
            {
                key: "clumpSize",
                label: "Clump Size",
                type: "float",
                default: 3.0,
                min: 1.0,
                max: 8.0
            },
            {
                key: "humidity",
                label: "Humidity Sheen",
                type: "float",
                default: 0.4,
                min: 0.0,
                max: 1.0
            },
            {
                key: "coverage",
                label: "Coverage",
                type: "float",
                default: 0.6,
                min: 0.0,
                max: 1.0
            }
        ],
        fragUniforms: `
      uniform vec3 u_mossColor;
      uniform vec3 u_baseColor;
      uniform float u_density;
      uniform float u_clumpSize;
      uniform float u_humidity;
      uniform float u_coverage;
    `,
        fragBody: `
      vec3 pos = vWorldPos * u_clumpSize;
      float n = fbm(vec3(pos.xy, 0.2)) * 0.6 + fbm(vec3(pos.xy * 2.0, 0.8)) * 0.4;
      float mossMask = smoothstep(0.4, 0.6, n) * u_coverage;
      mossMask *= smoothstep(0.2, 0.4, vWorldPos.y + 0.5) * u_density;
      vec3 mossCol = u_mossColor * (0.8 + 0.2 * fbm(vec3(pos.xy * 3.0, u_time * 0.02)));
      float mossSpec = pow(max(0.0, dot(N, normalize(V + KL))), 10.0) * u_humidity;
      col = mix(u_baseColor, mossCol, mossMask) * (diff * 0.7 + 0.4);
      col += mossSpec * u_mossColor * 0.3 * mossMask;
      alpha = 1.0;
    `
    },
    {
        id: "snake_skin",
        name: "Snake Skin",
        category: "Biomimetic & Organic",
        kind: "shader",
        swatch: "#556B2F",
        flags: {},
        params: [{
                name: 'Scale Size',
                default: 1.5,
                min: 0.5,
                max: 3.0
            },
            {
                name: 'Iridescence',
                default: 0.7,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Pattern Complexity',
                default: 0.6,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Color Shift',
                default: 0.4,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Contrast',
                default: 1.0,
                min: 0.5,
                max: 2.0
            },
        ],
        fragUniforms: `
      uniform float u_scaleSize;
      uniform float u_iridescence;
      uniform float u_patternComplexity;
      uniform float u_colorShift;
      uniform float u_contrast;
    `,
        fragBody: `
      vec2 uv = vUv * u_scaleSize;
      vec2 id = floor(uv);
      vec2 gv = fract(uv) - 0.5;
      float shape = length(gv) * 2.0;
      float hex = abs(gv.x) + abs(gv.y) * 0.7;
      float scale = mix(hex, shape, u_patternComplexity);

      float edge = 1.0 - smoothstep(0.35, 0.5, scale);
      float inner = smoothstep(0.15, 0.3, scale) * 0.6;
      float pattern = edge + inner;

      float hue = fract((id.x * 0.3 + id.y * 0.5) * 0.7 + uTime * 0.05 * u_colorShift);
      vec3 baseColor = hsv2rgb(vec3(hue, 0.6, 0.7));
      vec3 iri = hsv2rgb(vec3(fract(hue + 0.5), 0.8, 0.9));
      vec3 col = mix(baseColor, iri, u_iridescence * pattern);
      col = mix(vec3(0.5), col, u_contrast);
      gl_FragColor = vec4(col, 1.0);
    `
    },
    {
        id: "caustics",
        name: "Caustics",
        category: "Water & Fluid",
        kind: "shader",
        swatch: ["#003344", "#88ccff"],
        transparent: false,
        additive: false,
        depthWrite: true,
        params: [{
                key: "depth",
                label: "Depth",
                type: "float",
                default: 1.0,
                min: 0.1,
                max: 3.0
            },
            {
                key: "intensity",
                label: "Intensity",
                type: "float",
                default: 1.0,
                min: 0.0,
                max: 2.0
            },
            {
                key: "waveFreq",
                label: "Wave Frequency",
                type: "float",
                default: 2.0,
                min: 0.5,
                max: 8.0
            },
            {
                key: "sharpness",
                label: "Caustic Sharpness",
                type: "float",
                default: 0.7,
                min: 0.1,
                max: 1.5
            },
            {
                key: "dispersion",
                label: "Color Dispersion",
                type: "float",
                default: 0.3,
                min: 0.0,
                max: 1.0
            }
        ],
        fragUniforms: `
      uniform float u_depth;
      uniform float u_intensity;
      uniform float u_waveFreq;
      uniform float u_sharpness;
      uniform float u_dispersion;
    `,
        fragBody: `
      vec2 uv = vWorldPos.xy * u_waveFreq;
      float t = u_time;
      float n1 = snoise(vec3(uv + vec2(0.0, t * 0.3), 0.5));
      float n2 = snoise(vec3(uv * 1.7 - vec2(t * 0.2, 0.0), 0.9));
      float ddx = n1 - snoise(vec3(uv + vec2(0.01, 0.0), 0.5));
      float ddy = n1 - snoise(vec3(uv + vec2(0.0, 0.01), 0.5));
      float curvature = abs(ddx) + abs(ddy);
      float disp = u_dispersion * 0.01;
      float causticR = 1.0 - smoothstep(0.0, u_sharpness, abs(snoise(vec3(uv + disp, 0.5)) - curvature));
      float causticG = 1.0 - smoothstep(0.0, u_sharpness, abs(snoise(vec3(uv, 0.6)) - curvature));
      float causticB = 1.0 - smoothstep(0.0, u_sharpness, abs(snoise(vec3(uv - disp, 0.7)) - curvature));
      float mask = (causticR + causticG + causticB) * 0.33;
      vec3 causticCol = vec3(causticR, causticG, causticB) * u_intensity;
      vec3 waterCol = vec3(0.0, 0.1, 0.2) * u_depth;
      col = waterCol + causticCol * 1.5;
      alpha = 1.0;
    `
    },
    {
        id: "crystal_lattice",
        name: "Crystal Lattice",
        category: "Candy & Crystal",
        kind: "shader",
        swatch: ["#ee82ee", "#ffffff"],
        transparent: false,
        additive: false,
        depthWrite: true,
        params: [{
                key: "baseHue",
                label: "Base Hue",
                type: "float",
                default: 0.8,
                min: 0.0,
                max: 1.0
            },
            {
                key: "facetScale",
                label: "Facet Scale",
                type: "float",
                default: 6.0,
                min: 2.0,
                max: 20.0
            },
            {
                key: "impurity",
                label: "Impurity",
                type: "float",
                default: 0.2,
                min: 0.0,
                max: 1.0
            },
            {
                key: "growthRings",
                label: "Growth Rings",
                type: "float",
                default: 0.4,
                min: 0.0,
                max: 1.0
            },
            {
                key: "specHard",
                label: "Specular Hardness",
                type: "float",
                default: 80.0,
                min: 10.0,
                max: 200.0
            },
            {
                key: "internalGlow",
                label: "Internal Glow",
                type: "float",
                default: 0.15,
                min: 0.0,
                max: 0.5
            }
        ],
        fragUniforms: `
      uniform float u_baseHue;
      uniform float u_facetScale;
      uniform float u_impurity;
      uniform float u_growthRings;
      uniform float u_specHard;
      uniform float u_internalGlow;
    `,
        fragBody: `
      vec3 p = vWorldPos * u_facetScale;
      vec3 p_fract = fract(p) - 0.5;
      vec3 p_floor = floor(p);
      float seed = snoise(vec3(p_floor, 0.3));
      float impurityNoise = snoise(vec3(p_floor * 2.0, 0.9)) * u_impurity;
      float hue = fract(u_baseHue + impurityNoise * 0.4);
      vec3 crystalCol = hsv2rgb(vec3(hue, 0.5, 0.9));
      float ring = 1.0 - abs(snoise(vec3(p * 0.3, u_time * 0.05)) * u_growthRings);
      crystalCol = mix(crystalCol, crystalCol * 0.8, ring);
      float facet = max(max(abs(p_fract.x), abs(p_fract.y)), abs(p_fract.z));
      float facetMask = 1.0 - smoothstep(0.4, 0.45, facet);
      vec3 facetN = faceforward(vec3(1.0, 0.0, 0.0), V, N);
      float spec = pow(max(0.0, dot(N, normalize(V + KL))), u_specHard);
      spec *= facetMask * 0.8;
      vec3 glow = crystalCol * u_internalGlow;
      col = crystalCol * (diff * 0.5 + 0.5) + spec * 1.2 + glow;
      alpha = 1.0;
    `
    },
    {
        id: "geode",
        name: "Geode Crystal",
        category: "Candy & Crystal",
        kind: "shader",
        swatch: "#DA70D6",
        flags: {},
        params: [{
                name: 'Crystal Density',
                default: 0.8,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Facet Sharpness',
                default: 0.7,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Color Shift',
                default: 0.5,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Cavity Depth',
                default: 0.6,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Glow',
                default: 0.2,
                min: 0.0,
                max: 0.5
            },
        ],
        fragUniforms: `
      uniform float u_crystalDensity;
      uniform float u_facetSharpness;
      uniform float u_colorShift;
      uniform float u_cavityDepth;
      uniform float u_glow;
    `,
        fragBody: `
      vec2 uv = vUv;
      vec2 center = vec2(0.5, 0.5);
      float dist = length(uv - center) * 2.0;
      float cavity = smoothstep(0.2, 0.5, dist) * u_cavityDepth;
      float angle = atan(uv.y - center.y, uv.x - center.x);
      float crystals = abs(sin(angle * 8.0)) * u_crystalDensity;
      crystals += abs(sin(dist * 15.0)) * 0.3;
      crystals = pow(crystals, u_facetSharpness * 3.0);
      float pattern = crystals * (1.0 - cavity * 0.7);
      vec3 col = hsv2rgb(vec3(fract(angle / 6.28318 * u_colorShift), 0.8, 0.9));
      col *= pattern;
      col += u_glow * vec3(0.7, 0.2, 0.7) * (1.0 - dist);
      gl_FragColor = vec4(col, 1.0);
    `
    },
    {
        id: "topographical",
        name: "Topographical",
        category: "Maps & Cartography",
        kind: "shader",
        swatch: ["#228b22", "#8b4513"],
        transparent: false,
        additive: false,
        depthWrite: true,
        params: [{
                key: "lowColor",
                label: "Low Elevation Color",
                type: "color",
                default: "#006400"
            },
            {
                key: "highColor",
                label: "High Elevation Color",
                type: "color",
                default: "#d2b48c"
            },
            {
                key: "contourSpacing",
                label: "Contour Spacing",
                type: "float",
                default: 0.15,
                min: 0.05,
                max: 0.5
            },
            {
                key: "relief",
                label: "Relief Intensity",
                type: "float",
                default: 0.5,
                min: 0.0,
                max: 1.0
            },
            {
                key: "waterColor",
                label: "Water Color",
                type: "color",
                default: "#1e90ff"
            },
            {
                key: "terrainScale",
                label: "Terrain Scale",
                type: "float",
                default: 2.5,
                min: 1.0,
                max: 8.0
            }
        ],
        fragUniforms: `
      uniform vec3 u_lowColor;
      uniform vec3 u_highColor;
      uniform float u_contourSpacing;
      uniform float u_relief;
      uniform vec3 u_waterColor;
      uniform float u_terrainScale;
    `,
        fragBody: `
      vec2 uv = vWorldPos.xy * u_terrainScale;
      float height = fbm(vec3(uv, 0.5)) * 0.7 + snoise(vec3(uv * 2.0, 0.9)) * 0.3;
      height = height * 0.5 + 0.5;
      float contour = abs(fract(height / u_contourSpacing) - 0.5) * 2.0;
      contour = 1.0 - smoothstep(0.0, 0.03, contour);
      vec3 land = mix(u_lowColor, u_highColor, height);
      float water = step(height, 0.35);
      vec3 colTerrain = mix(land, u_waterColor, water);
      colTerrain = mix(colTerrain, vec3(0.0), contour * 0.6);
      float reliefShade = (diff * 0.5 + 0.5) * (0.8 + 0.2 * u_relief);
      col = colTerrain * reliefShade;
      alpha = 1.0;
    `
    },
    {
        id: "marble",
        name: "Marble",
        category: "Stone & Mineral",
        kind: "shader",
        swatch: ["#f5f5dc", "#2f4f4f"],
        transparent: false,
        additive: false,
        depthWrite: true,
        params: [{
                key: "stoneColor",
                label: "Stone Color",
                type: "color",
                default: "#f0ead6"
            },
            {
                key: "veinColor",
                label: "Vein Color",
                type: "color",
                default: "#3b3b3b"
            },
            {
                key: "veinScale",
                label: "Vein Scale",
                type: "float",
                default: 2.0,
                min: 0.5,
                max: 5.0
            },
            {
                key: "sharpness",
                label: "Vein Sharpness",
                type: "float",
                default: 0.5,
                min: 0.1,
                max: 1.0
            },
            {
                key: "polish",
                label: "Polish",
                type: "float",
                default: 0.8,
                min: 0.0,
                max: 1.5
            },
            {
                key: "turbulence",
                label: "Turbulence",
                type: "float",
                default: 0.6,
                min: 0.0,
                max: 1.0
            }
        ],
        fragUniforms: `
      uniform vec3 u_stoneColor;
      uniform vec3 u_veinColor;
      uniform float u_veinScale;
      uniform float u_sharpness;
      uniform float u_polish;
      uniform float u_turbulence;
    `,
        fragBody: `
      vec3 p = vWorldPos * u_veinScale;
      float n = fbm(vec3(p.xy, 0.3) + vec3(snoise(vec3(p.xy * u_turbulence, 0.7)), 0.0, 0.0));
      n = abs(n * 2.0 - 1.0);
      n = 1.0 - pow(n, u_sharpness * 3.0);
      vec3 marbleCol = mix(u_stoneColor, u_veinColor, n);
      float spec = pow(max(0.0, dot(N, normalize(V + KL))), 60.0) * u_polish;
      col = marbleCol * (diff * 0.6 + 0.5) + spec * 0.7;
      alpha = 1.0;
    `
    },
    {
        id: "cracked_glass",
        name: "Cracked Glass",
        category: "Destruction & Stress",
        kind: "shader",
        swatch: ["#aaddff", "#ffffff"],
        transparent: true,
        additive: false,
        depthWrite: false,
        params: [{
                key: "glassTint",
                label: "Glass Tint",
                type: "color",
                default: "#cceeff"
            },
            {
                key: "crackDensity",
                label: "Crack Density",
                type: "float",
                default: 1.5,
                min: 0.5,
                max: 4.0
            },
            {
                key: "crackWidth",
                label: "Crack Width",
                type: "float",
                default: 0.03,
                min: 0.01,
                max: 0.1
            },
            {
                key: "refractionStr",
                label: "Refraction Strength",
                type: "float",
                default: 0.15,
                min: 0.0,
                max: 0.4
            },
            {
                key: "stressIntensity",
                label: "Stress Intensity",
                type: "float",
                default: 0.5,
                min: 0.0,
                max: 1.0
            },
            {
                key: "shine",
                label: "Shine",
                type: "float",
                default: 1.0,
                min: 0.0,
                max: 2.0
            }
        ],
        fragUniforms: `
      uniform vec3 u_glassTint;
      uniform float u_crackDensity;
      uniform float u_crackWidth;
      uniform float u_refractionStr;
      uniform float u_stressIntensity;
      uniform float u_shine;
    `,
        fragBody: `
      vec2 uv = vWorldPos.xy * u_crackDensity;
      float t = u_time * 0.05;
      float crack = 0.0;
      vec2 seed = vec2(snoise(vec3(floor(uv), 0.0)), snoise(vec3(floor(uv), 1.0)));
      vec2 f = fract(uv) - 0.5;
      float dist = length(f - seed * 0.5);
      crack = 1.0 - smoothstep(u_crackWidth, u_crackWidth + 0.02, dist);
      float stress = snoise(vec3(uv * 0.5, t)) * u_stressIntensity;
      crack += stress * 0.3;
      vec3 refractN = N + vec3(snoise(vec3(vWorldPos.xy * 10.0, t)), snoise(vec3(vWorldPos.xy * 10.0, t + 1.0)), 0.0) * u_refractionStr;
      refractN = normalize(refractN);
      float spec = pow(max(0.0, dot(refractN, normalize(V + KL))), 100.0) * u_shine;
      vec3 glassCol = u_glassTint * (0.7 + 0.3 * fres);
      float alphaCrack = clamp(crack, 0.0, 1.0);
      col = mix(glassCol, vec3(1.0), spec);
      alpha = mix(0.15, 0.95, alphaCrack);
    `
    },
    {
        id: "sand",
        name: "Sand",
        category: "Natural Terrain",
        kind: "shader",
        swatch: ["#c2b280", "#8b7355"],
        transparent: false,
        additive: false,
        depthWrite: true,
        params: [{
                key: "sandColor",
                label: "Sand Color",
                type: "color",
                default: "#e6c280"
            },
            {
                key: "shadowColor",
                label: "Shadow Color",
                type: "color",
                default: "#8b7355"
            },
            {
                key: "grainSize",
                label: "Grain Size",
                type: "float",
                default: 10.0,
                min: 2.0,
                max: 30.0
            },
            {
                key: "duneScale",
                label: "Dune Scale",
                type: "float",
                default: 1.5,
                min: 0.5,
                max: 5.0
            },
            {
                key: "clumpiness",
                label: "Clumpiness",
                type: "float",
                default: 0.3,
                min: 0.0,
                max: 1.0
            },
            {
                key: "specular",
                label: "Specular",
                type: "float",
                default: 0.2,
                min: 0.0,
                max: 0.8
            }
        ],
        fragUniforms: `
      uniform vec3 u_sandColor;
      uniform vec3 u_shadowColor;
      uniform float u_grainSize;
      uniform float u_duneScale;
      uniform float u_clumpiness;
      uniform float u_specular;
    `,
        fragBody: `
      vec2 uv = vWorldPos.xy * u_duneScale;
      float dune = fbm(vec3(uv, 0.3)) * 0.7 + snoise(vec3(uv * 0.5, 0.8)) * 0.3;
      vec2 grainUV = vWorldPos.xy * u_grainSize;
      float grain = snoise(vec3(grainUV, 0.5)) * 0.5 + 0.5;
      grain = grain * 0.8 + 0.2 * snoise(vec3(grainUV * 2.0, 0.9));
      float clump = snoise(vec3(grainUV * 0.2, 0.7));
      grain = mix(grain, 0.5, clump * u_clumpiness);
      vec3 sandCol = mix(u_shadowColor, u_sandColor, grain);
      sandCol = mix(sandCol, u_sandColor, dune * 0.4);
      float spec = pow(max(0.0, dot(N, normalize(V + KL))), 30.0) * u_specular;
      col = sandCol * (diff * 0.8 + 0.3) + spec * u_sandColor * 0.2;
      alpha = 1.0;
    `
    },
    {
        id: "emblem",
        name: "Emblem",
        category: "Heraldry & Symbols",
        kind: "shader",
        swatch: ["#800020", "#ffd700"],
        transparent: false,
        additive: false,
        depthWrite: true,
        params: [{
                key: "fabricColor",
                label: "Fabric Color",
                type: "color",
                default: "#800020"
            },
            {
                key: "threadColor",
                label: "Thread Color",
                type: "color",
                default: "#ffd700"
            },
            {
                key: "patternScale",
                label: "Pattern Scale",
                type: "float",
                default: 3.0,
                min: 1.0,
                max: 8.0
            },
            {
                key: "embossHeight",
                label: "Emboss Height",
                type: "float",
                default: 0.4,
                min: 0.1,
                max: 1.0
            },
            {
                key: "metallic",
                label: "Metallic Shine",
                type: "float",
                default: 0.8,
                min: 0.0,
                max: 1.5
            },
            {
                key: "threadWidth",
                label: "Thread Width",
                type: "float",
                default: 0.3,
                min: 0.1,
                max: 0.8
            }
        ],
        fragUniforms: `
      uniform vec3 u_fabricColor;
      uniform vec3 u_threadColor;
      uniform float u_patternScale;
      uniform float u_embossHeight;
      uniform float u_metallic;
      uniform float u_threadWidth;
    `,
        fragBody: `
      vec2 uv = vWorldPos.xy * u_patternScale;
      float pattern = fbm(vec3(uv, 0.2));
      pattern = pattern * 0.6 + snoise(vec3(uv * 2.5, 0.7)) * 0.4;
      float filigree = sin(pattern * 10.0) * 0.5 + 0.5;
      filigree = smoothstep(u_threadWidth - 0.05, u_threadWidth, filigree);
      float emboss = pattern * 2.0 - 1.0;
      vec3 normalEmboss = normalize(N + vec3(emboss * u_embossHeight, emboss * u_embossHeight * 0.5, 0.0));
      float fabricShade = diff * 0.7 + 0.4;
      vec3 fabric = u_fabricColor * fabricShade;
      float spec = pow(max(0.0, dot(normalEmboss, normalize(V + KL))), 30.0) * u_metallic;
      col = mix(fabric, u_threadColor, filigree) + spec * u_threadColor * 0.7;
      alpha = 1.0;
    `
    },
    {
        id: "liquid_metal",
        name: "Liquid Metal",
        category: "Fluid Metals",
        kind: "shader",
        swatch: "#C0C0C0",
        flags: {
            isGravityAware: true
        },
        params: [{
                name: 'Flow Speed',
                default: 1.0,
                min: 0.0,
                max: 5.0
            },
            {
                name: 'Viscosity',
                default: 0.3,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Metallic Tint',
                default: 0.5,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Distortion',
                default: 0.7,
                min: 0.0,
                max: 2.0
            },
            {
                name: 'Contrast',
                default: 1.2,
                min: 0.5,
                max: 2.0
            },
        ],
        fragUniforms: `
      uniform float u_flowSpeed;
      uniform float u_viscosity;
      uniform float u_metallicTint;
      uniform float u_distortion;
      uniform float u_contrast;
    `,
        fragBody: `
      vec2 uv = vUv;
      vec3 gravity = normalize(uGravity);
      vec2 flowDir = gravity.xy * u_flowSpeed * 0.1;

      float n = 0.0;
      float amp = 1.0;
      float freq = 4.0;
      vec2 p = uv * 5.0;
      for (int i = 0; i < 5; i++) {
        n += amp * abs(snoise(vec3(p + flowDir * uTime * (1.0 + float(i) * 0.2), uTime * 0.1)));
        p *= 2.0;
        amp *= 0.5;
      }
      n = smoothstep(0.2, 0.8, n);

      vec3 metalBase = mix(vec3(0.7, 0.7, 0.8), vec3(0.9, 0.8, 0.6), u_metallicTint);
      float spec = pow(max(0.0, n - 0.5) * 2.0, 3.0);
      vec3 col = metalBase * (n * 0.8 + 0.2) + spec * vec3(1.0);
      col = mix(col, col * (1.0 + sin(uv.x * 10.0) * 0.2), u_distortion);
      col = mix(vec3(0.5), col, u_contrast);
      gl_FragColor = vec4(col, 1.0);
    `
    },
    {
        id: "leather",
        name: "Leather",
        category: "Materials & Textile",
        kind: "shader",
        swatch: "#8B4513",
        flags: {},
        params: [{
                name: 'Grain Size',
                default: 0.5,
                min: 0.1,
                max: 2.0
            },
            {
                name: 'Wrinkle Depth',
                default: 0.6,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Base Color',
                default: 0.0,
                min: 0.0,
                max: 1.0,
                isColor: true
            },
            {
                name: 'Aging',
                default: 0.3,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Gloss',
                default: 0.2,
                min: 0.0,
                max: 1.0
            },
        ],
        fragUniforms: `
      uniform float u_grainSize;
      uniform float u_wrinkleDepth;
      uniform vec3 u_baseColor;
      uniform float u_aging;
      uniform float u_gloss;
    `,
        fragBody: `
      vec2 uv = vUv;
      float grain = snoise(vec3(uv * 20.0 * u_grainSize, 0.0)) * 0.5 + 0.5;
      float wrinkle = abs(snoise(vec3(uv * 8.0 + uTime * 0.05, uTime * 0.02)));
      wrinkle = smoothstep(0.4, 1.0, wrinkle) * u_wrinkleDepth;

      float ageNoise = snoise(vec3(uv * 30.0, 1.0));
      float age = ageNoise * u_aging;

      vec3 leatherCol = mix(u_baseColor, u_baseColor * 0.7, grain * 0.8 + wrinkle * 0.6);
      leatherCol += age * vec3(0.6, 0.5, 0.4);
      leatherCol = clamp(leatherCol, 0.0, 1.0);

      float spec = pow(max(0.0, grain * 1.2 - 0.2), 8.0) * u_gloss;
      vec3 finalCol = leatherCol + spec;
      gl_FragColor = vec4(finalCol, 1.0);
    `
    },
    {
        id: "wood_grain",
        name: "Wood Grain",
        category: "Wood & Natural Materials",
        kind: "shader",
        swatch: "#8B5A2B",
        flags: {},
        params: [{
                name: 'Grain Scale',
                default: 1.0,
                min: 0.2,
                max: 3.0
            },
            {
                name: 'Knot Density',
                default: 0.3,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Ring Contrast',
                default: 1.5,
                min: 0.5,
                max: 3.0
            },
            {
                name: 'Wood Tint',
                default: 0.5,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Gloss',
                default: 0.15,
                min: 0.0,
                max: 0.5
            },
        ],
        fragUniforms: `
      uniform float u_grainScale;
      uniform float u_knotDensity;
      uniform float u_ringContrast;
      uniform float u_woodTint;
      uniform float u_gloss;
    `,
        fragBody: `
      vec2 uv = vUv * u_grainScale;
      vec2 grain = vec2(uv.x, uv.y + sin(uv.x * 6.28318) * 0.1);
      float rings = abs(sin(grain.y * 20.0 + grain.x * 5.0)) * 0.5 + 0.5;
      rings = pow(rings, u_ringContrast);

      float knot = 1.0;
      for (int i = 0; i < 3; i++) {
        vec2 knotPos = vec2(
          snoise(vec3(float(i) * 1.3, 0.0, 0.5)) * 0.8 + 0.5,
          snoise(vec3(float(i) * 2.7, 1.0, 0.3)) * 0.8 + 0.5
        );
        float d = length(grain - knotPos) * 8.0;
        float knotShape = 1.0 - smoothstep(0.2, 0.6, d) * 0.9;
        knot = min(knot, knotShape);
      }

      float wood = mix(rings, rings * 0.8 + knot * 0.5, u_knotDensity);
      vec3 baseCol = mix(vec3(0.6, 0.4, 0.2), vec3(0.8, 0.6, 0.3), u_woodTint);
      vec3 col = baseCol * (0.5 + wood * 0.6);
      float gloss = pow(rings * 0.8, 3.0) * u_gloss;
      col += gloss;
      gl_FragColor = vec4(col, 1.0);
    `
    },
    {
        id: "carbon_fiber",
        name: "Carbon Fiber",
        category: "Industrial",
        kind: "shader",
        swatch: "#2F4F4F",
        flags: {},
        params: [{
                name: 'Weave Scale',
                default: 2.0,
                min: 0.5,
                max: 5.0
            },
            {
                name: 'Anisotropy',
                default: 0.8,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Fiber Width',
                default: 0.6,
                min: 0.1,
                max: 1.0
            },
            {
                name: 'Highlight',
                default: 0.5,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Darkness',
                default: 0.3,
                min: 0.0,
                max: 0.8
            },
        ],
        fragUniforms: `
      uniform float u_weaveScale;
      uniform float u_anisotropy;
      uniform float u_fiberWidth;
      uniform float u_highlight;
      uniform float u_darkness;
    `,
        fragBody: `
      vec2 uv = vUv * u_weaveScale;
      float weave = abs(sin(uv.x * 3.14159 * 2.0)) * 0.5 + 0.5;
      float fiber = abs(sin(uv.y * 12.0)) > (1.0 - u_fiberWidth) ? 0.2 : 0.8;
      float pattern = weave * 0.7 + fiber * 0.5;
      float anisotropy = mix(0.0, (uv.x - floor(uv.x)) * 0.5, u_anisotropy);
      float dark = pattern * (1.0 - u_darkness) + u_darkness;
      float highlight = pow(pattern, 2.0) * u_highlight;
      float finalVal = dark + highlight + anisotropy;
      vec3 col = vec3(finalVal * 0.8, finalVal * 0.85, finalVal * 0.9);
      gl_FragColor = vec4(col, 1.0);
    `
    },
    {
        id: "honeycomb",
        name: "Honeycomb",
        category: "Geometric Patterns",
        kind: "shader",
        swatch: "#DAA520",
        flags: {},
        params: [{
                name: 'Cell Size',
                default: 1.5,
                min: 0.5,
                max: 3.0
            },
            {
                name: 'Wall Thickness',
                default: 0.4,
                min: 0.1,
                max: 1.0
            },
            {
                name: 'Depth Variation',
                default: 0.6,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Warm Tone',
                default: 0.7,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Highlight',
                default: 0.3,
                min: 0.0,
                max: 1.0
            },
        ],
        fragUniforms: `
      uniform float u_cellSize;
      uniform float u_wallThickness;
      uniform float u_depthVariation;
      uniform float u_warmTone;
      uniform float u_highlight;
    `,
        fragBody: `
      vec2 uv = vUv * u_cellSize;
      vec2 q = vec2(uv.x * 1.7320508, uv.y);
      vec2 id = round(q);
      vec2 gv = q - id;
      float angle = atan(gv.y, gv.x) + 3.14159;
      float sector = floor(angle / (3.14159 / 3.0));
      float dist = length(gv);
      float hexDist = 0.5 / cos(mod(angle + 3.14159 / 6.0, 3.14159 / 3.0) - 3.14159 / 6.0);
      float edge = smoothstep(hexDist * 0.95, hexDist, dist);
      float depth = snoise(vec3(id * 0.7, 0.0)) * u_depthVariation;
      float wall = 1.0 - edge * u_wallThickness;
      wall += depth * 0.3;
      float highlight = pow(max(0.0, 1.0 - dist * 2.0), 3.0) * u_highlight;
      vec3 col = mix(vec3(0.85, 0.65, 0.2), vec3(0.95, 0.8, 0.4), u_warmTone) * (wall * 0.7 + 0.3);
      col += highlight * vec3(1.0, 0.9, 0.6);
      gl_FragColor = vec4(col, 1.0);
    `
    },
    {
        id: "moire_pattern",
        name: "Moiré Interference",
        category: "Mathematical Art",
        kind: "shader",
        swatch: "#000000",
        flags: {},
        params: [{
                name: 'Scale A',
                default: 1.5,
                min: 0.5,
                max: 3.0
            },
            {
                name: 'Scale B',
                default: 1.0,
                min: 0.5,
                max: 3.0
            },
            {
                name: 'Angle Offset',
                default: 0.5,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Contrast',
                default: 1.0,
                min: 0.5,
                max: 2.0
            },
            {
                name: 'Color Shift',
                default: 0.5,
                min: 0.0,
                max: 1.0
            },
        ],
        fragUniforms: `
      uniform float u_scaleA;
      uniform float u_scaleB;
      uniform float u_angleOffset;
      uniform float u_contrast;
      uniform float u_colorShift;
    `,
        fragBody: `
      vec2 uv = vUv;
      float angle = u_angleOffset * 3.14159;
      vec2 uv1 = vec2(uv.x * cos(angle) - uv.y * sin(angle), uv.x * sin(angle) + uv.y * cos(angle)) * 10.0 * u_scaleA;
      vec2 uv2 = uv * 10.0 * u_scaleB;
      float pattern1 = sin(uv1.x) * sin(uv1.y);
      float pattern2 = sin(uv2.x) * sin(uv2.y);
      float moire = abs(pattern1 - pattern2);
      moire = pow(moire, u_contrast);
      vec3 col = hsv2rgb(vec3(moire * 0.5 + u_colorShift * 0.7, 0.8, 0.9));
      gl_FragColor = vec4(col, 1.0);
    `
    },
    {
        id: "distorted_grid",
        name: "Distorted Grid",
        category: "Mathematical Art",
        kind: "shader",
        swatch: "#FFFFFF",
        flags: {},
        params: [{
                name: 'Frequency',
                default: 1.0,
                min: 0.5,
                max: 4.0
            },
            {
                name: 'Distortion Amount',
                default: 0.8,
                min: 0.0,
                max: 2.0
            },
            {
                name: 'Line Thickness',
                default: 0.5,
                min: 0.1,
                max: 1.0
            },
            {
                name: 'Color1',
                default: 0.0,
                min: 0.0,
                max: 1.0,
                isColor: true
            },
            {
                name: 'Color2',
                default: 0.6,
                min: 0.0,
                max: 1.0,
                isColor: true
            },
        ],
        fragUniforms: `
      uniform float u_frequency;
      uniform float u_distortionAmount;
      uniform float u_lineThickness;
      uniform vec3 u_color1;
      uniform vec3 u_color2;
    `,
        fragBody: `
      vec2 uv = vUv * u_frequency;
      float distort = snoise(vec3(uv * 1.5 + uTime * 0.2, 0.5)) * u_distortionAmount;
      float gridX = abs(sin(uv.x * 3.14159 * 2.0 + distort)) > (1.0 - u_lineThickness) ? 0.0 : 1.0;
      float gridY = abs(sin(uv.y * 3.14159 * 2.0 + distort)) > (1.0 - u_lineThickness) ? 0.0 : 1.0;
      float grid = 1.0 - gridX * gridY;
      vec3 col = mix(u_color1, u_color2, grid);
      gl_FragColor = vec4(col, 1.0);
    `
    },
    {
        id: "glitch_art",
        name: "Glitch Art",
        category: "Digital Degradation",
        kind: "shader",
        swatch: "#FF00FF",
        flags: {},
        params: [{
                name: 'Block Size',
                default: 0.05,
                min: 0.01,
                max: 0.2
            },
            {
                name: 'Shift Amount',
                default: 0.5,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Tearing',
                default: 0.3,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Color Split',
                default: 1.0,
                min: 0.0,
                max: 2.0
            },
            {
                name: 'Noise Level',
                default: 0.2,
                min: 0.0,
                max: 1.0
            },
        ],
        fragUniforms: `
      uniform float u_blockSize;
      uniform float u_shiftAmount;
      uniform float u_tearing;
      uniform float u_colorSplit;
      uniform float u_noiseLevel;
    `,
        fragBody: `
      vec2 uv = vUv;
      float blockY = floor(uv.y / u_blockSize) * u_blockSize;
      float rand = hash(blockY);
      float shift = (rand - 0.5) * u_shiftAmount * step(u_tearing, hash(blockY + 0.1));
      float colorShift = (rand - 0.5) * u_colorSplit * step(u_noiseLevel, hash(blockY + 0.2));
      vec2 glitchUV = uv;
      glitchUV.x += shift;
      float r = sin(glitchUV.x * 50.0) * 0.5 + 0.5;
      float g = sin((glitchUV.x + colorShift) * 50.0) * 0.5 + 0.5;
      float b = sin((glitchUV.x - colorShift) * 50.0) * 0.5 + 0.5;
      float noise = hash(vec2(floor(uv.y * 40.0), floor(uv.x * 40.0))) * u_noiseLevel;
      vec3 col = vec3(r, g, b) + noise;
      col = clamp(col, 0.0, 1.0);
      gl_FragColor = vec4(col, 1.0);
    `
    },
    {
        id: "sand_dollar",
        name: "Sand Dollar",
        category: "Organics & Sea",
        kind: "shader",
        swatch: "#F5DEB3",
        flags: {},
        params: [{
                name: 'Size',
                default: 1.0,
                min: 0.5,
                max: 2.0
            },
            {
                name: 'Petal Count',
                default: 5,
                min: 3,
                max: 8,
                step: 1
            },
            {
                name: 'Ridge Depth',
                default: 0.7,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Texture Noise',
                default: 0.4,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Color Warmth',
                default: 0.6,
                min: 0.0,
                max: 1.0
            },
        ],
        fragUniforms: `
      uniform float u_size;
      uniform int u_petalCount;
      uniform float u_ridgeDepth;
      uniform float u_textureNoise;
      uniform float u_colorWarmth;
    `,
        fragBody: `
      vec2 uv = vUv - 0.5;
      float r = length(uv) * u_size;
      float a = atan(uv.y, uv.x);
      float petalCount = float(u_petalCount);
      float petal = sin(a * petalCount) * 0.5 + 0.5;
      float ridge = sin(r * 15.0) * 0.5 + 0.5;
      float shape = petal * ridge;
      shape = smoothstep(0.3, 0.7, shape) * u_ridgeDepth + (1.0 - u_ridgeDepth);
      float noise = snoise(vec3(uv * 5.0, 0.0)) * u_textureNoise;
      float pattern = min(1.0, shape + noise);
      vec3 col = mix(vec3(0.96, 0.87, 0.70), vec3(0.94, 0.81, 0.65), u_colorWarmth) * pattern;
      float edge = 1.0 - smoothstep(0.45, 0.5, r);
      col *= edge;
      gl_FragColor = vec4(col, 1.0);
    `
    },
    {
        id: "camouflage",
        name: "Camouflage",
        category: "Patterns & Textiles",
        kind: "shader",
        swatch: "#556B2F",
        flags: {},
        params: [{
                name: 'Blob Size',
                default: 1.0,
                min: 0.5,
                max: 3.0
            },
            {
                name: 'Complexity',
                default: 0.5,
                min: 0.1,
                max: 1.0
            },
            {
                name: 'Color1',
                default: 0.0,
                min: 0.0,
                max: 1.0,
                isColor: true
            },
            {
                name: 'Color2',
                default: 0.3,
                min: 0.0,
                max: 1.0,
                isColor: true
            },
            {
                name: 'Color3',
                default: 0.6,
                min: 0.0,
                max: 1.0,
                isColor: true
            },
        ],
        fragUniforms: `
      uniform float u_blobSize;
      uniform float u_complexity;
      uniform vec3 u_color1;
      uniform vec3 u_color2;
      uniform vec3 u_color3;
    `,
        fragBody: `
      vec2 uv = vUv * u_blobSize;
      float n1 = snoise(vec3(uv * 3.0, 0.0));
      float n2 = snoise(vec3(uv * 5.0 + 10.0, 1.0));
      float blob = n1 * 0.6 + n2 * 0.4;
      blob = smoothstep(0.2, 0.8, blob) * u_complexity;
      blob += snoise(vec3(uv * 8.0, 2.0)) * 0.3;
      float t = clamp((blob + 0.5) * 1.5, 0.0, 1.0);
      vec3 col = mix(mix(u_color1, u_color2, t), u_color3, pow(t, 2.0));
      gl_FragColor = vec4(col, 1.0);
    `
    },
    {
        id: "cave_painting",
        name: "Cave Painting",
        category: "Ancient & Painted",
        kind: "shader",
        swatch: "#A0522D",
        flags: {},
        params: [{
                name: 'Rock Roughness',
                default: 0.7,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Pigment Density',
                default: 0.5,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Ochre Hue',
                default: 0.15,
                min: 0.0,
                max: 0.5
            },
            {
                name: 'Crack Depth',
                default: 0.4,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Age',
                default: 0.6,
                min: 0.0,
                max: 1.0
            },
        ],
        fragUniforms: `
      uniform float u_rockRoughness;
      uniform float u_pigmentDensity;
      uniform float u_ochreHue;
      uniform float u_crackDepth;
      uniform float u_age;
    `,
        fragBody: `
      vec2 uv = vUv;
      float rock = snoise(vec3(uv * 10.0, 0.0)) * u_rockRoughness;
      rock += snoise(vec3(uv * 25.0, 1.0)) * 0.3;
      rock = rock * 0.5 + 0.5;

      float pigment = snoise(vec3(uv * 15.0 + 5.0, 2.0)) * u_pigmentDensity;
      pigment = smoothstep(0.2, 0.8, pigment);

      vec3 ochre = hsv2rgb(vec3(u_ochreHue, 0.7, 0.6));
      vec3 baseCol = mix(vec3(0.6, 0.55, 0.45), ochre, pigment);

      float crack = abs(snoise(vec3(uv * 25.0, 3.0)));
      crack = smoothstep(0.5 - u_crackDepth * 0.3, 0.5, crack);

      float age = snoise(vec3(uv * 5.0, 4.0)) * u_age;
      vec3 col = baseCol * (1.0 - crack * 0.7) * (1.0 - age * 0.5);
      col = clamp(col, 0.0, 1.0);
      gl_FragColor = vec4(col, 1.0);
    `
    },
    {
        id: "neon_lights",
        name: "Neon Lights",
        category: "Neon & Fluorescent",
        kind: "shader",
        swatch: "#FF1493",
        flags: {
            isAdditive: true
        },
        params: [{
                name: 'Tube Spacing',
                default: 1.0,
                min: 0.5,
                max: 3.0
            },
            {
                name: 'Glow Intensity',
                default: 0.8,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Speed',
                default: 1.0,
                min: 0.0,
                max: 2.0
            },
            {
                name: 'Color Variation',
                default: 0.6,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Blur Width',
                default: 0.3,
                min: 0.05,
                max: 0.8
            },
        ],
        fragUniforms: `
      uniform float u_tubeSpacing;
      uniform float u_glowIntensity;
      uniform float u_speed;
      uniform float u_colorVariation;
      uniform float u_blurWidth;
    `,
        fragBody: `
      vec2 uv = vUv;
      float y = uv.y;
      float tube = abs(sin(y * 15.0 * u_tubeSpacing + uTime * u_speed)) < u_blurWidth ? 1.0 : 0.0;
      float glow = exp(-abs(sin(y * 15.0 * u_tubeSpacing + uTime * u_speed)) * 5.0 / u_blurWidth) * u_glowIntensity;
      float hue = fract(y * u_colorVariation + uTime * 0.1);
      vec3 col = hsv2rgb(vec3(hue, 1.0, 1.0)) * (tube + glow);
      gl_FragColor = vec4(col, col.r + col.g + col.b > 0.0 ? 1.0 : 0.0);
    `
    },
    {
        id: "acid_wash",
        name: "Acid Wash Denim",
        category: "Denim & Fabric",
        kind: "shader",
        swatch: "#4B6E9C",
        flags: {},
        params: [{
                name: 'Wash Intensity',
                default: 0.7,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Fiber Detail',
                default: 0.5,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Contrast',
                default: 1.2,
                min: 0.5,
                max: 2.0
            },
            {
                name: 'Blue Hue',
                default: 0.6,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Fade Amount',
                default: 0.4,
                min: 0.0,
                max: 1.0
            },
        ],
        fragUniforms: `
      uniform float u_washIntensity;
      uniform float u_fiberDetail;
      uniform float u_contrast;
      uniform float u_blueHue;
      uniform float u_fadeAmount;
    `,
        fragBody: `
      vec2 uv = vUv;
      float noise = snoise(vec3(uv * 20.0, 0.0)) * 0.5 + 0.5;
      float fiber = snoise(vec3(uv * 80.0 + 5.0, 1.0)) * u_fiberDetail;
      float wash = snoise(vec3(uv * 8.0 + 3.0, 2.0)) * u_washIntensity;
      wash = smoothstep(0.3, 0.7, wash);
      float pattern = mix(noise, wash, 0.7) + fiber * 0.2;
      pattern = mix(pattern, pattern * 1.5, u_contrast - 1.0);
      float fade = snoise(vec3(uv * 12.0, 3.0)) * u_fadeAmount;
      vec3 baseBlue = hsv2rgb(vec3(u_blueHue, 0.8, 0.7));
      vec3 white = vec3(0.95);
      vec3 col = mix(baseBlue, white, pattern * fade);
      gl_FragColor = vec4(col, 1.0);
    `
    },
    {
        id: "asteroid",
        name: "Asteroid Surface",
        category: "Astro & Planetary",
        kind: "shader",
        swatch: "#696969",
        flags: {},
        params: [{
                name: 'Rock Density',
                default: 0.8,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Crater Size',
                default: 1.0,
                min: 0.5,
                max: 2.0
            },
            {
                name: 'Regolith Noise',
                default: 0.6,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Gray Tint',
                default: 0.5,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Roughness',
                default: 0.7,
                min: 0.0,
                max: 1.0
            },
        ],
        fragUniforms: `
      uniform float u_rockDensity;
      uniform float u_craterSize;
      uniform float u_regolithNoise;
      uniform float u_grayTint;
      uniform float u_roughness;
    `,
        fragBody: `
      vec2 uv = vUv;
      float rock = snoise(vec3(uv * 10.0, 0.0)) * u_rockDensity;
      rock += snoise(vec3(uv * 25.0, 1.0)) * 0.3;
      rock = rock * 0.5 + 0.5;

      float crater = 1.0;
      for (int i = 0; i < 4; i++) {
        vec2 center = vec2(
          hash(vec2(uv.x + float(i) * 0.4, float(i))),
          hash(vec2(uv.y - float(i), float(i) * 2.0))
        ) * 0.8 + 0.1;
        float dist = length(uv - center) * u_craterSize;
        float ring = abs(dist - 0.15) < 0.02 ? 0.2 : 0.0;
        crater = min(crater, 1.0 - ring);
      }

      float regolith = snoise(vec3(uv * 40.0, 2.0)) * u_regolithNoise;
      regolith = regolith * 0.5 + 0.5;
      float surface = (rock * 0.6 + regolith * 0.4) * crater;
      surface = mix(surface, surface * 0.8 + 0.2, u_roughness);
      vec3 col = mix(vec3(0.5, 0.5, 0.5), vec3(0.7, 0.7, 0.7), u_grayTint) * surface;
      col += regolith * 0.1;
      gl_FragColor = vec4(col, 1.0);
    `
    },
    {
        id: "frosted_glass",
        name: "Frosted Glass",
        category: "Glass & Transmission",
        kind: "shader",
        swatch: "#E0FFFF",
        flags: {
            isGlass: true
        },
        params: [{
                name: 'Frost Roughness',
                default: 0.7,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Scatter Strength',
                default: 0.5,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Tint',
                default: 0.0,
                min: 0.0,
                max: 1.0,
                isColor: true
            },
            {
                name: 'Speckle Size',
                default: 0.3,
                min: 0.1,
                max: 1.0
            },
            {
                name: 'Opacity',
                default: 0.6,
                min: 0.0,
                max: 1.0
            },
        ],
        fragUniforms: `
      uniform float u_frostRoughness;
      uniform float u_scatterStrength;
      uniform vec3 u_tint;
      uniform float u_speckleSize;
      uniform float u_opacity;
    `,
        fragBody: `
      vec2 uv = vUv;
      float noise = snoise(vec3(uv * 50.0 * u_speckleSize, 0.0)) * u_frostRoughness;
      noise += snoise(vec3(uv * 30.0, 1.0)) * 0.3;
      float scatter = snoise(vec3(uv * 20.0, 2.0)) * u_scatterStrength;
      float alpha = mix(0.3, 0.9, u_opacity) * (noise * 0.7 + 0.3);
      vec3 col = mix(vec3(0.9, 0.95, 1.0), u_tint, 0.3) + scatter * 0.2;
      gl_FragColor = vec4(col, alpha);
    `
    },
    {
        id: "jellyfish",
        name: "Jellyfish",
        category: "Underwater Life",
        kind: "shader",
        swatch: "#E6E6FA",
        flags: {
            isAdditive: true
        },
        params: [{
                name: 'Tentacle Count',
                default: 8,
                min: 4,
                max: 16,
                step: 1
            },
            {
                name: 'Waviness',
                default: 0.6,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Translucency',
                default: 0.7,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Color Cycle',
                default: 0.5,
                min: 0.0,
                max: 1.0
            },
            {
                name: 'Glow',
                default: 0.4,
                min: 0.0,
                max: 1.0
            },
        ],
        fragUniforms: `
      uniform int u_tentacleCount;
      uniform float u_waviness;
      uniform float u_translucency;
      uniform float u_colorCycle;
      uniform float u_glow;
    `,
        fragBody: `
      vec2 uv = vUv - 0.5;
      float r = length(uv);
      float a = atan(uv.y, uv.x);
      float tentacleCount = float(u_tentacleCount);
      float tentacle = 1.0 - abs(sin(a * tentacleCount + uTime * 0.5) * 0.5 + 0.5);
      tentacle = smoothstep(0.0, 0.1, tentacle);
      float wave = sin(a * 4.0 + r * 20.0 - uTime * 3.0 * u_waviness) * 0.5 + 0.5;
      float opacity = tentacle * wave * u_translucency;
      float bell = smoothstep(0.5, 0.4, r) * 0.6;
      float edge = smoothstep(0.0, 0.1, r) * smoothstep(0.45, 0.5, r);
      float jelly = mix(opacity, bell, 0.6) * edge;
      jelly += u_glow * exp(-r * 5.0);
      vec3 col = hsv2rgb(vec3(fract(a / 6.28318 + uTime * 0.1 * u_colorCycle), 0.6, 1.0));
      col *= jelly;
      gl_FragColor = vec4(col, jelly);
    `
    }
];